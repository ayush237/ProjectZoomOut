Redesign the sticky notes moment so it actually reads as physical notes pinned to a board, not styled cards in a list - this is the fourth of the five Leaf slides and it currently has none of that physicality. Render two to six notes, the app's own range for this slide, scattered across a textured board background - cork, felt or a wood grain, not a flat colour - each note independently rotated a few degrees so they don't sit in a grid, each held down by a small pin or a torn piece of tape at the top edge, and each with a soft drop shadow beneath it so it reads as a raised piece of paper rather than a flat rectangle. Keep every note within colours already in the design system's palette - do not introduce amber, since that colour is reserved for reward moments only and this slide is not one. The StickyNote component is shared with the notes-collection screen reached from Profile, so update it there too rather than only in the Leaf slide, and confirm both still render correctly afterward. If making the text read as handwritten needs an accent font the design system doesn't already have, say so explicitly rather than picking one quietly - that's a real addition to the system, not a one-off styling choice. Do not make the notes draggable or editable; this is a recap, not a workspace. Do not let the board scroll internally - if six notes don't fit the frame, let the board grow taller. Do not put lined or graph-paper texture on the notes themselves - a plain paper note against a textured board is the contrast that will make it read as real.
