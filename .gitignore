# Secrets — never commit
.env
.env.*
!.env.example

# Dependencies
node_modules/

# Build output
dist/
build/
*.tsbuildinfo

# Next.js (apps/admin)
.next/
next-env.d.ts

# Test output
coverage/

# Expo / React Native
.expo/
.expo-shared/
web-build/
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Python (apps/pipeline) — the pipeline is a standalone Python project.
# These are unanchored so they hold on every branch regardless of eslint config:
# an untracked .venv already produced 26 phantom lint errors and sits one
# `git add .` away from committing a virtualenv.
.venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Pipeline run artefacts — book text, embeddings scratch, per-run output
apps/pipeline/.data/
apps/pipeline/runs/

# Payload's local upload store. Generated assets, not source: 70 files and 50MB
# after one illustrated Track, and it grows per book. Production serves these
# from object storage, so committing them would bloat the repo permanently to
# mirror a directory that is rebuilt from the CMS anyway.
apps/admin/media/

# Editors and OS
.DS_Store
.idea/
*.swp

# Claude Code local overrides
.claude/settings.local.json
