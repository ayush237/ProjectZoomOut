# Project Roadmap

Owned by Architect.

## Where things stand — 2026-09-25

**Read this first if you are a fresh session.** Rewritten 2026-09-24 at an Architect clear; **the ONBOARD bullets were patched 2026-09-25 at ONBOARD-3's sign-off.** The previous
version was dated 2026-09-18 and had been patched in place since — by the end it still said two merged PRs
were open and that a ruling was pending which had been executed two days earlier.

> **⚠️ Check your branch before trusting anything below.** Checkouts in this project have repeatedly
> sat on stale feature branches. `git log --oneline -1 origin/main` — never `HEAD`.

**Four worktrees, and `main` lives in `ZO` alone.** `ZO` (Architect, `main`) · `ZO-admin` (Manager) ·
`ZO-pipeline` (Pipeline Manager) · `ZO-vo3` (Manager — reused for ONBOARD-3). **Never `git checkout main`
in a linked worktree** — branch from `origin/main` instead.

### What is true today

- **The app works end to end on a real phone, and onboarding has been walked once.** Voiceover is finished
  (144 clips, both narrators, verified against the CMS; VO-3 `060a286`), and ONBOARD-1 and PILOT-1 were
  device-gated 2026-09-23/24 on Android/Expo Go. **The narrators are Lara (female) and Druv (male)** — the
  provider's voice ids, Achernar and Sadaltager, are never spoken or shown. **The default is Druv**, ruled by
  the founder after listening to both 29:11 tracks in full.
- **ONBOARD-3 is signed off (2026-09-25, 17/17) and merged as PR #61 (`b32191a`, observed 2026-09-25); its
  device gate is the founder's** (Manager worktree `ZO-vo3`). `git pull` in `ZO`, then **restart the backend
  from it: an old backend answers 404 on `/content/narrator-samples`, and the narrator beat then has no way
  past its error screen.** The pre-flight in `GETTING_STARTED.md` checks the route. Manager's report is now
  the first entry under Completions in `collaboration-log.md`.
- **ONBOARD-2 and ONBOARD-2.1 are closed.** ONBOARD-2's follow-up merged as PR #60 (`2f52e47`); ONBOARD-2.1
  is signed off (2026-09-25, 14/14) and merged as PR #62 (`6c5d8fd`) — $0, no model calls: the legal fence
  pinned, the ceiling's five `reserve`/`settle` lines pinned in both render copies, an upload pre-flight, and
  `narrate` at three attempts. **The cost-ledger lock is still open and slightly more urgent** — a
  precondition of book #3's narration package.
- **Waiting on the founder:** *proposed, not yet approved:* **ONBOARD-3.1** — Manager, Sonnet, after the
  device gate: the narrator beat's failure handling and stored-preference overwrite, pick-book's choose/skip
  races, and a copy pass (Explore's two "fifteen minutes" lines, two drafted lines that are factually wrong,
  whatever the founder edits at the gate). Then a small Manager bundle — a relative `coverUrl` in the admin
  publish rule (ends the LAN-IP pinning below) and a Python CI job (also below).
- **Next, in order:** **the archive pass (log, register, board) — unblocked now** (#60 is in, 2.1 is signed off,
  and no session is mid-package). **Starting ONBOARD-3.1 would block it again, so it goes first:** a session
  that has already read the log must not have it rewritten under it. Then a natural Architect clear, with the
  record complete.
- **The library is 28 Tracks of which two are real** — Track 42 (*The Science of Getting Rich*) and Track 50
  (*Ikigai*). **Still the largest gap: the app works, and there is almost nothing in it to read.** Book #3
  is the next real lever. **Its narration package needs the cost-ledger lock first** (register row; ruled out
  of ONBOARD-2.1 on 2026-09-25).
- **Nothing is deployed.** It all runs on the founder's Mac; reviews come from friends, in person — the
  2026-09-15 ruling to stay on the free setup stands. `MEDIA_BASE_URL` (PILOT-1, merged) is the prerequisite
  for deployment.
- **The covers are our own art, uploaded 2026-09-23 — pinned to an absolute LAN address**, because the
  admin's publish rule rejects a relative path. Any network change breaks both until the two `coverUrl`s
  are edited (debt row on `checkCoverUrlIsImage`).
- **No CI job runs the Python pipeline.** "CI green" on a pipeline PR has only ever meant the JS
  workspaces; pipeline sign-offs rest on the report's counts plus a reviewer running the gate by hand
  (debt row, verified 2026-09-24).

### The environment facts that cost time if rediscovered

- **A device session needs four things pointing at the Mac's current LAN address, and it changes.**
  (1) `apps/mobile/.env`: `EXPO_PUBLIC_API_URL=http://<IP>:3000` — **one line, key once**; a pasted
  `EXPO_PUBLIC_API_URL=EXPO_PUBLIC_API_URL=http://…` is not a URL, and the app reports it as "Could not
  reach ZoomOut". (2) The backend started with `MEDIA_BASE_URL=http://<IP>:3001/api` and
  `HIDE_PLACEHOLDER_CONTENT=true` — without them images and audio silently fail on a phone while text
  loads, and Explore shows all 28 Tracks. (3) Both Tracks' `coverUrl`. (4) Expo restarted with `--clear`.
  **Run the pre-flight in `GETTING_STARTED.md` ("Before a device gate") instead of finding these one at a
  time** — it cost most of a day on 2026-09-23, none of it app code.
- **Per-install flags persist across accounts.** `introSeen`, `onboardingSeen` and the coach-mark flag are
  SecureStore, keyed to the install, not the reader — a used phone shows none of onboarding. **Clear Expo
  Go's storage before a gate** (Android: Settings → Apps → Expo Go → Storage). SecureStore also survives
  uninstall and reinstall on the iOS Simulator.
- **`Ctrl+C` does not always kill Metro.** A 13-day-old process held port 8081 while a new one took 8082,
  and the phone kept reconnecting to the old one. The pre-flight counts listening ports in 8081–8089 (want
  exactly one once Expo is running).
- **The device gate runs on an Android phone over Expo Go.** Worth stating because the rest of this file may
  read iOS — the VO-3 crash was `expo.modules.audio.AudioPlayer` receiving a `java.lang.Integer`, an Android
  stack. **A native iOS build fails on this Mac** (`expo-modules-jsi` under Xcode 26.3 / Swift 6, one Xcode
  installed); device gates run on Expo Go. Do not pin an older Xcode.
- **Payload's file route answers `GET`, not `HEAD`** — a `HEAD` returns 404 for a file that is there. Check
  with `curl -s -o /dev/null -w '%{http_code}'`, not `curl -I`.
- **The backend runs healthy on 3000 and Payload/admin on 3001.** An unrelated Next.js app holds 3000 only
  if it is started first.
- **A `git pull` does not run `npm install`.** `ZO`'s `node_modules` was stale against the lockfile until
  2026-09-23 (Expo failed with "Failed to resolve plugin for module expo-audio"); run `npm install` after
  any pull that touched dependencies. **`ZO`'s `apps/pipeline/.venv` is stale too** (mypy shows 20 spurious
  errors) — run the pipeline gate from `ZO-pipeline`.
- **An Architect session cannot read `.env` files** (permission denied) — ask the founder to paste the line.
  Services an Architect session starts in the background may not outlive it; re-run the pre-flight after a
  clear.

### The token budget — measured 2026-09-24

**The collaboration log is 186KB (~46k tokens), up from ~14k on 09-22** — ONBOARD-1, -2, -3, COVER-1 and
PILOT-1 landed and none is archived yet, correctly (never mid-package). **Archive them once #60 and #61 have
merged** (ONBOARD-3 was signed off 2026-09-25). The roadmap is 162KB (~40k tokens). The 09-22 work is done: the decisions log was split (13
rules promoted into the persona files, 31 rows archived), the register swept (16 of 157 rows were not
true; ~138 now) and six founder decisions moved to `launch-blockers.md` Part 3. **The rest of what was
proposed then — assigned rows out of the register, environment facts out, the remaining founder decisions —
was only partly executed**, and this session added eight long register rows and several long board rows.
**Manager and Pipeline Manager are protected by their reading rule (their handoff only). An Architect
orients from this section, the status board and the log's headers — do not read the decisions log or the
register end to end.** The connector finding stands: Notion and Drive off by default removed ~56 tool
definitions from every request of every session.

## Phase 1 status board

**Single source of truth for "what's done, what's next."** Architect updates this at every sign-off and every handoff.

**✅ Reached 2026-08-13.** Every Phase 1 mechanic works end to end: sign up, browse, add to library, play a Leaf, answer, unlock the payoff, earn XP, keep a streak, hit a graceful cap, wrap up, share, unlock achievements, report an error, and see the legal surfaces. Next: sequence `launch-blockers.md`.

~~Current goal: an end-to-end working app~~ — every Phase 1 feature working against seeded content on a device. That is **WP5b → WP9 → WP10**, three packages.

**Launch blockers live in `launch-blockers.md`, not here** (split 2026-08-12). Deployment, password reset, test hardening, real content, and every legal item are parked there so they stop competing with the build. Nothing in that file blocks current work.

Legend: ✅ done · 🔵 in progress · ⬜ not started · 🔒 blocked

### Work packages

| # | Delivers | Owner | Status | Waiting on |
|---|---|---|---|---|
| WP0 | Monorepo, shared types, Postgres, CI | Manager | ✅ Signed off 2026-08-06 (PR #1) | — |
| WP1 | Payload 3.x CMS — Track + Leaf collections, publish-time validation | Manager | ✅ Signed off 2026-08-07 | — |
| **GATE** | **Schema freeze — one structurally complete Leaf authored** | **Founder** | ✅ **Closed 2026-08-08** — 4 findings, all ruled | — |
| WP2 | Backend foundation — auth (email/Apple/Google), age gate, profile | Manager | ✅ Signed off 2026-08-07 | — |
| WP2.1 | Schema-freeze alignment (4 gate rulings) + small backend gaps (logout, error split, token reaping) | Manager | ✅ Signed off 2026-08-08 (12/12) | — |
| WP3 | Content API — Explore, Library, Track/Leaf delivery | Manager | ✅ Signed off 2026-08-08 (11/11) | — |
| WP4 | Learning loop API — answer, unlock payoff, complete Leaf, award XP | Manager | ✅ **Signed off 2026-08-09 (12/12)** — reviewed by `code-reviewer`, no blocking defects | One ~10-line test to add before merge |
| WP5a | Session cap and streaks — server-authoritative | Manager | ✅ **Signed off 2026-08-12 (7/8)** | 8th is a device check, blocked on the env fix in WP5b |
| WP5b | Env fix, achievements, total XP | Manager | ✅ **Signed off 2026-08-12 (9/9)** — Part A resolved, verified by query | Founder to push the branch |
| ~~GATE~~ | ~~Device verification~~ | Manager | ✅ **Done in WP5b** — 7 items pass, first since WP5a | — |
| WP6 | Mobile shell — navigation, auth screens, age gate, design system | Manager | ✅ **Signed off 2026-08-11 (14/15)** after a rejected first pass and a clean second | 15th void — Apple/Google dormant |
| WP7 | Mobile surfaces — Explore, Library, Journey, Profile | Manager | ✅ **Signed off 2026-08-11 (11/11)** after a rejected first pass | — |
| WP11 | Seed fixture — full-length placeholder Track (~20 Leaves) | Manager | ✅ **Signed off 2026-08-12 (10/11)** | 11th deferred to WP8, where it becomes verifiable by tapping |
| WP8 | Mobile Leaf player — 5 slides, unlock gate, SFX | Manager | ✅ **Signed off 2026-08-12 (10/11)** — the loop works on a device | 11th is a **founder** device check: reduced-motion, and how the unlock feels |
| WP9 | Shareable session wrap-up + achievement screens | Manager | ✅ **Signed off 2026-08-13 (10/10)** | — |
| WP10 | Report-an-error, fix queue, legal surfaces | Manager | ✅ **Signed off 2026-08-13 (9/10)** | — |
| — | **🏁 PHASE 1 COMPLETE — the app works end to end** | — | ✅ **2026-08-13** | — |
| WP15 | Leaf v2 — scenario image, sticky-notes diagram, apply-in-life | Manager | ✅ **Signed off 2026-08-13 (10/10)** | — |
| WP16 | Pipeline skeleton — ingest, analyze, breakdown, human gate 1 | **Pipeline Manager** | ✅ **Signed off 2026-08-26 (12/12)** — reviewed against the diff, not the report | Merge to `main` |
| WP16.1 | Breakdown prompt + model measurement with n≥3 | **Pipeline Manager** | ✅ **Signed off 2026-08-27 (6/6)** — structure check 1-of-3 → 8-of-8; models measured indistinguishable | — |
| WP17 | Leaf generation, grounding, and the Payload draft boundary | **Pipeline Manager** | ✅ **Signed off 2026-08-28 (10/10)** — both halves; merged as PR #19 | — |
| WP15.1 | Track `acquisition` field + the red typecheck on `main` | Manager | ✅ **Signed off 2026-08-27 (7/7)** — root `typecheck` green for the first time since WP15 | — |
| WP18 | Assets — scenario images, diagram specs, one visual identity | **Pipeline Manager** | ✅ **Signed off 2026-08-29 (11/11)** — Track 42 fully illustrated, $2.34 against credit | Merge |
| WP15.2 | Publish-incapable API key + admin `allowedDevOrigins` | Manager | ✅ **Signed off 2026-08-29 (8/8)** — merged as PR #20 | `delete` scoping + the pipeline-side switch, both handed off |
| WP19 | Gate 2, the editorial reviewer, and the answer-length check | **Pipeline Manager** | ✅ **Signed off 2026-08-28 (11/11)** — closed live against real Track 42 | Merge |
| WP15.6 | Thumbnails for gate 2's image candidates | Manager | ✅ **Signed off 2026-08-29 (6/6)** | Merge |
| WP15.7 | "Use this candidate" — one click into `scenario.image` | Manager | ✅ **Signed off 2026-09-02 (7/7)** — merged as PR #28. The never-set-before case and sibling survival both proven by re-fetching the stored document | — |
| WP20 | **One book end to end — published** | **Pipeline Manager** | ✅ **Signed off 2026-08-29 (11/11)** — Track 42 live, raw text purged, verified by query | Merge PR #27 |
| — | **🏁 PHASE 2 COMPLETE — the pipeline produces publishable books** | — | ✅ **2026-08-29** | — |
| WP15.3 | Remove `delete` from the machine account | Manager | ✅ **Signed off 2026-08-30 (4/4)** — refusals confirmed by row counts, not status codes | Merge |
| WP15.4 | Gate 2's three fields on Leaves | Manager | ✅ **Signed off 2026-08-28 (8/8)** — verified against the live dev DB, not only a fresh container | Merge; **unblocks WP19's last 3** |
| WP15.5 | Empty-options validation bug + an overclaiming comment | Manager | ✅ **Signed off 2026-08-28 (6/6)** — narrow fix confirmed; both halves mutation-checked against the rule that carries each | Merge |
| WP20.1 | Attach Track 42's scenario images — one candidate per Leaf, as drafts | **Pipeline Manager** | ✅ **Signed off 2026-09-02 (6/6)** — 18/18 draft Leaves illustrated, 0/18 published. Sibling survival proven against the *published* copy, which the machine key structurally cannot touch | Open the PR; founder overrides 8 glow breaches, then publishes |
| WP15.8 | Resolve CMS-relative media URLs in the content mapper | Manager | ✅ **Signed off 2026-09-02 (7/7)** — a full Leaf played on a device, both media types rendering, in both themes. First time pipeline content has reached a reader's screen | Merged as PR #30 |
| — | **🎨 THE VISUAL REDESIGN — plan approved 2026-09-06, six packages.** Mockups complete for all 14 surfaces; see `projectplan.md` | — | — | — |
| WP21 | Redesign foundation — `react-native-svg`, Caveat font, token diff. **No screen changes** | Manager | ✅ **Signed off 2026-09-08 (8/8)** — **token diff came back zero**, checked in both directions. Device gate initially fell short and was reported as such, then closed on a signed build | Merged as PR #31 |
| WP22 | **Track roadmap** — the knowledge graph on real data. **Opus** | Manager | ✅ **Signed off 2026-09-09 (10/10)** — verified against the diff: geometry is genuinely pure, the test file names the 18-node trap itself, the `nextLeafId` cross-check ships with a degrade path. **The first screen of the redesign a reader can see** | PR #32 open |
| WP22.1 | Close the reduce-motion mechanism — one flag, four call sites | Manager | ✅ **Signed off 2026-09-09, now 6/6** — `motionTimingConfig` centralises the flag; three sites routed through it; **AuthStack correctly needed no change**, its transition being native rather than Reanimated. **The 6th closed 2026-09-10 by founder observation** | Merged as PR #33 |
| WP23 | Leaf player re-skin + the sticky-notes board | Manager | ⚠️ **Signed off 2026-09-11 on what it delivered — the board, not the re-skin.** `stickyNotesLayout.ts` is pure, Tier A and mutation-checked; Caveat has its first consumer; a VoiceOver reading-order bug was caught **before** it shipped. **Four slides untouched, correctly** — there is no Leaf player spec in the repo and Manager refused to guess | Merged as PR #34 |
| WP22.2 | **Port the roadmap's visuals from `graph.jsx`** — the first package under the changed method. **Opus** | Manager | ✅ **Signed off 2026-09-09 (9/9)** — and it found the drift's real cause, which was not the one I named | ⚠️ **PR #37 still OPEN as of 2026-09-10 — the code is not on `main`.** The roadmap screen still renders the pre-port version. **Expect a trivial conflict in `eslint.config.js`**, since WP25 shipped the same `design/**` line first |
| WP23.1 | Summary / Scenario / Payoff / Takeaway + the cork board | Manager | ✅ **Signed off 2026-09-09 (9/9)** — PayoffSlide ruling below | Branch `wp23.1-leaf-slides-cork-board`, unpushed |
| WP24 | Auth, age gate, and the Track legal surface | Manager | ✅ **Signed off (6/8 observed, 2 pending two screenshots)** — sign-up validation now mirrors `signUpBodySchema` exactly, closing the malformed-email defect the spec and the bug shared. **AgeGate/AgeRefused audited and deliberately left unchanged** | Merged as PR #35. **Open: the sign-up → age-gate seam, unlooked-at** |
| WP25 | Achievement unlock, session-end/cap, report-error, failure states | Manager | ✅ **Signed off 2026-09-10 (8/9)** — 9th is code-verified only; the one-shot-transition limit WP22.1 established makes it unbracketable. Also fixed a real pre-existing a11y bug: the report sheet's scrim was hiding the whole sheet from assistive tech | ✅ **Merged as PR #38** (verified 2026-09-10). It carried the `design/**` eslint fix, so `main`'s lint gate is green again |
| WP26 | Track complete + share card | Manager | ✅ **Signed off 2026-09-10** — verified against the diff: consumes `layoutRoadmap` with `seedFromTrackId` so the constellation matches the roadmap for free, `MascotSlot` preserved, the aspect change deliberately lands on the same `View` `captureRef` photographs, `Icon.tsx` untouched | **PR #39 open as of 2026-09-10** |
| WP26.1 | Track-scoped XP and first-try count on the read path, then three stats on Track complete | Manager | ⬜ **Proposed — needs the founder's go** | Backend + a small mobile follow-up |
| WP27 | **Explore, Library, Journey, Profile** + the shared icon swap — **the last redesign package** | Manager | ✅ **Signed off 2026-09-10** — trophy gone from all three consumers, verified on the captured file rather than the preview. Scope ruling below | ⚠️ **PR #40 is OPEN and not on `main` — observed 2026-09-11.** Verified two ways: `gh pr view 40` reads OPEN, and `origin/main:Icon.tsx:83` still reads `achievement: 'trophy'`, the exact line this package removed. **So four of the fourteen surfaces are not in the mainline app.** This row previously read *"Merged as PR #40"* — a prediction written as an observation, and **the second occurrence of the exact failure the 2026-09-10 ruling below was written to prevent** |
| — | **🎨 THE VISUAL REDESIGN IS COMPLETE — all fourteen surfaces** — **as signed-off work. Ten of the eleven packages are on `main`; WP27 is not** (see its row) | — | ✅ **2026-09-10** | — |
| WP28 | **Diagnose the tap failure and Reanimated's reduce-motion disagreement.** **Opus** | Manager | ✅ **Signed off 2026-09-11** — **there was no bug.** Two causes, both correct behaviour, proven by holding coordinates constant and changing only a prop. 20 taps, 20 first-attempt hits | Merged as PR #42 |
| WP28.1 | Reduce-motion override on the full-motion branches + route `TrackRoadmap` through the constant | Manager | ✅ **Signed off 2026-09-11 (6/7 observed)** — verified against the diff, not the report: all four full-motion branches carry the override at every nesting level, `ReduceMotion` is no longer imported anywhere in `apps/mobile` outside `motion.ts`, and the four surfaces are provably the complete set. **The 7th is argued, not observed** — see the ruling below | **Merged as PR #43** (observed 2026-09-11, `1357fa1` on `origin/main`) |
| WP29 | **The Leaf player's footer hosts the live action** | Manager | ⏭️ **Superseded 2026-09-17 — absorbed into VO-3.** Handed off 2026-09-11 and never started. A voiceover control is a persistent secondary control in the same footer WP29 restructures; done separately the footer is rebuilt twice and the second pass invalidates the first (WP22.2's shape). **Not dropped — its requirements carry into VO-3 whole** | — |
| **WP30** | **Ikigai end to end — and scenario images that match their scenarios.** **Opus** | **Pipeline Manager** | ✅ **Signed off 2026-09-15 (7/9)** — **and one of the two unmet criteria was unmeetable as written; that was Architect's error, not the package's.** Verified independently: `pytest` 226/2, and the variety check run against Track 42 reproduces 0.502 against a 0.60 floor with six near-duplicate pairs, exit 1. The anchor judgement was **tested rather than argued** — four regenerations with anchors untouched produced four different places, so the expensive recut was not needed. Cost overrun self-reported and owned | **Merged as PR #44** (observed 2026-09-15). Both unmet criteria have one cause: no Ikigai images. → **WP30.1** |
| **WP30.1** | **Finish Ikigai's eighteen images inside the credit; rewrite Leaf 17's named-framework breach.** **Opus** | **Pipeline Manager** | ✅ **Signed off 2026-09-15 (8/8)** — **$3.56 against a $5 ceiling, and it stopped with $1.44 unspent rather than buy its way to a passing image.** Verified by looking at the retained contact sheet: eighteen genuinely distinct settings — interiors, exteriors, two macro hand shots, two landscapes with no figures, day and night. **The images were retained this time**, which is what made this review possible at all | **Merged as PR #45.** Four rulings below; two items carried as debt, both named rather than buried |
| **WP31** | **The output-side image guard** — text, glow and floating iconography; Ikigai Leaves 3 and 7; the light-rule rewrite. **Opus** | **Pipeline Manager** | 📤 **Handed off 2026-09-15** | $4 ceiling. **Must land before Ikigai publishes** — the machine account cannot edit published content, so publishing first makes these two images a founder problem |
| **WP31** *(row above superseded)* | — | — | ✅ **Signed off 2026-09-15 (8/8)** — $1.52 against $4. **The guard caught a bloom two humans had cleared by eye, one of them Architect at sign-off.** Ratified as a spending gate, ruling below | Merged as PR #46 |
| **WP32** | **Make the paid-tier constraint a check** — key it off `acquisition`, record the transport. **Opus** | **Pipeline Manager** | 📤 **Handed off 2026-09-15** | **Ruled next-package on 2026-09-15 and then not handed off — that delay is Architect's**, and it is why three consecutive reports raised it |
| **WP32** *(superseded above)* | — | — | ✅ **Signed off 2026-09-15 (8/8)** — **$0.00: the package made no model calls at all.** All three judgement calls ratified. `require_paid_tier` exists and is called from two sites; `FREE_TIER_ACQUISITIONS` is a whitelist of one; `ZOOMOUT_PIPELINE_PAID_TIER` is gone. **Verified in the code, not from the report** | Merged as PR #47 |
| **WP33** | **Regenerate Ikigai Leaf 4's image** — the bloom WP31 found and reported rather than fixed | **Pipeline Manager** | ✅ **Signed off 2026-09-16 (8/8)** — **$0.1455 of $0.50; passed on attempt 1 of 1.** Verified against the diff: **no source file changed**, PR #48 is the completion report alone. The guard's RED verdict was taken on the image **Payload was actually serving**, proven byte-identical to the local candidate by sha256 — **the one Leaf where 'guarded at generation' and 'guarded as attached' are known to be the same thing** | Merged as PR #48 |
| **WP33.1** | **Publish pre-flight (sha256 transfer check) + the credential leak** | **Pipeline Manager** | ✅ **Signed off 2026-09-16 (8/8)** — **$0.00, no model called in either part.** **18 of 18 match**, all `draft`, Track 50's `updatedAt` unchanged. **Auth proven as an identity with a negative control** — `pipeline-bot@zoomout.local` on the correct scheme, `None` on the wrong one — which is the trap the handoff named. **And the handoff's named fix was wrong twice; both were caught before shipping.** See the decision below | Merged as PR #49 |
| — | **🔊 VOICEOVER — plan approved 2026-09-17. Four slides, one voice, Ikigai only, $3 ceiling on credit.** Full plan in `projectplan.md` | — | — | — |
| **VO-1** | **Foundations** — audio mime types on `Media`; `resolveMediaUrl` applied to audio | Manager | ✅ **Signed off 2026-09-17 (7/7)** — verified against the diff: **all five audio call sites** routed through `baseUrl`, `stickyNotes` included, and the mutation contrast reported as asked. `audio/mpeg` only, with the reasoning stated. **The image-only-processing question came back with a real answer** — Payload's own `canResizeImage`/`isImage` checks gate resizing, so audio skips it without a parallel branch. **And it asked three questions rather than assuming, one of which found a hole in our process docs** | Branch `vo-1-audio-path` |
| **VO-2** | **Generation** — 144 clips, two narrators. **Opus** | **Pipeline Manager** | ✅ **Signed off 2026-09-17 at 6/9, by design** — **$1.79 of $3.** The founder chose two narrators mid-package, which made the CMS write impossible against a one-voice schema, so writes were held. **Two unmet criteria move to VO-2.1; the third — listening to the full tracks — was never meetable by an agent that cannot hear, and that was Architect's error in the handoff.** Verified: 183 raw renders cached (not 144 mp3s — encoding happens at upload), clip cache keyed on text, Google's own counts show zero AI Studio calls. **19 of 19 mutations went red.** The report's best finding is the guard's: its transcriber silently dropped spoken instructions in 9 of 13 transcripts, and it now has a model-free pace check behind it | **Uncommitted** in `ZO-pipeline` — commit, PR, merge |
| **VO-1.1** | **Narrator-keyed audio + `textDigest`**, the live DB push, the backend contract test | Manager | ✅ **Signed off 2026-09-18 (12/12)** — **1,391 tests, +24 and nothing dropped**, the count reported and reconciled per the new habit. Database backed up, dropped columns proven empty, baseline re-verified. **The contract test that `manager.md` claimed for weeks now exists**, and goes red on the named regression. **Ran on Sonnet 5, not the suggested Opus** — flagged before starting, founder's call, 12/12; see the decision below | Merged as PR #52 |
| **VO-2.1** | **Attach both narrators as drafts** — first live run of the upload path | **Pipeline Manager** | ✅ **Signed off 2026-09-18 (9/9)** — **144 = 18 Leaves × 4 slides × 2 narrators**, every entry with a URL, a positive `durationSeconds` and a 64-char digest, verified by a script outside the pipeline's own code that re-fetched all 18 Leaves and recomputed every digest. **Only 4 of 144 clips were freshly rendered** — the run cache bought the rest, at **$0.0346** against a $1.21 remainder. Ledger closed at **$1.8230 of the $3.00 ceiling** | Merged |
| **VO-3** | **Playback** — player, narrator choice, audio session. **Absorbs WP29** | Manager | ✅ **Signed off 2026-09-22** — and **the risk the package named before shipping is exactly the one that bit**: its report said the whole thing rested on `expo-audio`'s hooks behaving as their `.d.ts` describes, *"since I have never once seen them run."* On a real phone `useAudioPlayer` releases its native player on unmount by itself, undocumented, so the package's own cleanup called `pause()` on a released object. **Fixed same day and mutation-checked against the exact failure** — `releaseFakePlayer()` reproduces the native error, and stripping `safely()` from any one of three call sites fails the new test. **Verified at sign-off by running the suite, not reading the report: 42 suites / 691 tests.** Two debt rows below | **Merged 2026-09-22 (`060a286`)** |
| **PILOT-1** | **Only real content, and assets that reach a phone** — `MEDIA_BASE_URL`, `HIDE_PLACEHOLDER_CONTENT`, and a play failure the reader can see | Manager | ✅ **Signed off 2026-09-22 (12/12 code-verified)** — **527 backend / 695 mobile tests** (+33 / +4), independently confirmed against the real CI logs, not the report. Both traps named in the handoff actually closed: the service guard holds with the repository filter disabled, and grading (`progress.service.ts`) refuses a placeholder Leaf on its own code path — plus a third call site (`finishTrackIfDone`) the handoff didn't name but Manager found and gated anyway. `HIDE_PLACEHOLDER_CONTENT=false` genuinely means false. Live-verified against real Payload + Postgres, not mocks | **Merged as PR #57.** **Device gate entirely unobserved by anyone — needs the founder on Android/Expo Go**: Explore shows exactly 2 real Tracks, a Leaf plays end to end (narration audible, images visible), and a `MEDIA_BASE_URL` pointed at nothing produces a visible failure rather than a dead button. One test-quality gap logged as debt below (cache-key test doesn't prove independence; the shipped code is correct). **Both covers stay out of scope here** — see COVER-1 |
| **COVER-1** | **Two covers that are ours** — replace both hotlinked covers with guarded, textless art in the house style | **Pipeline Manager** | ✅ **Signed off 2026-09-22 (9/9)** — **$0.275 of $0.50**, both images 848×1264 (2:3, dimensions read back from the file, not trusted from the request), guard-clean, neither Track's `updatedAt` touched. Verified independently against the diff and the actual files on disk, not the report — one moderate finding logged as debt below, not a blocker | **Merged as PR #56. Uploaded by the founder 2026-09-23** — both Tracks serve our own art, verified against `zoomout_cms.tracks` 2026-09-24. **Pinned to an absolute LAN address** (`http://192.168.1.5:3001/api/media/file/…`) because the admin's publish rule rejects a relative path (debt row); any network change breaks both until edited again. **Ruled 2026-09-23: one candidate per book is enough** — no COVER-1.1. **Note on the "CI green" this row once carried:** no CI job runs Python (debt row), so it meant the JS workspaces only; this package's 99 files / 441 tests were corroborated later, when ONBOARD-2's reviewer ran the gate (102 = 99 + 3, 495 = 441 + 54) |
| **ONBOARD-1** | **The five-beat activation flow** — promise, pick a book, choose narrator, into Leaf 1, a coach-mark on the unlock gate | Manager | ✅ **Signed off 2026-09-23 (9/11 code-verified)** — **735 tests (+40)**, independently confirmed against real CI logs. Live-verified: beat 2's pagination risk is real today (Ikigai alone on page 1 of the live catalogue, the second real Track on page 2), `nextLeafId` sourcing matches `LibraryScreen`'s exactly, both narrators' audio confirmed present on Ikigai's Leaf 1. Two genuine, low-risk gaps found by `code-reviewer`, **neither flagged in the completion report**: beat 2's skip is untested (identical handler to beat 1's tested one), and `AppStack`'s new onboarding transitions never joined the Reduce Motion mechanical guard the handoff explicitly named. Both narrow, mechanical fixes — logged as debt below, not blocking sign-off. Three architectural divergences from the handoff (beat 4 as an action not a screen; three screens registered on `AppStack` via `initialRouteName`, mirroring `AuthStack`; a four-state gate that fails open to `seen` on a Library-fetch error) all independently confirmed real and sound | **Merged as PR #58. Device gate observed 2026-09-23/24, after a long infrastructure detour** (stale `node_modules`, a duplicated env line, `MEDIA_BASE_URL`/`HIDE_PLACEHOLDER_CONTENT` never set for the session — all resolved, none were app bugs). **Five findings came back — four real product gaps, now ONBOARD-2/ONBOARD-3 below**, one (`Leaf narration audible`) a confirmation. Achievement-banner-suppression follow-up resolved without a package: `AchievementStatus.unlockedAt` is a nullable server timestamp, so Profile's grid shows `first-book` regardless of the suppressed banner |
| **ONBOARD-2** | **Two narrator self-introduction clips** — Lara and Druv, replacing the book-reading sample so the narrator beat can move ahead of book-picking | **Pipeline Manager** | ✅ **Signed off 2026-09-24 (6/6)** — **deliverable verified by me, not the report:** Payload holds exactly two greeting documents (**Media 350/351**, no `-1` rename, old pair gone), served bytes **sha256-identical** to the files the founder approved by ear (pace right; "Druv" has its *v*), 5.04 s / 5.11 s. **`code-reviewer` ran it against the diff: the legal fence is intact** — `synthesize` diffed against its pre-merge self across nine scenarios and identical, no pre-existing test modified, 28 probes of which 21 were caught; the twin render is faithful line for line today. Spend **$0.0378** (female $0.0182, male $0.0196 — from the ledger; the report gave no per-clip split). **The gate was verified by running it** (mypy 102 files, pytest 495), **because no CI job runs Python.** **The handoff was wrong three ways, all mine** — it scripted the provider's voice ids as the narrators' names, said "reuse VO-2's TTS call, no new logic" (false by design: that call is a legal fence), and omitted `LEGAL.md`'s narration boundary — **and a fourth I only saw afterwards: this was the queued trigger package for the `narrate --max-attempts` ruling and the handoff never carried it.** The package handled the first three correctly, which is why it grew to 2,481 lines | **Merged as PR #59 (`7369536`).** Pipeline Manager's follow-up entry (new ids, ear result) is **merged as PR #60 (`2f52e47`, observed 2026-09-25)** — the record now matches the deliverable. Findings → debt rows below, and a proposed **ONBOARD-2.1** (fence hardening, upload pre-flight, the queued `narrate` default). **Unblocks ONBOARD-3's narrator-beat device gate** |
| **ONBOARD-3** | **Pre-intro, intro repositioning, beat reorder, the closing screen** — the four real gaps from ONBOARD-1's device gate | Manager | ✅ **Signed off 2026-09-25 (17/17 code-verified)** — **770 mobile / 533 backend / 80 shared / 204 admin, every count read from the real CI logs, none dropped** (mobile +35, reconciled). **Verified against the diff, not the report:** `code-reviewer` read the assertion behind every guard Manager's 41 mutation checks name and found each goes red for its named breakage; I read the gate, `AppStack`/`RootNavigator`, the player and `WrapUp` diffs, both rewritten screens and the endpoint. **Closes ONBOARD-1's two test-gap rows.** **Found beyond the criteria, none blocking:** the narrator beat has no way past a failed fetch (and the new endpoint adds a deploy-order trap), it overwrites a stored narrator on a repeat pass, pick-book has choose/skip races, and two drafted copy lines are factually wrong → **ONBOARD-3.1**. **Some of that traces to the handoff, not the package:** its state table had no failure rows and did not say what a repeat pass starts from | **Merged as PR #61 (`b32191a`, observed 2026-09-25).** **Device gate unobserved by anyone:** Lara and Druv audible, distinct, each saying its own name; Android and hardware back; one-voice-at-a-time at real tap speed; replay of a finished clip; the four drafted strings. The founder's `:3000` must be restarted from `main` after the merge |
| **ONBOARD-2.1** | **Pin the legal fence and the spending cap, make "both or neither" true, `narrate` at three attempts** — the four items ONBOARD-2's review left | **Pipeline Manager** | ✅ **Signed off 2026-09-25 (14/14)** — **$0, no model calls.** **Verified by me, not the report:** the gate run by hand from `ZO-pipeline` (ruff format 125, mypy 107 clean, **pytest 573 passed**, +78 — every count up; `runs/greetings/` hashes identically before and after my run); **Media 350/351 re-hashed live off Payload**, bytes and sha256 equal ONBOARD-2's; the ledger hash unchanged; and **an independent 18-mutant sample on a scratch copy — all red, each killed by the test for its own copy, and the old fence pins stayed green on the same bypasses.** **The handoff's procedure paid for itself: the register said three unpinned money lines, and deleting them showed five per copy** — the timeout and unreadable-audio settles were the two it missed; the ledger was tested and the ceiling was not. The greeting door now refuses off-list text at runtime (`SpeechFenceError`, which never repeats the words). Not run: `code-reviewer` — the evidence was mutation, not reading, and I re-ran a sample of it | **Merged as PR #62 (`6c5d8fd`, observed 2026-09-25).** Residuals named and not fixed (register). **The cost-ledger lock is still open and now slightly more urgent** (three attempts) — a precondition of book #3's narration |
| WP22.3 | Roadmap vertical rhythm derived from label height | Manager | ✅ **Signed off 2026-09-10** — spacing now floors at a worst-case two-line label at the reader's real text scale; also fixed a pre-existing memo bug that would have made the whole feature inert | Merged as PR #41. **Unverified on Track 42's long titles** |
| — | *WP12 deployment · WP13 password reset · WP14 test hardening* | Manager | ⏸ **parked** | see `launch-blockers.md` |

### Blocked on the founder

> **The live list moved to `launch-blockers.md` Part 3 on 2026-08-27.** Keep it there and nowhere else. Two lists drifted apart — this one still said "GCP account, do this now" after the credit was already being spent, and still carried a device gate closed in WP5b. A stale blocker list is worse than none, because it costs the reader trust in every row including the true ones.

<details><summary>Phase 1 archive — closed items, kept for the record</summary>

| Item | Cost | Blocks | Status |
|---|---|---|---|
| **Confirm how Phase 1 content gets drafted** — ad-hoc AI assistance vs pulling the Phase 2 pipeline forward | minutes | The Phase 1/Phase 2 boundary, and the Critic-in-the-Loop review standard | ⬜ |
| **Fix the gate content in the admin UI** — add `publisher` + `coverUrl` to the Track, add a locator to the Leaf's source reference, re-save both | ~2 min | **WP3** — the mapper rejects both records as they stand | ⬜ **do this now** |
| **GATE: device verification session** — its own slot, not the tail of a package | ~10 min | Closes deferrals from **three** packages | ⬜ **after WP5b**. Slipped twice because it was always the tail of something larger; given its own slot for that reason |
| ↳ 1. Restart the backend | 1 cmd | Closes WP5b Part A's last criterion — the running process still points at the old database |  |
| ↳ 2. Play a Leaf to the cap | | Does "That is today done" read as an ending or a refusal? |  |
| ↳ 3. Unlock an achievement | | Does the moment land? |  |
| ↳ 4. iOS Reduce Motion on, replay the unlock | | Fade rather than spring — **WP8's open 11th criterion** |  |
| ↳ 5. Both themes at XXXL on the new screens | | Carries the three XXXL items from WP11 |  |
| **Narrow `Read(**/.env.*)` in `.claude/settings.json`** | ~1 min | Three incidents so far; see the debt register | ⬜ recommended |

</details>

### When the founder can try it

Four distinct moments. Sequence, not dates — there is no velocity data and no target launch date yet.

| Moment | After | What you can actually do | Still missing |
|---|---|---|---|
| **Author content** | WP1 | Log into the CMS and build a Leaf. Real software you use, and it *is* the schema-freeze gate | Everything user-facing |
| **Exercise the loop headless** | WP4 | Hit the API from the terminal: fetch a Leaf, submit a wrong answer, submit the right one, watch the payoff unlock and XP land. Validates the core mechanic before any UI exists | All UI |
| **Hold the app** | WP6 | Install on your own phone, sign up, pass the age gate, navigate the four surfaces | Content, the Leaf player — the surfaces are empty shells |
| **Test the product** | WP8 | The real thing: read a summary, answer a scenario, unlock the payoff, complete a Leaf, feel whether the thesis works | Session cap, streaks, share screens (WP5, WP9) |

**WP8 is the answer to "when can I judge whether this works."** Chain to reach it: WP1 → GATE → WP2 → WP3 → WP4 → WP6 → WP11 → WP8. WP6 is the pinch point, and it is blocked on founder items (design direction, Xcode), not on Manager.

### Where to look for what
- **This board** — current state, at a glance
- `projectplan.md` — the one work package in flight, in detail
- `collaboration-log.md` — every handoff and completion report, newest first
- `proposals/phase-1-implementation-plan.md` — the full milestone plan and its reasoning
- **`launch-blockers.md`** — everything between a working app and a shippable one. Parked, deliberately

## Vision
ZoomOut turns non-fiction books into gamified, interactive micro-lessons that build lasting retention — reclaiming screen time from algorithmic feeds with focused, ~15-minute sessions that require active engagement instead of passive consumption.

## Milestones
| Milestone | Target | Status |
|---|---|---|
| Phase 1 — core learning loop, gamification, all app surfaces, manual content admin tool | MVP / promotional launch | **In progress** — plan approved 2026-08-06, see `proposals/phase-1-implementation-plan.md` |
| Phase 2 — AI content pipeline (Gemini/Vertex AI, ElevenLabs) | Post-MVP | Not started |
| Phase 3 — social ("Reading Circles") | Post-MVP | Not started |
| Phase 4 — monetization | Last | Not started — deliberately deferred to the final phase |

## Backlog
| Feature | Priority | Status | Notes |
|---|---|---|---|
| **INTRO-1 — the first-run intro, a zoom out through a neuron network** | P1 | ✅ **Done — merged (PR #54), 8/8 criteria, device gate observed 2026-09-18** | Code signed off on the tests (657 mobile, up from 616, nothing dropped); **the gate was then observed by the founder on a real phone via Expo Go.** **That settles the three questions no test could answer** — one continuous camera move, the graph's shape, text legibility over beat 1 — **so `INTRO_SEED = 8_675_309` and `INTRO_FOCUS_SCALE = 6` are settled values, not placeholders.** Residual: a live theme switch and largest-OS-text were verified by test, not by eye |
| **Onboarding flow — five beats, ending inside the first Leaf** | P1 | ✅ **Shipped as ONBOARD-1 — see the status board, not this row** | Moved to the Work packages table 2026-09-23 to stop tracking the same package in two places. Full design and the five rulings: `projectplan.md:557` |
| WP0 — Monorepo scaffolding (npm workspaces, TS strict, shared types, Postgres + migrations, CI) | P0 | ✅ **Done** — signed off 2026-08-06, PR #1 | 9/10 criteria verified by execution, CI green. 10th carried forward, see below |
| Verify Expo app boots in the iOS simulator | P0 — gates WP6 | 🔵 Appended to WP2.1 (2026-08-08) | Xcode now installed. Was the last open WP0 criterion |
| WP1 — Content admin tool: Payload 3.x CMS setup | P0 | **Handed off** 2026-08-06 | Blocks all content — see `PRODUCT.md` Phased scope. Approval workflow deferred; Payload makes it a config change later |
| **GATE — schema freeze** | P0 — **blocks everything downstream of WP1** | Not started | ~45 min of **founder** time: author one structurally complete Leaf through the real Payload editor, placeholder prose, no research. Nothing downstream starts until the schema is signed off. See plan §5 |
| User auth & profile management | P0 | Not started | |
| Core learning loop (5-slide Leaf UI, unlock-on-correct-answer logic) | P0 | Not started | |
| Gamification (streaks, XP, badges, SFX) | P0 | Not started | |
| Session cap / positive friction (15 min or 500 XP) | P0 | Not started | Intentional constraint, not a bug |
| Explore / Library / Journey screens | P0 | Not started | |
| Shareable session-wrap-up & achievement screens | P1 | Not started | Growth-loop mechanic |
| "Report an error" flow + fix queue | P1 | Not started | Legal requirement — see `PRODUCT.md` Content integrity |
| Purchase-forward / affiliate links | P1 | Not started | |
| Age-gating / minor protection | P0, pre-launch blocker | Not started | Threshold owner: TBD (legal) |
| AI content pipeline (book → Leaves via Gemini/Vertex) | Phase 2 | Not started | Source of launch content, but built **after all app and admin work completes** (decided 2026-08-08). Needs its own brainstorming cycle. Separate Python service. **No Architect proposal until Phase 1 app work is done** |
| Social / Reading Circles | Phase 3 | Not started | |
| Monetization / subscription | Phase 4 | Not started | Deliberately last. Model still undecided — see Open items in `PRODUCT.md` |
| Launch library authoring — "The Mountain Is You" (~20 Leaves) | P0, pre-launch gate | Not started | Deferred off the build critical path 2026-08-06; app is built against placeholder content. ~10–15 hours of solo writing still required before launch |
| Replace all placeholder content | P0, pre-launch blocker | Not started | `isPlaceholder` records must never reach production — see proposal §3.4 |

## Decisions log

> **Split 2026-09-22.** Older feature decisions, and the thirteen rulings that had become **rules**, are in
> `project/archive/decisions-through-2026-09-18.md`. **A rule does not expire, so a dated table that prunes
> by date could only bury it** — each of those thirteen now lives in the persona file that is loaded at
> session start, and the archive's header says which. **Do not archive a ruling from here until its rule has
> a home.**
>
> Pre-2026-08-25 decisions remain in `archive/decisions-through-2026-08-24.md` and
> `archive/decisions-and-debt-through-2026-08-29.md`.

| Date | Decision | Why | Alternatives rejected |
|---|---|---|---|
| 2026-09-25 | **ONBOARD-2.1 signed off 14/14 — and the pattern that found five money lines where the register said three: re-derive a register's count by deleting, not by reading** | The register's "three unpinned lines" came from reading the code; the table came from deleting each `reserve`/`settle` line — **five per copy, the ledger tested and the ceiling not.** The handoff had said *re-derive the set by mutation rather than trusting this list*, and that sentence is what surfaced the two the register missed. **Ratified as good judgement, all inside `apps/pipeline`:** the door refusal checks both `text` and `spoken` and never echoes the words; the `_call` pin flags a *reference*, not only a call; the pre-flight names every stale document; `generate-greetings --help` had the same overclaim and was fixed (a sibling); one docstring word in `narration_guard.py` that contradicted the ruling; the read-only rehearsal script kept in `tests/`. **Verified independently before sign-off:** the gate by hand, Media 350/351 re-hashed live, the ledger hash, and 18 mutants on a scratch copy — all red | Run `code-reviewer` — not this time: the evidence was mutation, not reading, and I re-ran a sample of it. Keep the rehearsal in scratch — it would not outlive the session that wrote it |
| 2026-09-25 | **ONBOARD-2.1 is a go — the legal fence pinned, the three money lines pinned, an upload pre-flight, `narrate` at three attempts. $0, no model calls, Sonnet. The ledger lock stays out; greetings keep two attempts on their own constant** | Every item is a register row with a fix, and each was **re-read against the code before it was handed off** — which corrected the row for the attempts default (stale line numbers, a README claim that was false, and a coupling it missed). **The three money lines are the ceiling itself:** `reserve` is a stateless check and `settle` the only thing that moves `spent`. **The lock is out because it needs a design** (lock file vs `flock`, stale locks) — judgement, not procedure — and becomes a precondition of book #3's narration package. **Greetings stay at two** because an ear-driven job on a small cap should not gain a paid attempt by inheritance. **Sonnet** because the danger is a fence test that passes for the wrong reason, and the handoff writes the procedure that catches that (red against a scratch bypass first). Sequencing: alongside the device gate; **the archive pass waits until it signs off** | Fold it into book #3's narration package — ships a hardening and a spend together, which is how the money lines went out unpinned. Split it in two — same worktree, files and reviewer. A type-level fence (a sealed type only the two doors can build) — stronger, but touches both signatures and the twin; worth doing if the boundary should be structural, and not needed for the two named blind spots. Opus — the procedure carries the risk |
| 2026-09-25 | **ONBOARD-3 signed off 17/17. Four rulings on Manager's questions, and a hardening package proposed for what review found** | **(1) Explore's two "fifteen minutes" lines are a follow-up, not this PR:** the handoff said nothing else changes for anyone else, nothing pins them, and the words are the founder's. **(2) The extra `markSeen()` row is confirmed:** the table's own principle is that the flag stays unset only when a later step in the same flow will set it, and here none will; the alternative replays a beat this reader has already had. **The handoff's table listed the paths a reader chooses and missed the one where a request fails — Architect's omission**, and the same omission is behind the narrator beat's no-exit failure and its stored-preference overwrite (register). **(3) One-voice-at-a-time stays,** its limits logged. **(4) `NARRATOR_LABELS` beside `NARRATOR_IDS` in the frozen `content.ts` is not a thaw:** nothing validates or persists it (a saved choice keys on the id). **Rule: a presentation constant may sit beside its ids; anything validated or persisted needs a thaw ruling.** The two delivery types belong in `delivery.ts` and CLAUDE.md's single-definition rule requires them — the handoff's "(one constant)" contradicted its own invitation to return a narrower type | Fold the Explore copy into #61 — delays the gate for words not yet chosen. Overrule the extra row — costs a repeated narrator beat for nothing. Revert one-voice — two hellos overlapping is not an introduction. Move the labels to a new file — defensible, but the instruction was to sit them beside the ids and nothing is harmed |
| 2026-09-24 | **The narrators are called Lara (female) and Druv (male); the provider's voice ids — Achernar, Sadaltager — are never spoken or shown to a reader** | Founder's ruling, made in the ONBOARD-2 session after hearing the first greetings say "I'm Achernar" and "I'm Sadaltager" — **which the handoff had scripted.** The codebase already said this in two places (`packages/shared/src/content.ts`, `NarrationControl.tsx`): `narrator` is ZoomOut's vocabulary and `voice` is the provider's, kept apart so a narrator can be re-cast without breaking a reader's saved choice. The handoff did not read either. Persona name and voice id are now separate in the pipeline too (`NARRATOR_NAMES` beside `NARRATOR_VOICES`). "Druv" is spelled as ruled, not "Dhruv" | **Speaking the voice ids** — what shipped first, and a provider's internal label is not a name for a person. **A live spoken reader name** — ruled out the same day (a new per-signup TTS capability). **Leaving the app's labels at "Female"/"Male"** — the clip says "Lara", so the card must |
| 2026-09-18 | **The visual gate runs on Expo Go; the Xcode wall is not fixed by pinning an older Xcode** | A native rebuild fails on this Mac — `expo-modules-jsi` will not compile under Xcode 26.3 / Swift 6, and there is exactly one Xcode installed. **Expo Go clears it today and costs nothing:** every plugin in `app.json` (`expo-font`, `expo-secure-store`, `expo-web-browser`, `expo-asset`, `expo-sharing`) is in the Expo Go runtime, as are `react-native-svg` and `react-native-reanimated`, and **the intro needs no backend** — so neither the API URL nor the port-3000 squatter matters. **It is better than the specified gate**, putting the intro on real hardware. Escalation order after that: `npx expo prebuild --clean` (`ios/` is generated, not committed, verified), then an Expo patch bump or EAS Build | **Pinning an older Xcode** — a machine-level change affecting everything else on the Mac, and it fights the platform: **App Store submission requires a current Xcode regardless**, so it converts a problem due today into a larger one due at Stage 5. **Adding `react-native-web`** — Manager declined to add a dependency to work around a gate gap, correctly |
| 2026-09-18 | **The default narrator is male (Sadaltager)** | Founder's ruling, **made after listening to both 29:11 tracks in full** — which is what the plan reserved this question for. It applies to every reader until the onboarding flow exists, and to every reader who skips it afterwards. **VO-3 owns the stored preference, the default and the Profile control; onboarding writes the same key.** Recorded because a default that is never written down becomes a constant nobody can explain | **Female (Achernar)** — the other audition survivor, rejected by ear, not by argument. **Deferring the default until onboarding** — VO-3 ships first and every reader needs a voice before then |
| 2026-09-18 | **`narrate --max-attempts` defaults to 3, and `MAX_NARRATION_ATTEMPTS` moves with it** — ⚠️ **ruled 2026-09-18, still unimplemented; re-affirmed by the founder 2026-09-22** | **The CLI default of 2 contradicts the README's own documented recipe, which already passes 3** — and Ikigai rediscovered it the way traps get rediscovered, by a held Leaf mid-run. **The cost argument is settled by the option's own help text**: *"attempts already on disk are reused, so raising this re-buys only clips that still fail"* — so the default applies to the few clips that failed twice, not to the corpus. At VO-2.1's measured **$0.0087/clip**, five difficult lines in a new book cost about four cents. **The asymmetry decides it:** too few costs a held Leaf found mid-run plus founder attention plus a re-run; too many costs pennies. Accepted consequence — the default equals the maximum, so the flag becomes "lower this to economise", **which is the better shape: the safe value is the default** **Correction, 2026-09-22:** this row recorded the ruling as settled fact and `origin/main` said otherwise — `cli.py:1666` and `narration_nodes.py:114` both read `2`, while `README.md:274` already documents a recipe passing `3`. Found by the first sweep of the register, not by anyone reading this row. **The founder re-affirmed the ruling on 2026-09-22: the code moves, not the decision.** It rides the pipeline package *after* COVER-1, which was already in flight when the call was made. | **Fixing only the README** — the founder's other option. It leaves a CLI whose default disagrees with its documentation, which is the trap itself rather than a warning about it. **Leaving it at 2** — defensible only if a third attempt were usually wasted, and Leaf 12 cleared on exactly that attempt |
| 2026-09-18 | **The first-run intro is a composition over the app's own neuron geometry, not a rendered video** | Founder approved a four-beat poetic cold open. **The build decision is the load-bearing one.** A video bakes one theme, cannot scale its text with the OS setting, is unreadable to VoiceOver and **cannot honour Reduce Motion at all** — three regressions on the one surface every single user sees, in an app that has held six packages to that standard and carries a live large-text clipping debt. A composition gets all three for free by reading `theme.palette` and `typography`. **And most of the visual already exists**: `roadmapGeometry.ts` emits a seeded neuron graph with dendritic arbors and Bézier connections, `constellationLayers.ts` paints and batches it, and `TrackRoadmap.tsx` already animates SVG through a Reanimated wrapper — so the zoom is a camera move over art-directed geometry. **The intro is therefore a preview of the Track roadmap**, which is the screen readers live in | **A rendered video** (After Effects or AI-generated) — more beautiful in principle, and the honest counter-argument, but it loses theme, text scaling, VoiceOver and Reduce Motion, and adds a multi-MB asset. **Lottie** — a new native dependency, with colours baked at author time and text as its known weak spot |
| 2026-09-18 | **Onboarding is an activation flow, not a preference wizard — five beats ending inside the reader's first Leaf** | Founder-approved. **The differentiator is the active-recall gate, which is a feature bullet when explained and the product when experienced** — so the flow ends in the Leaf player, not on Explore, where `TabShell` drops a new reader onto a catalogue today. **The library being two real Tracks is what rules out the conventional design**: interest tags and "personalising your feed" cannot be honoured at that size, and the hollowness lands on the next screen. Three consequences ruled with it — the narrator preference is **device-local**, matching VO-3 and accepting that a reinstall re-runs onboarding; onboarding is **skippable but skip must land on a designed Explore first-run state**; and **existing installs see the narrator beat only** | **Onboarding before signup** — `PRODUCT.md` rules out guest mode, so a pre-auth taste Leaf needs client-side grading, a second implementation of the thing the product guarantees is server-decided. **An interests questionnaire** and a **"minutes per day" goal setter** — the second also undercuts the positive-friction cap, whose point is that the app decides. **A feature-tour carousel** — beat 4 does the job better |
| 2026-09-18 | **The notifications permission is not asked in onboarding** | **iOS grants exactly one prompt, ever, and push notifications are Stage 4 — they do not exist.** Asking before there is anything to send spends a one-shot asset for nothing, and a reader who declines is gone permanently rather than re-askable. **The moment is after the first streak day**, when the value is concrete. Recorded as its own ruling because asking during onboarding is what most apps do, so it will be proposed again | **Asking in onboarding** — the conventional pattern, and the one that burns the prompt on a feature that cannot yet deliver anything |
| 2026-09-18 | **The reader's narrator is chosen in an onboarding flow — a separate feature, after voiceover** | Founder's call. **It does not close VO-3's open question, and that is the thing to notice:** VO-3 ships before onboarding exists, so it still needs a default narrator for every reader who has not been onboarded. **What it does settle is the mechanism** — the preference is a stored value with a default, written by onboarding later, rather than VO-3 asking the reader itself. **So VO-3's design is unchanged and only the default value is still open** | **VO-3 asking on first play** — a prompt in front of the first listen puts friction exactly where the feature should feel best, and would become a second way of setting the same preference once onboarding lands |
| 2026-09-18 | **Python and Node agree on `textDigest` — verified, 9 of 9 vectors, including precomposed vs decomposed accents** | The staleness check only works if the pipeline and the backend hash identically, and a disagreement would have **suppressed every clip after publication** — a total, silent failure discovered only when nothing played. Checked directly rather than left as VO-2.1's risk: `hashlib.sha256(t.encode('utf-8'))` and `createHash('sha256').update(t,'utf8')` match on ASCII, em dashes, curly quotes, padded whitespace, real Leaf bodies, **and on both Unicode forms of "Héctor García" — which correctly differ from each other**, proving neither side normalises | **Leaving it to VO-2.1 to discover.** The failure is silent and total, and the check cost one command |
| 2026-09-17 | **Two narrators, and the reader chooses — a female and a male voice** | Ruled by the founder **by ear** after a six-voice audition in VO-2, replacing "one voice is enough" from earlier the same day. **It turned a data fill into a content-model change**, which reordered the packages | Shortlist and exclusions are in VO-2's report — upbeat and breathy voices rejected for fatigue over 30 minutes, firm/informative ones for newsreader risk |
| 2026-09-17 | **Slide audio is an array keyed by a closed narrator enum; each entry carries `textDigest`; the backend drops stale entries; the default narrator lives in app config, never in array order** | Pipeline Manager's option (a), plus three things. **Array order must carry no meaning**, or regeneration order decides which voice a reader with no preference hears — and it can differ Leaf to Leaf. **All narrators or none per Leaf**, extending VO-2's own *"three clips and a silent fourth"* rule across voices. **`textDigest` because audio is the first *derived* content in the model**: nothing reached the CMS about the text a clip was made from, so any later text edit would leave narration that no longer matches the screen, undetectably. Stale audio now fails closed | **Default + `audioAlternates`** — encodes a product default into the data shape. **`audioFemale`/`audioMale`** — encodes today's decision into the schema. **Google voice names as keys** — breaks every saved preference on a re-cast. **Playing stale audio** — narration of words not on the screen, in a product whose posture is accuracy |
| 2026-09-17 | **For Gemini-TTS, any direction text can be spoken — keep it short, descriptive and without conversational framing; do not use emotion tags** | v1 opened *"Read the text exactly as written:"* and was read aloud in 12 of 13 clips (~$0.32 wasted, founder told and re-approved). v2's *"as if telling a friend"* made the model ad-lib *"You know,"*. **v3 still leaks from imperative per-slide sentences in 7 of 144 first attempts**, all caught. **Google's own guide says adjective tags such as `[curious]` are spoken aloud** — which makes the VO-2 handoff's `[curiosity]` and `[hope]` examples another case of a handoff naming something that was not true, and Pipeline Manager was right not to use them. Same family as *a model-facing prompt that explains a prohibition reintroduces it* | **Emotion tags** — spoken. **Longer, more explicit direction** — every added instruction is more text that can be read out |
| 2026-09-17 | **Voiceover uses Gemini 2.5 Flash TTS on Google Cloud, not ElevenLabs as PRODUCT.md named since 2026-08-06** | **The wall is free-tier commercial licensing, not cost.** ElevenLabs, Hume, Cartesia and Azure all forbid commercial use on their free tiers — ElevenLabs also requires attribution in the title. **Expressive + free + commercially usable is offered by no hosted vendor.** Google Cloud reconciles all three, and **it is not the product WP32's check refuses**: that gates the Gemini Developer API (AI Studio), which states on its own pricing page that it trains on submitted content, while Cloud TTS and Vertex are Customer Data under the GCP DPA's training restriction — and are what the credit pays for. ~$0.35 for the book | **ElevenLabs' free tier** — a licence violation for a commercial product, not a saving. **Chirp 3 HD at $0** — clean licence, but no emotion steering at all, and emotion is the requirement. **Chatterbox locally** — MIT and genuinely expressive, kept as the fallback if credit is ever withdrawn; costs Mac time and its Apple Silicon support is reportedly inconsistent |
| 2026-09-17 | **Voiceover plays on the iOS `playback` category; SFX keeps `ambient`** | A reader who taps *read this to me* has asked for sound. **Being silently muted by the hardware switch reads as broken**, while SFX muted by it reads as correct. The app therefore switches category by use | **One category for both.** `ambient` makes the feature look dead on a silenced phone; `playback` makes SFX blare through a switch the reader deliberately set |
| 2026-09-10 | **Track-scoped XP and first-try count get added to the read path, as WP26.1 — and reading the code changed the shape I would have chosen** | WP26 shipped the Track-complete screen with **one stat, day streak**, because nothing the client receives carries the other two. **Verified rather than taken on report:** `summariseTrackProgress` is a pure function whose third argument is `completedLeafIds: ReadonlySet<string>` — **identifiers only**, no XP and no first-try flag — while `progress.repository.ts` already selects `xpAwarded` and `firstTryCorrect` per row. So the data exists and the rollup simply never sees it: a read-path addition, not new tracking, exactly as Manager described. **My instinct was a dedicated endpoint**, on the principle that the common path should not pay for the rare one — Track complete is seen once per book, while `TrackProgressSummary` is fetched on every Library and Journey render. **Reading the code overturned that.** It is not a new query, it is two more columns on a query already running, summed inside a pure function that is already there. **The simple answer wins and the endpoint would have been over-engineering.** One piece of guidance for whoever builds it: **make the two fields required, not optional**, so the compiler forces the backend to supply them rather than letting the client read `undefined` and render a zero that looks like a real score | A dedicated endpoint (my first instinct; the cost turned out to be two columns, not a query); leaving the screen at one stat permanently (the biggest reward moment in the product showing a single number, against a spec that asked for three) |


## Technical debt register

> **Swept 2026-09-22 — first full pass, and 16 of 157 rows were not true.** Among them *"the backend is not
> running at all"* (it answers `{"status":"ok"}`) and *"Ikigai's eighteen Leaves are still drafts"* (18 of 18
> published). **Two would have been cited in a handoff as live constraints.** It drifts rather than duplicates.
>
> **Rows not marked otherwise were not individually verified.** Open as far as anyone knows, which is weaker
> than open — **verify against the live system before citing one in a handoff.**
>
> ✅ rows archive to `archive/debt-resolved-through-2026-09-15.md` (holds resolutions through 2026-09-22).
> **Founder decisions belong in `launch-blockers.md` Part 3 and nowhere else**; six were moved there 2026-09-22.

| Item | Impact | Flagged by | Status |
|---|---|---|---|
| **No CI job runs the Python pipeline — `ruff`, `mypy --strict` and `pytest` (495 tests) run on no pull request** | `.github/workflows/ci.yml` has one job, `npm ci → lint → typecheck → test → build`; the pipeline has no `package.json`, and no workflow anywhere mentions python, pytest, mypy or ruff (**verified 2026-09-24**). **So "CI green" on a pipeline PR has only ever meant the JS workspaces**, and every pipeline sign-off has rested on the report's own counts. It surfaced only because ONBOARD-2's reviewer went to read the CI log for 495 / 102 and found none, and ran the gate by hand instead — which also corroborated COVER-1's 99 / 441 indirectly (102 = 99 + 3, 495 = 441 + 54). The `ZO` checkout's `apps/pipeline/.venv` is stale (mypy shows 20 spurious errors), so the gate cannot be run from `ZO` either. **Fix:** a Python job; it needs a Postgres service, since `pytest` uses a `zoomout_pipeline_test` database | `code-reviewer`, ONBOARD-2 sign-off, 2026-09-24; verified by Architect | Open — small and mechanical; do it before another pipeline sign-off leans on "CI green" |
| **`NarrationBudget` reserves a worst case that is blind to the request, so a small cap allows almost no iteration** | It reserves Cloud TTS's 655-second maximum — $0.1638 — before every call and refuses when spent plus that would cross the cap, so a $0.20 cap leaves about $0.036 of real spend, roughly five takes. Right for a 144-clip run; wrong for ear-driven work on a two-sentence clip, where a take costs about $0.0035 per narrator. It halted ONBOARD-2 at $0.0378. **Architect's recommendation (2026-09-24): fix the shape, not the number** — size the worst case to the request (characters × price, with a margin) rather than raise the default, since a higher default raises real exposure for every job, including the ones where the big reservation is correct. **The number a founder authorises is a cap on a reservation, not on spend:** real spend cannot pass the cap minus $0.1638 | Pipeline Manager, ONBOARD-2 | Open, not urgent. If a re-take is ever wanted: `--ceiling-usd 0.30`, real spend ≤ about $0.14 all-in — the founder's authorisation |
| ℹ️ **Redoing any Media costs the founder a manual delete** | `PayloadClient` can create Media but never replace or delete it (`machinesNeverDelete`, WP15.3 — deliberate). The greeting command refuses to upload over an existing document with different bytes and names its id, so a redo is: founder listens → founder deletes → one free command uploads from cache. Recorded so nobody "fixes" it by widening the machine key | Pipeline Manager, ONBOARD-2 | ℹ️ A designed property with a cost, not debt |
| **Narrator names are defined in two places across two languages** | Python `assets/greeting.py:NARRATOR_NAMES` (what is *spoken*) and `packages/shared`'s `NARRATOR_LABELS` (what is *shown*) — **nothing checks they agree.** ONBOARD-3 collapsed the three mobile "Female"/"Male" maps into the shared one (one definition, three consumers, verified by grep). The 144 Leaf clips' Media `alt` also still says "read by Achernar / Sadaltager" (admin-only, cosmetic) | Architect, 2026-09-24 | ✅ Mobile half closed by ONBOARD-3 (2026-09-25); the cross-language half is open and low — names change rarely |
| **ONBOARD-2 leftovers, all low** | No fake-driven end-to-end test of `generate-greetings` (it builds `PayloadClient` directly at `cli.py:2112` rather than via `deps.payload_client or …`, which is why); the per-voice lesson bands (pace, pitch, movement, pauses) were measured by hand and are not in the repo — a good automated *pointer* for the ear, never a gate; `NarrationBudget.report()` says "voiceover ceiling" for a greeting run; a held outcome prints no file paths, and a fresh upload that comes back with different bytes names no Media id or next step; one test claims three refusals and asserts one; no `--take N`; and **the finishing command's cap and its "free from cache" claim depend on the working directory** — `runs/greetings/` lives only under `ZO-pipeline`, so run from anywhere else it starts a fresh ledger and re-buys takes nobody heard | Pipeline Manager; `code-reviewer` | Open, low |
| **`checkCoverUrlIsImage` (admin publish-gate) rejects a relative Payload media path outright** | `apps/admin/src/validation/trackRules.ts:126-164` calls `new URL(raw)`, which throws on a relative path like `/api/media/file/x.png` before the http(s)/extension checks ever run — so the rule accepts only an absolute, externally-hotlinked URL, exactly what COVER-1 exists to move away from. The backend's own `resolveMediaUrl` already treats a relative Payload media path as the expected case and rebases it against `MEDIA_BASE_URL`; this validation rule was never updated to match, so uploading art through Media and referencing it relatively — the intended workflow — fails at Save with a confusing "not a valid URL" error. Worked around during COVER-1's own upload by using an absolute `http://<LAN-IP>:3001/api/media/file/...` URL instead, which passes (the rule does accept `http:`, despite its error text saying "https://") but carries the same LAN-IP fragility as the pre-PILOT-1 `MEDIA_BASE_URL` bug | Architect, 2026-09-23, hit live while walking the founder through COVER-1's upload | Open — fix is narrow: accept a path starting with `/api/media/` (or resolve it against the admin's own origin before validating), matching what `resolveMediaUrl` already assumes on the serving side |
| **The promise's copy hand-syncs one number to `env.ts`'s default, with nothing to catch drift** | "Fifteen minutes" is typed into the promise screen's copy (`OnboardingPromiseScreen.tsx`, rewritten in ONBOARD-3 and pinned by its test) and matches `SESSION_CAP_SECONDS`'s default of 900, which is env-configurable. **The other number ONBOARD-1's copy carried, "500 XP", no longer appears anywhere in mobile source** (grepped 2026-09-25). If the default ever changes the copy goes silently wrong — no test or mechanism would catch it | Manager, ONBOARD-1 completion report, 2026-09-23; re-confirmed by `code-reviewer` (`env.ts:194`), 2026-09-25 | Open, low priority — worth a shared constant or a comment pointing back at `env.ts` next time either is touched |
| **PILOT-1's cache-key independence test doesn't prove independence** | The criterion-9 test constructs two separate `PayloadContentRepository` instances to show `HIDE_PLACEHOLDER_CONTENT`'s two values never share a cached result. Each instance owns its own `TtlCache` (`ttlCache.ts`: `entries = new Map()` is a per-instance field), so **two independent caches would pass this test even if the cache key string dropped the mode suffix entirely** — it re-proves the query where-clause differs by flag, which two other tests already cover. **The shipped code is correct by direct reading** (`` `tracks:${page}:${perPage}:${hidePlaceholders ? 'published' : 'all'}` ``) and real-world risk is near zero since `AppConfig` is frozen at boot, so this is a test-quality gap, not a production defect | `code-reviewer`, at PILOT-1 sign-off, 2026-09-22 | Open, not blocking — rewrite against a single instance's serialised key (or assert on `TtlCache` contents) next time this file is touched |
| **COVER-1's $0.50 ceiling hard-stops image spend, not total spend** | `ImageBudget` computes `max_images` once before generation and genuinely halts on it — the property criterion 5 needed. But the style-guard's own read cost (`guard_spend`) accumulates in a separate, unchecked counter and is never compared to the ceiling before being spent; the report's printed `total: $X of $0.50` line has no conditional, unlike the dimension and Track-touched checks, which do flip to red on failure. **Not live today** — worst case at current rates is ~$0.41, under the cap either way — but the guarantee is narrower than "an explicit ceiling that stops," against `budget.py`'s own stated philosophy that a check nobody enforces is indistinguishable from no budget | `code-reviewer`, at COVER-1 sign-off, 2026-09-22 | Open, not blocking — close before any pipeline package raises per-run image count or rate |
| **⚠️ `CONTENT_API_URL` does two different jobs, and they diverge off localhost** | Its own docstring calls it the backend's **private** path to Payload — *"called anonymously and over private networking"*. It is also the base every **public** media URL is built from, so the client is handed the backend's private address. Same string only while backend and client share a host. **They already do not** — this cost a full session on 2026-09-22, presenting as narration doing nothing and a diagram rendering its alt text. Today it is papered over by an untracked `.env` pinning one Mac's LAN IP, which breaks when the router reassigns it. **At deployment it is not papered over at all**: Cloud Run's private Payload URL and its public media URL are nothing alike. Fix is a separate `MEDIA_BASE_URL`, defaulting to `CONTENT_API_URL` so local dev is unchanged | Architect, 2026-09-22, diagnosed against the running backend | 📤 **PILOT-1, handed off 2026-09-22** — `MEDIA_BASE_URL` splits the public media base from the private CMS path. |
| **`safely()` swallows a failed `play()`, so a dead narration button looks like a working one** | `useNarration.ts` routes all three native call sites through one `safely()` wrapper. **Correct for two of them** — on unmount and on backgrounding, "not playing" is already true, so the exception carries nothing to act on, and that is exactly what its docstring argues. **The third is `toggle`'s play branch, where the reasoning does not hold**: a reader tapped, nothing happened, and the only trace is a `console.warn`. This is *why* 2026-09-22's media-URL bug presented as silence rather than as an error. Nothing is wrong with the crash fix — it stopped a real crash and was mutation-checked | Architect, 2026-09-22, at VO-3's sign-off | 📤 **PILOT-1, handed off 2026-09-22.** |
| **A native iOS build fails on this Mac — `expo-modules-jsi` under Xcode 26.3 / Swift 6** | `JavaScriptCodable+Date.swift:53:50: type of expression is ambiguous`. A transitive Expo dependency pinned before INTRO-1 existed, so **it blocks any package needing a native rebuild here**, VO-3 included. Only one Xcode is installed | Manager, INTRO-1 | **Open, with a route around it — ruled 2026-09-18: Expo Go first, then `expo prebuild --clean`, then a patch bump or EAS Build. Not an older Xcode** |
| **SecureStore survives uninstall and reinstall on the iOS Simulator** | So `zoomout.introSeen` persists across a reinstall, and **anyone checking "does the intro show on first launch" by reinstalling will conclude it is broken.** Clear the key, or use a fresh simulator or device | Manager, INTRO-1 | ℹ️ **Not debt — inherited knowledge.** Carry into any handoff whose gate involves a first-run flag |
| **`buildDoneConstellationLayers` cannot paint a pre-sign-in surface** | It colours every soma ring and core bud in `palette.reward` — earned-progress amber — and **nobody has read anything before sign-in.** INTRO-1 correctly wrote an intro-local painter instead of forking it silently. **A real constraint on the shared primitive**, not a defect in it | Manager, INTRO-1 | ℹ️ Recorded — **any future pre-sign-in or zero-progress surface hits this** |
| **`textDigest` is sensitive to Unicode normalisation, by design** | Precomposed and decomposed *"Héctor García"* hash differently — correct, and proven 2026-09-18 that neither Python nor Node normalises. **The consequence: any future tool that re-saves a Leaf's text through a normalising step silently invalidates every clip on it.** Payload and Postgres do not normalise, so nothing does today. **It fails closed** — the audio disappears rather than mismatching — which is the right direction, but the cause would be non-obvious | Architect, 2026-09-18 | Open — **recorded so the symptom has an explanation waiting** |
| **Two `narrate` processes on one run race on the cost ledger** | **The ledger is what enforces spending ceilings**, and the only guard against a race is a sentence in the README. VO-2 already had one ledger gap — a network drop lost a paid clip's entry, recovered from the files — and now writes spend back after every call. A concurrent run would corrupt exactly the number the founder's ceilings rest on | Pipeline Manager, VO-2 | Open — **a lock, before anyone runs narration for a second book.** **Ruled out of ONBOARD-2.1 on 2026-09-25** (a design, not a procedure): **a precondition of book #3's narration package.** **Slightly more urgent since ONBOARD-2.1: the default is three attempts, so a second process can over-spend by more per clip.** |
| **VO-2's Tier C, harvested at sign-off** | **No live-model suite for the TTS client or the narration guard** — the guard's prompt already failed silently once, and a live suite is what would catch the next one. No CLI-level test of `narrate` or `audition-voices` (node functions are tested; both commands were run live). `audition-voices` overwrites its previous output. Review pages embed the whole track (~19 MB each). `narration_transport` is msgpack in `checkpoint_blobs`, readable through `status` but not by plain SQL. **The narration guard is Gemini grading Gemini**, the same R3 question as the image guard's row. `lameenc` is LGPL — fine while the pipeline is internal and undistributed, a question only if that changes | Pipeline Manager, VO-2 | Open — **none blocks voiceover** |
| **The backend seam has no maximal-fixture contract test, and `manager.md` said for weeks that it did** | `manager.md` described *"one test in `apps/backend`… fetch it through the backend"*. **It does not exist and never has.** `apps/pipeline/tests/test_cms_roundtrip.py` does the same job at a different seam — real Payload, `live`-marked, WP15 cited in its docstring — but it goes **pipeline → Payload → pipeline and never calls the backend** (verified 2026-09-17: zero references to port 3000). **WP15's defect was in the backend mapper**, so the guard named for that defect does not cover it. **VO-1's audio-URL bug is the proof**: same class, survived a green suite, found by reading code. **Seventh instance of documented-intent-absent-behaviour — and the first inside our own process documentation**, which is the hardest place to notice because nobody greps a persona file | Manager, VO-1, by asking rather than assuming | Open — **`manager.md` corrected; the test itself not yet packaged.** Belongs with WP14 test hardening, not in front of voiceover |
| **Ikigai is published with gate 2 never run — `gateTwoStatus` is `pending` on all eighteen Leaves** | Track 42 got ~73 minutes of founder editorial review at gate 2; **Ikigai got none, and nothing at publish time required it.** Verified anonymously 2026-09-17: 18/18 `pending`. **It does not change what a reader sees**, so it did not block the pilot — it means Ikigai's text has had machine review and no human pass. **Publishing being possible without gate 2 is the gap**, and it is the same shape as the Leaf publish gate already on this register | Architect, 2026-09-17 | Open — **decide before Ikigai goes past friends in a room** |
| **25 unresolved `editorialFindings` across 15 of Ikigai's 18 published Leaves** | WP19's editorial reviewer flagged them and nothing resolved them. They are real: Leaf 17's scenario asks *"What approach aligns with the Okinawan principles?"*, which the reviewer calls breaking the fourth wall and turning an immersive scenario into a comprehension quiz — **and it ships that way.** Counted live 2026-09-17. **The pilot is the cheap way to price these:** where readers independently snag on what the reviewer flagged, that measures the reviewer's precision, which is worth more than any individual fix | Architect, 2026-09-17 | Open — **bring pilot notes back and compare against the 25** |
| **A verification command that uses unescaped `[` `]` with `curl -s` fails silently** | `curl` treats brackets as its own glob syntax and **`-s` suppresses curl's error message**, so a Payload `where[field]` query returns an empty pipe rather than a complaint — which surfaces as a downstream `JSONDecodeError` that looks like the *server* misbehaved. It cost the founder a confusing minute on the publish verification. **Percent-encode or pass `--globoff` in anything handed over to be run** | Architect, 2026-09-17 — my own command | Open as a habit, not a defect |
| **There is no way to run the style guard over an image the CMS is actually serving** | **The operation wanted is not a sweep — it is _one Leaf, as Payload serves it_: fetch `scenario.image`, guard the bytes, print the verdict.** That is what proves a fix landed, and the `live` suite structurally cannot do it because it reads committed fixtures rather than Payload. WP33 did exactly this for Leaf 4 with a throwaway script and it is what produced the RED verdict worth having. **`check_style` is imported in exactly one place — `cli.py:397`, inside `generate-assets` — and fires only on a candidate as it is generated.** WP31's closing note promised this command; it does not exist, and the note is corrected. **Consequence today: 'Ikigai has no known style breach' means _at generation time_ for seventeen Leaves and _as attached_ for Leaf 4 only** | Architect 2026-09-16; shape established by Pipeline Manager, WP33 | Open — **not blocking the pilot** (see the sha256 note below); build it after, with the `SecretStr` fix riding along |
| **The paid-tier check reads the label, not the book — and that is the permanent answer at this layer** | A work ingested as `public-domain` that is not public domain passes, reaches the free tier, and nothing downstream notices. **Pipeline Manager asked whether this is the answer or the first half of one. Ruled: it is the answer, for the pipeline.** Whether a work is in the public domain is a **legal determination** — jurisdiction, publication date, author death, renewal — and no code check can make it. What the pipeline can do is make the human's claim **load-bearing and visible**, which it now does: the label decides the transport, the refusal names the label, and the run records it. **So the split is: the pipeline enforces that the label and the transport agree; the Content Curation Policy governs whether the label is true.** That policy is founder item 2, open since the original brief — **and it is now the only thing standing behind an otherwise sound check**, which raises its priority from 'most likely to surprise you late' to 'load-bearing today'. Recorded here because Pipeline Manager was right that it should be written down either way rather than rediscovered | Pipeline Manager, WP32; ruled by Architect 2026-09-15 | **Accepted as designed** — the verification half belongs to the curation policy |
| **A comment naming a function should be checkable, and twice it was not** | `config.py` claimed `require_paid_tier` *"turns that from a memory into a check"* when no such function existed, and `client.py` pointed at `config.paid_tier` as though it recorded something. **The damage is specific and measured: three consecutive completion reports cited that comment as evidence the constraint was enforced.** Both are now true. **Adopted as a rule: a comment that names a symbol gets a test asserting the symbol exists.** It costs nothing, and it mechanises the cheapest slice of the documented-intent-absent-behaviour family that has now bitten this project five times — WP3's ungated payoff, WP6's dropped `missingFields`, the stale reduce-motion register entry, and twice here. **The general version is not buildable; this slice is** | Pipeline Manager, WP32 | Open — **apply the test pattern**; the rule is in force now |
| **The transport record starts from 2026-09-15; Ikigai's and Track 42's runs are attested by completion report only** | Both went through Vertex, and that is a sentence in a report rather than a row anybody can query. **Not reconstructible, and deliberately not backfilled** — Pipeline Manager advised against inventing one and is right: a fabricated provenance row is worse than an honest gap, because it looks exactly like a real one. **This is the cost of the delay between ruling the check urgent on 2026-09-15 and handing it off, which was Architect's**, and it is the concrete form of what that delay bought | Pipeline Manager, WP32 | Recorded — **do not backfill** |
| **Claude on Vertex is not reachable on a free-trial billing account, and Architect twice called it "a console action" — that was wrong** | **Checked against Google's own free-trial documentation, not inferred.** Two independent blocks, either of which is fatal: **(1)** the $300 Welcome credit *"can't be used for a generative AI partner model offered as a managed API"* — Claude in Model Garden is exactly that, so the credits cannot pay for it at all; **(2)** a Free Trial billing account **cannot request quota increases**, which is both why every Anthropic model returns 429 on a default-zero quota and why the increase Architect kept recommending could never have been submitted. `config.py:58`'s note that submitting it is *"a console action for whoever holds the project"* was written 2026-08-29 and is true **only of a paid account**. **Architect repeated it to the founder twice without checking the account type.** **The correction changes the priority, not just the instructions.** Both consumers — R3's editorial reviewer and WP31's image guard — are **advisory**; the proposal is explicit that grounding verification is mechanical and needs no second family, **so the legally load-bearing gate is unaffected either way.** Cross-family is a quality improvement, and it now costs a paid billing account or a second vendor (Anthropic direct, which also means a third DPA before launch). **Recommendation: do not chase it now.** | Architect, 2026-09-15, correcting Architect | Open — **revisit when the billing account changes**, which is due anyway: the trial expires 2026-09-17 |
| **A model-facing prompt that explains a prohibition reintroduces it — three for three** | WP30's removed subject menu was explained *by quoting it*, putting the same five words back into every prompt. WP31's first light-rule rewrite explained hardness as *"an edge you could trace with one line"* — twice — in a contract whose previous paragraph forbids line art, and **both regenerated images came back as outlined drawings**. WP31's own scenario note nearly did it a third time and was moved to a docstring instead. **Telling an image model not to draw a desk mentions a desk.** **Standing rule: a model-facing file states the rule and never the reason. Reasons live in docstrings, which no model reads.** WP31 shipped the enforcement pattern — a test that fails if `line`, `trace`, `stroke`, `outline` or `contour` reappears in that section — and **that pattern should be copied to any prompt section carrying a prohibition**, not left as one file's local habit | Pipeline Manager, WP30/WP31 | Open — **generalise the regression test**; the rule is in force now |
| **The scenario prose outranks the style contract, and it will recur on any scenario naming something undrawable** | Ikigai's Leaf 3 resisted four regenerations because its scenario opens *"Your smartphone is buzzing with group chat notifications"*, and `scenario_image_prompt` puts that text **first, above the contract** — so the most specific instruction requested exactly what the most general one forbids, and specific wins. **This is Leaf 8's spotlight one layer up.** WP30 guarded the *focus* field and WP30.1 stopped it naming a light effect; **neither can see the scenario, because the scenario is written for a reader** and names whatever the situation needs — a form, a screen of messages, a lit sign. **Ruled: no new gate-1 step for this.** The fix landed where it belongs — scene derivation now says the scenario is a situation to evoke, not an inventory to depict — and building a check for a class with one instance whose prompt-level fix just shipped is premature. **Revisit if it recurs on book #3**, which is the honest trigger | Pipeline Manager, WP31; ruled by Architect | Open — **watch, do not build yet** |
| **The classical route to style-guard detection does not work, and the numbers are recorded so nobody spends an afternoon rediscovering it** | Two pure-PIL falloff designs were tried first, `variety.py` being the precedent. **Both score the real breaches below several clean images.** The reason is structural rather than a tuning failure: these illustrations carry **pervasive soft shading by design**, so smooth variation is not anomalous — and **the two Tracks differ more from each other (0.12 vs 0.45 on ramp density) than breaches differ from clean images within a Track**, which makes any calibrated threshold meaningless on the next book. Both tables are in the module docstring. **The consequence is a ruling, not just a note** — see the R3 row below: a check that cannot be made mechanical stops being exempt from cross-family review | Pipeline Manager, WP31 | Recorded — **do not retry the classical route** |
| **The image guard is Gemini grading Gemini, and R3's exemption does not cover it** | The proposal scopes R3 to the *editorial* reviewer and exempts grounding explicitly, on the stated grounds that **grounding verification is mechanical and needs no second family**. **WP31 proved the image guard cannot be mechanical** (row above). **So it has moved out of the exempt category and into R3's, by the proposal's own logic** — and the exemption was never narrowed, it simply never contemplated a check like this. **Ruled: R3 is not narrowed to text.** The general form is **a check that cannot be made mechanical inherits the cross-family requirement.** Consequence: **the Claude-on-Vertex quota increase now gates two things rather than one**, and stops being a background wish. `config.py:58` records that Claude is reachable through the existing Vertex/ADC setup with no new credential and every Anthropic base model returns 429 on a default-zero Model Garden quota — **a console action for whoever holds the project.** Until it lands, the guard stays: same-family grading demonstrably caught a bloom two humans cleared | Pipeline Manager, WP31; ruled by Architect; **cost/eligibility corrected 2026-09-15** | Open — **not achievable on the current account, and not urgent.** Neither consumer is the legal gate |
| **Nothing stops a Leaf being published before the guard has seen it, and three completion reports have now asked people to remember** | Track 50 is published while its eighteen Leaves are drafts, and **that accident is the only reason WP31 could run at all** — the machine account cannot edit published documents, so a published Leaf would have frozen both style breaches permanently. **That is twice now that a publish-ordering accident has decided whether a package was possible.** **Ruled: mechanise it, and it is Manager's rather than Pipeline Manager's** — it needs a verdict field in `packages/shared` plus Payload publish validation, and the pipeline only writes the verdict. Shape: the pipeline records the guard's result on the Leaf; Payload refuses to publish a Leaf whose scenario image carries no passing verdict. **Three reports asking a human to remember is past the bar for a mechanism** | Pipeline Manager, WP31; ruled by Architect | Open — **Manager package, not yet handed off** |
| **Ikigai Leaf 4 carries a real bloom, and two humans cleared it by eye** | A teal radial falloff at a soldering-iron tip. **WP30.1's human pass cleared it and Architect cleared it again at sign-off, looking at the same contact sheet** — the guard caught it. **That is the strongest evidence in WP31 and it is evidence against the reviewers, not for them**: the failure mode of a human style check is not laziness, it is that a small plausible glow inside a busy image does not read as a violation until something measures it. **Ruled: fix it — the $0.14 is authorised.** WP31 correctly did not spend, because the criterion said a flag on the other sixteen was *"a finding, not a failure"*; that instruction was right for reporting and is now discharged | Pipeline Manager, WP31; verified by Architect | Open — **fold into the next pipeline run** |
| **Ikigai's cover hotlinks `taleaway.com` — Track 42's mistake, repeated on the second book** | `coverUrl` is `https://taleaway.com/wp-content/uploads/.../ikigai-hector-garcia-550x717.jpg`. **Someone else's image from someone else's infrastructure, on a published Track**, in a product whose entire posture is care with other people's material — and it is the same defect still open as founder item 1 for Track 42. **The URL is well-formed, so nothing breaks and nothing warns**; this is a correctness-of-conduct problem, not a functional one, which is exactly why it repeated. **Cheap to fix and a human can do it now**: the founder can edit a published Track (only the machine cannot), so uploading a cover into Payload's Media collection and pasting the relative URL closes it without an unpublish. **Worth a publish-time check that rejects a `coverUrl` whose host is not ours** — that would have caught both instances | Architect, 2026-09-15 | 📤 **COVER-1, handed off 2026-09-22** — generated in the house style. **The attach is the founder's**: the machine account cannot reach published content. |
| **Payload's publish gate is narrower than the backend's schema, so a Track can be published and then silently vanish — Track 42's bug, about to repeat on Ikigai** | Payload refuses to publish a Track without a **disclaimer** and **one purchase link**. But `trackSchema` in `packages/shared` requires **five** fields — `publisher`, `coverUrl` (as `z.url()`), `description`, `disclaimer`, `purchaseLinks` — and `mapTrack` rejects the whole document if any is missing or malformed, while Explore's `keepValid` **drops invalid Tracks silently rather than erroring.** **So the two fields Payload does not check — `publisher` and `coverUrl` — are exactly the gap**, and publishing a Track without them produces a book that exists, validates in the CMS, and cannot be seen. **This is precisely how Track 42 spent weeks invisible**: its purchase URL had no scheme, `z.url()` rejected it, and 27 of 28 Tracks reached Explore with the one missing being the only real book. **Live now for Ikigai**: the pipeline deliberately writes none of the four commercial fields (`cms/mapper.py` — *"inventing them would be fabrication of a different kind"*), so Track 50 is missing all four and two of them are unguarded at the publish gate. **Verified by reading both schemas, not inferred** | Architect, 2026-09-15 | Open — **a publish-time check that mirrors `trackSchema`** belongs in `apps/admin`; until then the founder fills all four, not the two Payload asks for |
| **The named-framework breach was in 14 places, not 3 — and most of them were not slide bodies** | WP30 reported Ikigai's Leaf 17 lifting three of the book's *"ten rules of ikigai"*. WP30.1 measured it: **all five sticky notes, the Dinner Table fact, and seven citation notes and quotes across four slides** — text like *"Rule 1 states to stay active and not retire"*, which renders as `sourceReferences` rows. **A fix confined to the five slides would have moved the breach and reported green.** **Ruled: no general phrasing detector.** Pipeline Manager is right that it is not buildable as a check — `surviving_phrases` only proves named strings are gone, and a paraphrase of the same list clears it. **Build the cheap upstream flag instead: gate 1 flags any planned Leaf whose `source_chapters` include a chapter that is itself a named framework.** Ikigai's chapter 61 is literally titled *"The ten rules of ikigai"*, so a title heuristic catches the common case for the cost of a note on a plan. **Second half of the ruling, from the 14-vs-3 gap: a framework fix must enumerate every field that carries prose**, citations and locators included, not only the five slides | Pipeline Manager, WP30/WP30.1 | Open — the gate-1 flag is a pipeline package; Leaf 17's own instance is fixed |
| **The grounding gate has a reproducible false reject, and it is the legal gate** | `normalise_for_quote_match` folds an em dash to a hyphen and then collapses whitespace, so a PDF line break after an em dash leaves the passage reading `world- like` while an honest verbatim quote normalises to `world-like`. **The match fails on a correct quote** — and the function's own docstring says it exists to fold exactly this class of artefact. **The direction is safe**: it rejects honest quotes and can never accept invented ones. **But it is not harmless.** It costs a Pro-model call, and it pushes models toward dropping quotes rather than fighting the gate — **which thins the audit trail, and the audit trail is the legal artefact.** **Ruled: its own package with a fixture corpus across real books, and it goes before book #3**, because every run thins the trail further. Not a one-line edit inside a credit-bound run, which is the call Pipeline Manager already made correctly | Pipeline Manager, WP30.1 | Open — **before book #3** |
| **`asset_style.md`'s light rule is over-broad, and the whole library sits outside it** | The rule reads *"a bright window is a lighter shape, never a shaft of light thrown across a surface"*. Leaf 8's auditorium, Leaf 2's window, Leaf 16's doorway, Leaf 13's table and **the committed anchors** all draw cast light. By the letter, the house style breaches its own contract. **Ruled by looking at the two images that decide it.** Leaf 8's light is a **hard-edged flat polygon** with no gradient — it is a *shape*, which is exactly what *"a lamp is a shape"* asks for and what *"forms are defined by where one colour meets another"* requires. Leaf 3's phone is a **soft radial bloom**. **The real line is falloff, not subject matter**, and that is also the rule's own stated origin — it was added after an anchor came back as a luminous cone that *"cannot be reproduced consistently"*. **So: cast light may be drawn as a flat, hard-edged shape of a lighter surface value. No gradient, bloom, halo, or emissive source.** This keeps the library and the anchors legal and **leaves Leaf 3's glow and Track 42 Leaf 1's card glow as genuine breaches**, which is the correct outcome — the ruling must not launder the defect it was asked about | Pipeline Manager, WP30.1; ruled by Architect 2026-09-15 | Open — **rewrite the sentence**; fold into the image-detector package |
| **Ikigai Leaves 3 and 7 ship with style-contract breaches, knowingly** | Floating notification icons on both, and a soft phone glow on Leaf 3 — **confirmed by looking**. Each was regenerated once; Leaf 3 came back worse. **Pipeline Manager stopped with ~$1.44 unspent rather than buy its way to a pass**, which is the behaviour the ceiling existed to produce and the right call. Both scenarios are about digital distraction, which is why the model keeps reaching for the iconography — **so this is not random, it is a predictable failure mode of a whole scenario class.** This is the output-side detector WP30.1 was deliberately told to leave alone | Pipeline Manager, WP30.1 | Open — **the image-detector package**, which now also owns Track 42 Leaf 1 and the light-rule rewrite |
| **A value that lives only in a shell export is a value the next session cannot recover** | The Vertex project id had never been written down — not in the environment, `gcloud config`, or the repo — and **every command that costs money was blocked on it for an hour.** Now in the README's env table. **Generalised as a rule, because the specific fix does not prevent the next instance:** anything a run needs that exists only as a shell export is undiscoverable to the session after it, and the failure is always at the moment work starts. **Same shape as the paid-tier entry above** — both are operating knowledge that was never written to a file anyone reads | Pipeline Manager, WP30.1 | Recorded — rule, not a task |
| **Track 42's published Leaf 1 renders "$10K" and "$2K" legibly, and carries a glow — two absolute prohibitions, live on published content** | **Confirmed by looking at the image, not by report.** The style contract forbids *"no text, letters, numerals or written symbols of any kind"* and *"no glow, no light cones or beams, no bloom"*; this image breaches both, and Track 42 sorts **first** in Explore. **The deeper gap is that nothing checks the output.** WP30 closed the input half — a focus object whose whole point is its writing is now refused at parse — but a model asked for a laptop will still draw "$10K" on it, and nothing looks. **An output-side text and glow detector does not exist**, and until it does the only defence is a human looking at every image before it attaches | Pipeline Manager, WP30; verified by Architect 2026-09-15 | Open — **its own package**, deliberately kept out of WP30.1 so it cannot put a credit-bound run at risk |
| **The 1:1 named-framework check measures chapter mapping and cannot see phrasing** | Ikigai's Leaf 17 lifts three of the book's *"ten rules of ikigai"* as sticky notes **in the book's own imperative phrasing**, and passed every mechanical gate in the pipeline. `PRODUCT.md` forbids reproducing a named framework 1:1, so this is a breach rather than a near-miss — caught by Pipeline Manager reading its own output, which is the only thing that would have caught it. **The structural cause is upstream of the check**: the plan assigned a synthesis Leaf to the chapter that *is* the ten rules, and gate 1 approved it without a flag. **Ruled: the cheap fix is a gate-1 flag** — a plan that puts a Leaf on the chapter holding a named framework gets a note attached — **not a new phrasing checker.** Recorded explicitly that phrasing is defended by gate 1 and a human reader and by nothing else | Pipeline Manager, WP30 | Open — **the Leaf 17 instance is WP30.1's**; the gate-1 flag is the next pipeline package |
| **No per-Track dollar budget for text — `ImageBudget` has no sibling, and text is where the money went** | Ikigai's text cost **$9.09 against WP20's $2.94** for the same node set, and WP30 overran a stated $5 ceiling by 2x. The lever is `editorial_attempts`: the review/revise loop on the pro model is up to five calls per Leaf and hit its cap on most of Ikigai's. **WP20 made that knob configurable for throughput; it is also the cost knob**, and `editorial_attempts=1` would have roughly halved the book. **Pipeline Manager's own framing is the right one and worth keeping: narrating a rate is not asking about it, and budget judgements are the founder's.** A budget that *refuses* rather than warns is the natural sibling of the one images already have | Pipeline Manager, WP30 | Open — **its own package**, and the default for book #3 |
| **A draft Track is unservable in every environment, so no draft content can ever be device-verified** | `apps/backend/src/content/contentVisibility.ts` returns `content.status === 'published'` outside production — *"a draft is never servable anywhere"*, deliberate, with only the placeholder half relaxed. **This made WP30's device gate unmeetable**: the same handoff required Ikigai to stay a draft *and* required someone to open an Ikigai Leaf in the app, and closing the gap needs a backend change that handoff put out of scope. **Architect's error, and the exact rule already in `agents/architect.md`** about criteria being meetable inside the scope given — now broken after being written down. Pipeline Manager flagged it rather than guessing, which is the right call. **Consequence for every future content package: a draft cannot be observed in the app**, so the observation is of the artefacts or it is deferred to publication | Pipeline Manager, WP30; owned by Architect 2026-09-15 | Open — **decide when a content package first needs in-app observation**; relaxing the rule in development is a real security decision, not a convenience |
| **A visual claim whose images were not retained cannot be reviewed** | WP30's four-image before/after is, by its own report, *"the only evidence that bears on the actual claim"* — and **no image from it survives**: nothing on disk dated 2026-09-14 or later, and Payload's media is unchanged since 2026-09-01 at 146 files. The description is specific and self-critical (Leaf 9 named as the weakest, composition barely moved), which reads as someone who genuinely looked — **the problem is not honesty, it is that neither the founder nor Architect can see it.** **Rule: a package making a visual claim retains the images it is claiming about**, written into WP30.1 | Architect, WP30 sign-off, 2026-09-15 | Open — carried as a standing requirement rather than a fix |
| **The variety floor of 0.60 is calibrated on two real sets, which is thin** | It separates Track 42 (0.502, collapsed) from what the fixed generator produces, and two points is enough to ship and not enough to trust. **Revisit on the third Track.** One deliberate property to preserve through any edit: `test_variety.py` splits synthetic from real evidence on purpose — **synthetic fixtures assert ordering only and never the threshold**, because a renderer that draws rectangles cannot be asked where the line goes. That split will look like an inconsistency to a future reader and is not one | Pipeline Manager, WP30 | Open — revisit at book #3 |
| **The reduce-motion guard catches a flag being *removed*, not a new animation arriving *unflagged* — and the likelier future regression is the second one** | `overridesPassed()` pushes a value only when it finds a `ReduceMotion` enum positionally or a config object that **has** a `reduceMotion` key. A config without the key contributes **nothing** — not `undefined` — so the set assertion (`new Set(overrides)` equals `{Never}`) cannot see a missing flag; it only catches a *wrong* one. **The count assertion is the sole defence, and it is a lower bound** (`toBeGreaterThanOrEqual(7|5|4|2)`). Those bounds are tight — I counted the override sites in the diff and each equals its bound exactly — so **dropping any single flag does fail, which is what the criterion asked for and what the three mutation checks genuinely proved.** But **adding** a new unflagged animation leaves the count at 7 and the set at `{Never}`: green, and silently suppressible. That is the same bug class the guard exists for. **The fix is cheap and expressible with what the helper already records**: assert that every recorded factory call carries an override, rather than counting the overrides. Two smaller notes on the same file: the report and the test's own docstring both say *"counts are exact, not thresholds"* while the code asserts lower bounds — **behaviour right, description overstated, and that is the third member of the claims-more-than-it-asserts family** (see the surviving-mutation row below); and WP28.1's report headline says *"all seven criteria met"* while its own body says the device measurement was not decisive — **the body is the honest part** | Architect, WP28.1 sign-off, 2026-09-11 | Open — **cheap; fold into WP14** with `NextNodeRing`'s fixture coverage |
| **There is no pixel-level measurement instrument for the iOS Simulator, and two packages have now needed one** | WP22.1 caught the original suppression bug *"by measuring pixels across six frames, not by looking"*; WP28.1 needed the same instrument and did not have it, which is exactly why its positive half is argued rather than observed. The simulator tooling has no zoom and no frame-buffer access, unlike the Browser pane's. **Manager's own sizing: `simctl io screenshot` piped through a pixel-diff script would likely suffice**, and was correctly judged not worth building for a single package. **It becomes worth building the moment a third package needs to prove a subtle animation is running rather than merely not crashing** | Manager, WP28.1 | Open — build it when a package needs it, not before |
| **The simulator device gate is workable, and the three things that made it work must be carried into WP25/WP26** | After four packages of friction, WP24's device gate ran cleanly. **Three reusable pieces, all from WP24's log entry:** the **coordinate-space correction** (screenshot pixels are not the tool's tap-point space — this was the actual cause of most of WP21–23's tap failures); a working **`xcodebuild` invocation that routes around an `expo run:ios` signing bug**; and **zero-tap `simctl` commands for switching theme and text size**, which removes the most tap-heavy part of every remaining visual gate. **The last one matters most for what is left** — WP25 and WP26 both gate on both themes at accessibility-max, and that is now a command rather than a tap sequence | Manager, WP24 | ℹ️ Not debt — **must be an inherited-knowledge block in WP25's and WP26's handoffs**, the way WP22's curve batching was carried into WP23 |
| **Superseded — see above** | WP27 checked the Reduce Motion lead from WP26 and **eliminated it: the OS setting reads off and Reanimated says on regardless.** That disagreement is a finding in its own right and nobody has looked at it directly. **If Reanimated believes reduce-motion is on permanently, every animation that does not carry `ReduceMotion.Never` is being silently suppressed** — meaning WP22.1's flag is not belt-and-braces on the surfaces that have it, and the surfaces that do not have it are running degraded without anyone knowing. **Separately, the Leaf-player tap failure now has three independent reports** (WP25, WP26, WP27) and has survived one workaround per package | Manager, WP27 | 📤 **WP28 proposed** — a diagnosis, not a fourth workaround |
| **The achievement glyph is `'leaf'`, and a real illustration is still owed** | WP27 removed the trophy from all three consumers. The replacement is Ionicons' leaf — thematically right and, more to the point, not the glyph `screen-11` bans — but **it is a stand-in, and it is the same category as `ShareCard`'s `MascotSlot` placeholder**, which has been reserved since WP9 and never filled. **Two placeholder illustrations now wait on the same thing** and should be solved in one pass rather than separately | Manager, WP27 | Open — pair with `MascotSlot` whenever a real illustration pass happens |
| **iOS Simulator with Reduce Motion ON shows a debugger banner that silently swallows every touch in the bottom ~15% of the screen — the tab bar included** | Found in WP26's device pass. **No visible cause**: the banner reads as a warning, not as an overlay, and taps beneath it simply do nothing. **Zero-tap fix, same family as the existing theme and text-size route:** `xcrun simctl spawn <udid> defaults write com.apple.Accessibility ReduceMotionEnabled -bool NO`. **The nasty part is the interaction**: we turn Reduce Motion on *precisely* to verify the swap-never-remove path, and doing so breaks input in the region containing the primary navigation. **Manager flags it as a possible root cause of WP25's still-open "Next button won't register a tap" — a lead, not a confirmed fix.** This is a second, independent cause of tap failure alongside the coordinate-space error found in WP24 | Manager, WP26 | Open — **check this first in any package that hits unexplained tap failures**, and carry the command in every remaining handoff |
| **Verifying against placeholder content instead of real content has now happened three times** | WP22.3 device-verified on the 20-Leaf placeholder Track but **not on Track 42, whose titles are the long ones the package exists to accommodate** — its Library card would not respond to tap. **Manager's reasoning for why it cannot differ is sound**: spacing floors at a worst-case two-line label and `roadmapLabels.ts` caps at two lines, so a longer title cannot exceed the floor. **Accepted on that basis.** But the pattern is now three: WP15.8's 21 media-less placeholder Leaves, WP22's 18-node coincidence with Track 42, WP23's 3–4 note range against a 2–6 schema. **Each time, the placeholder corpus failed to exercise the case the package was about** | Architect, WP22.3, 2026-09-10 | Open — **re-check the roadmap on Track 42.** The blocker is gone (WP28 closed 2026-09-11, and there was never a tap bug), so this is now simply undone. **WP28.1 made it four**: its device gate ran on the 20-Leaf placeholder Track again. Defensible there — `NextNodeRing`'s behaviour does not depend on Leaf count or title length — but it was the natural opportunity and it passed |
| **React Native's jest preset reports `fontScale: 2`, so every screen test in this repo renders the degraded layout** | Found in WP22.2 after a confusing failure with no defect behind it. **Consequence, and it is not small: no screen test in this codebase exercises the default text size.** They all run as though the reader had doubled it. Anything keyed on text scale — WP23's sticky-note collapse threshold, WP22.2's label budget, any future responsive branch — is being asserted in its *degraded* state and its normal state is untested by construction | Manager, WP22.2; sharpened by Architect, WP22.3 | Open — **name it in every remaining handoff.** A screen test that passes says nothing about the default rendering. **And it is worse than "tests run degraded":** because the preset's `fontScale` is *fixed*, it never changes during a test, so **a missing `fontScale` dependency is invisible to the suite by construction** — which is exactly the pre-existing memo bug WP22.3 found by hand. A whole class of reactivity bug cannot be caught here |
| **A surviving mutation exposed a test whose name claimed more than its assertion — the second time** | *"Gives every Leaf a label, on both sides of the spine"* stayed green when label placement was flipped inward, because nodes exist on both sides whichever way the labels point. The name describes a guarantee; the assertion checks a weaker fact. **This is the 2026-09-02 naming rule in a new place** — that one was about a check covering one of four stated conditions while being named for all four. **Same defect, applied to tests rather than guardrails**, which suggests the rule should be read as being about names generally rather than about guardrails specifically | Manager, WP22.2 | ℹ️ Recorded — mutation testing is what caught it, which is the argument for keeping it |
| **The roadmap's dendrites went from ~450 curves to ~9,900, and frame rate was never instrumented** | Element count is unchanged — they batch into the same few dozen native paths — and scrolling held across repeated passes, but nobody measured it. **`GRAPH.dendriteBudget` is the single constant to turn down** if stutter is ever reported | Manager, WP22.2 | Open — low. Trigger: any report of scroll stutter on the roadmap |
| **`NextNodeRing` is not covered by the new reduce-motion guard, and it is the surface with the most to lose** | WP28 shipped `reduceMotionCallSites.test.tsx` — **the guard WP22.1 asked for and could not write.** It spies on Reanimated's animation factories rather than the rendered tree, because suppression jumps an animation straight to its final value and the rendered output is identical either way. Mutation-checked three ways including the hard case: dropping only the **outer** `withDelay` flag while leaving the inner `withTiming` correct. **`NextNodeRing` is excluded** — private component, needs a full graph fixture — **and it is the one that matters most**, being the only wayfinding marker on a long scrolling roadmap | Manager, WP28 | Open — **WP14.** WP28.1 deliberately did not close it: the handoff scoped the guard extension to branches already covered, not to new fixture infrastructure. `NextNodeRing`'s override is instead verified by reading the diff and on-device |
| **Four of the redesigned screens contain no animation at all** | Track complete, the share card and all four tab screens have **zero Reanimated, zero `Animated.`, zero `LayoutAnimation`.** Measured in WP28 while scoping the override question. **This dissolves a concern rather than raising one** — there is nothing there to run degraded — and it narrows every future motion question to the roadmap and the two Leaf slides | Manager, WP28 | ℹ️ Not debt — a bound worth knowing |
| **Unexplained: WP27's report of the Leaf header close button being unresponsive** | WP28 could not reproduce it — the button closed the Leaf on first tap. **Manager has two candidate explanations and evidence for neither, and recorded it as unexplained rather than folding it into the tidy answer.** That refusal is the right instinct: the rest of WP28 is a clean two-cause story, and an unexplained fourth observation is exactly the thing a satisfying narrative absorbs | Manager, WP27; unreproduced WP28 | Open — low. Re-check if it recurs |
| **A growing set of screens has never been observed rendering, and the mitigation that is working is not the one I ruled** | Four packages have now lost real time to simulator automation, each with a different cause, and **WP23's board shipped with its on-screen rendering unverified** — clipping, theme and the collapse threshold all rest on reasoning. The 2026-09-09 instrument split routed interaction checks to the founder; here the founder reasonably declined to spend the time, so **neither instrument covered it.** Acceptable today because nothing is deployed and no reader exists — **but it accrues silently, and "we never actually looked" is the worst thing to discover at deploy.** The mitigation that is demonstrably working is different and worth naming: **Manager has twice moved the hard part into a pure module** — `roadmapGeometry.ts`, now `stickyNotesLayout.ts`, both Tier A and mutation-checked — which shrinks what observation has to cover rather than arguing about the instrument | Architect, WP23, 2026-09-11 | Open — **WP12 must carry a visual sweep of every redesigned screen before deploy.** Standing preference meanwhile: push layout decisions into pure modules |
| **Track 42's sticky-note counts are 3–4 — the schema's real extremes live only on placeholder Tracks, one of which is scheduled for deletion** | The board must handle 2–6 notes. **Track 42, the only real content, never exercises either edge.** The 2- and 6-note Leaves exist only on placeholder Tracks including *"The mountain is you"* — **which is on the founder's list to unpublish.** Doing that removes the only test data for the board's extremes. **Third instance of this project's recurring shape**: verifying against real content alone proves less than it appears, after WP15.8's 21 media-less placeholder Leaves and WP22's 18-node coincidence | Manager, WP23 | Open — **check the board's extremes before unpublishing that Track**, or keep a fixture that preserves them |
| **Reanimated suppresses reduced-motion animations itself, which silently turned our "swap" into the "removal" the rule forbids** | Found in WP22 and **proved by measuring pixels across ten frames per mode**, not by inspection: before the fix, six frames were identical to three decimal places; after, clean oscillation. Blast radius is every animated surface — WP22's own site is fixed; `AuthStack`, `PayoffSlide`, `ScenarioSlide` and `AchievementUnlock` are unverified and **not** claimed broken. Honest severity: those are one-shot reveals, so the default snaps them to their final value rather than removing feedback outright — milder than a repeating indicator, still not what the code says. **Invisible to every test by construction** | Manager, WP22 | 📤 **WP22.1**, ruled 2026-09-09 — fixed at the mechanism, not the call sites |
| **Locked dendrites at 0.28 opacity are near-invisible on the light theme** | Deliberate in WP22 — the node outline and label carry the state, so no information is lost — but **the opacity is a single theme-independent value**, and light needs more presence than dark for the same perceived weight. Splitting it per theme is the fix if the tissue should read on white | Manager, WP22 | Open — low. Trigger: the first time the light theme gets a real design review |
| **Two techniques from WP22 that the remaining screen packages should inherit rather than rediscover** | **Curve batching:** concatenating every curve sharing a colour and stroke width into one `d` turned ~450 curves into **20 native `<Path>` elements**, flat at any Leaf count — it is why dendrite density did not have to be cut and no Track length was capped. **Any future SVG-heavy screen starts there.** And **`roadmapGeometry.ts` is a pure `(leafCount, viewport, seed) → geometry` function with 187 tests across 15–30, seeded from the Track id** — so WP26's finished-Track constellation and any compressed graph strip should **consume it, not re-derive geometry**, and will agree on shape for free. Also: node labels use `small`, not `caption`, because `caption` uppercases and eighteen real chapter titles in caps read as signage | Manager, WP22 | ℹ️ Not debt — **must be named in WP23–WP26's handoffs** |
| **The client cannot tell which specific Leaves a reader has finished — only how many** | `LeafSummary` is `{id, trackId, orderIndex, title, isPlaceholder}` and `TrackProgressSummary` is counts plus `nextLeafId`. **The roadmap's node states are therefore derived, not read**: first `completedLeaves` in `orderIndex` order are done, the next is `nextLeafId`, the rest locked. That is only true while completion has no gaps. WP22 is required to cross-check it against `nextLeafId` and degrade honestly if it ever fails, which makes the assumption self-reporting rather than silent — but **the derivation is still an assumption, and this project has twice been burned trusting a derived count** | Architect, WP22 planning, 2026-09-08 | Open — **acceptable while the unlock path is strictly sequential.** Trigger for a real per-Leaf field: anything that lets a reader complete Leaves out of order, or WP22's cross-check firing even once |
| **A one-line patch inside `node_modules` is load-bearing for every iOS build on this machine, and it is not in the repo** | `expo-modules-jsi`'s vendored `JavaScriptCodable+Date.swift:53` does not compile under Swift 6.2 — a strictness regression in Expo's own source, not ours. Manager qualified the call to `Swift.abs(milliseconds)` to unblock WP21's build. **Correctly not in the git diff, which is exactly the problem**: a fresh `npm install`, a new machine, or CI hits the same compile error and has to find the recovery in a log entry. **This is the project's signature failure shape — the codebase knows the fix, it knows it in the wrong place** | Manager, WP21 | Open — **`patch-package` (or an Expo config plugin) makes it survive a reinstall.** Trigger: the next `npm install`, an SDK bump, or the first time a second machine or CI builds iOS |
| **Tapping the app's pill buttons via simulator automation fails repeatedly — investigated, and it is not the app** | Cost real time in **both** WP15.8 and WP21; Manager twice found asking the founder to tap by hand faster than hunting coordinates. **Checked rather than assumed:** `Button.tsx:100` sets `minHeight: MIN_TOUCH_TARGET` (44), it is a plain `Pressable` with `spacing.md`/`spacing.xl` padding and no animated wrapper, so the hit region is not smaller than the visible pill. A human finger works; synthetic coordinate taps do not. **Leading hypothesis, unverified: these buttons sit inside `Screen`'s ScrollView, and a synthetic tap with any drift is claimed by the scroll responder — which would explain why `TextInput` (focuses on touch-down) and the tab bar (outside the ScrollView) are never affected** | Manager, WP15.8 + WP21; ruled out as a code defect by Architect, 2026-09-08 | Open — **a tooling cost, not a product defect. Do not change app code to suit the automation.** Five device-gated screen packages remain, so a cheap mitigation is worth finding; the founder tapping is a valid one |
| **This machine's iOS toolchain moved during WP21 and the new baseline is undocumented elsewhere** | Xcode 16.4 → **26.3** (a real Swift-tools-version requirement, not optional), the `iPhone 16 Pro` simulator recreated on the iOS 26.3 runtime after the old one stopped producing a screen surface, and an Apple ID added to Xcode Accounts so builds sign with "Sign to Run Locally" — which is what **unblocked `expo-secure-store`'s Keychain entitlement** and therefore any device gate behind sign-in. Durable and good news: future packages will not hit these walls. Recorded because a major toolchain bump remembered by nobody is how a future "it used to build" starts | Manager, WP21 | ℹ️ Not debt — the machine's baseline, written down |
| **A root `test`/`lint`/`build` kills a running backend dev server** | All three root scripts run `build:shared` first, which rewrites `packages/shared/dist` — and a `tsx watch` backend holding those files dies when they vanish. **Cost time in WP15.8** and reads as an unrelated crash: the gate passes, and the server you were about to run the device gate against is gone. Not a defect, a sequencing property | Manager, WP15.8 | Open — **run the cold gate before starting the servers, not after.** Recorded so it is not re-diagnosed |
| **`Array.prototype.map` passes the index as the second argument — a trap for every mapper that gains a parameter** | `response.docs.map(mapTrack)` was correct until `mapTrack` took a `baseUrl`, at which point it would have silently received `0`, `1`, `2`. WP15.8 wrapped all four call sites in arrows and avoided it. **Recorded because the next parameter added to a mapper meets the same trap**, and TypeScript only catches it while the new parameter's type is incompatible with `number` | Architect, WP15.8 review | ℹ️ Avoided, not open — a working note |
| **Production media serving has never been designed** | The unenforced half of WP15.8's invariant. That package makes the mapper emit a resolvable URL; **it does not make that URL reachable from a phone in production.** Payload must not be publicly exposed — the top security item on the launch list — so a URL resolved against Payload's origin will not load once deployed. Closing it needs a storage adapter writing to object storage, or the backend proxying media. **Neither exists and no package owns it.** Logged at handoff rather than sign-off because the gap is known now | Architect, 2026-09-02 | **Open — WP12, and now the only thing standing between Track 42 and a real phone.** Confirmed still open by Manager at WP15.8 sign-off. Must be in that handoff, not discovered at deploy |
| **Payload `destroy()` does not close its database pool** — it only resets in-memory schema state, and `pool.end()` hangs because Payload keeps a client checked out | Affects anything booting Payload **outside a request lifecycle**: WP3's integration tests if they seed via the Local API, and the WP11 seed script. **Not** the `ContentRepository` runtime path, which calls REST over HTTP and never boots Payload | Manager, WP1 | Open — mitigate once in a shared test/seed harness, not per caller |
| **Payload attaches no `error` listener to its pool** — an idle-client error becomes an uncaught exception | Same blast radius as above: test harnesses and the seed script | Manager, WP1 | Open — same shared harness |
| **No rate limit on answer submission** | Brute-forcing a scenario is trivial *by design* (3 options, unlimited retries), but it is an unbounded write path — each attempt is a row update plus a cached CMS read | Manager, WP4 | Open — pre-launch, on write-volume grounds not answer-secrecy grounds |
| The answer response identifies the correct option by elimination — two wrong answers leave one | Inherent to 3 options plus unlimited retries as specified, not a defect | Manager, WP4 | Recorded so WP8 does not rediscover it as a bug |
| **Payload's own REST API serves `payoff` and `isCorrect` anonymously** — by design, since the backend calls anonymously to get published-only content | **The highest-consequence item in the WP4 review.** WP4 made the payoff gate the product's central guarantee, and that guarantee stops at the ZoomOut API. If Payload is reachable from the public internet in production, both the payoff gate and the answer key are bypassable by calling Payload directly. Private networking is currently only a comment in `payloadClient.ts` — nothing in code, config, or `.env.example` enforces or records it | `code-reviewer`, WP4 | Open — **must become an explicit, verified deployment requirement before any deploy.** Not a code change; a deployment constraint that currently exists only as prose |
| **`leaf_progress.attempt_count` is a PG `integer` with no ceiling** | Extends the rate-limit item above. Sustained wrong answers reach 2^31 in under two weeks at a few thousand/sec, after which every further answer on that Leaf raises `integer out of range` — a permanent 500 on a Leaf the reader can never complete, contradicting "unlimited retries". Self-inflicted, but a terminal state | `code-reviewer`, WP4 | Open — rate limiting on `POST .../answer` closes both this and the write-amplification concern |
| **`toDomainProgress` validates on the *response* path, but the error handler maps any `ZodError` to `400 INVALID_REQUEST`** | A server-side data-contract violation would be reported to the client as a malformed request, with internal field paths in `details` — misattributing our bug to the caller. Latent: every column is constrained tighter in Postgres than in Zod | `code-reviewer`, WP4 | Open — handler design predates WP4; fix when the error handler is next touched |
| **A non-numeric `:leafId` may 503 rather than 404** | `leafIdParams` accepts any non-empty string, reaching Payload as `where[id][equals]=abc` against serial integer ids. `FakePayload` returns empty docs so tests see a clean 404; real Payload may error → `ContentUnavailableError` → 503. Not an injection risk — `URL.searchParams.set` encodes properly | `code-reviewer`, WP4 | Open — unverified without the live CMS. Pre-existing from WP3 |
| **API-shape notes for WP7**: `CompletionOutcome.xpAwarded` (paid by *this call*) and `progress.xpAwarded` (the total) share a name in one response body; `start`/`GET` wrap in `{ progress }` while `answer`/`complete` return flat objects | A client author reading JSON rather than TypeScript will misread the first. The second is inconsistent within one module | `code-reviewer`, WP4 | Open — state in the WP7 handoff |
| `ProgressRepository.findReaderTimezone` selects from `users`, which auth/users owns | Bounded-context bleed. A small injected port would be cleaner | `code-reviewer`, WP4 | Open — low priority |
| **`listTracks` returns Payload's unfiltered totals** — in production a page of placeholder Tracks yields fewer rows than `totalTracks` claims | UI pagination will under-report. Ruled approach: `where` filter in the Payload query, service guard retained | Manager, WP3 | Assigned to **WP7** — must be stated in that handoff, not discovered in the UI |
| **`listLeavesForTrack` caps at 100 Leaves with no paging** | A Track is specified at 15–30 so this is comfortable, but it is a silent ceiling rather than an error | Manager, WP3 | Open — revisit if Track length ever changes |
| **Content cache is per-process and unbounded in entry count** | With multiple instances the TTL becomes the *worst-case* takedown latency across them — a legal-obligation concern, not just performance | Manager, WP3 | Open — must be revisited before horizontal scaling |
| **No ETag / `If-None-Match` on content responses** | Mobile refetches full Track lists on every Explore visit | Manager, WP3 | Open — consider in WP7 once payload size is real |
| `user_tracks.status` exists and is always `active` | Nothing sets it yet | Manager, WP3 | Open — WP4/WP5 own the transitions |
| **Integration tests use a Payload stand-in — a real response shape could go unseen** | Mitigated by a manual end-to-end run against the live CMS at WP3 | Architect, WP3 review | Open — **re-run the manual end-to-end check after any Payload upgrade**; added to the upgrade checklist |
| **Testcontainers intermittently skips whole integration suites** when they run back to back (`inspectContainerUntilPortsExposed`); an immediate re-run passes | An occasional red CI build that is green on re-run is this, not a regression | Manager, WP2.1 | Open — add a retry step to the workflow if it recurs |
| **Payload emits `id: number`, domain uses `cmsIdSchema` string** — serial integer keys | The WP3 mapper must stringify ids and handle relationships arriving populated or bare depending on `depth` | Manager, WP2.1 | Assigned to WP3 |
| CMS↔domain divergences documented in `payload.config.ts`: `trackId` is `string \| Track`; `stickyNotes.notes` is `{ note }[]` not `string[]`; `scenario.options` is a plain array not a 3-tuple; Payload adds `_status`, timestamps, row ids | `ContentRepository` must map across these. Left unreconciled, WP3 hand-rolls the mapping and WP4 inherits it | Manager, WP1 | Open — reconcile at the schema-freeze gate |
| `apps/admin/CLAUDE.md` + `AGENTS.md` are generated by `next dev` and committed | Third-party tooling injecting agent instructions into a repo where `CLAUDE.md` is a controlled artifact. Content is benign; the mechanism is unreviewed | Architect, WP1 review | Open — recommend a header noting it's tool-generated and that root `CLAUDE.md` wins |
| `typescript.declare: false` costs loose slug typing inside `apps/admin` | Bounded to one workspace with three collections; integration tests use bracket access for `_status` / `isPlaceholder` | Manager, WP1 | **Accepted** — ruled 2026-08-07, revisit only if `apps/admin` grows |
| `publicLeafSchema` derives from the Leaf shape *before* the Dinner Table Knowledge refinement | Safe today — `toPublicLeaf` only accepts an already-validated `Leaf`. Becomes a hole the moment anything parses a public Leaf from untrusted input | Manager, WP0 | Open — WP3 handoff must forbid parsing untrusted input with `publicLeafSchema` |
| 14 moderate dev-only npm advisories (drizzle-kit's bundled esbuild-kit and vite transitives) | Dev-only, no runtime exposure | Manager, WP0 | Accepted — revisit when upstream updates. Not worth forcing breaking upgrades |
| **React Native Testing Library v14 made `render`, `renderHook`, `fireEvent` and `unmount` async** | Un-awaited they fail as `undefined.current` or as assertions against a tree that never committed — errors pointing nowhere near the cause. `jest-expo` also does not set `IS_REACT_ACT_ENVIRONMENT` | Manager, WP6 | Recorded so WP7 does not rediscover it; setup fixed |
| **Password reset is now the only account-recovery path** | Was survivable while social sign-in offered an alternative way in. With email/password as the sole method, a reader who forgets their password has **no route back into their account at all** — and their streak, library and XP are behind it | Architect, 2026-08-11 | **Open — priority raised. Pre-launch blocker.** Needs a transactional email provider, not an auth migration |
| **Push notifications are not scoped anywhere** | The streak is a core retention mechanic and a streak without a reminder is much weaker — a reader who forgets loses it with no prompt. FCM or Expo Push, usable without adopting Firebase as a backend | Architect, 2026-08-11 | **Open — needs scheduling.** Pairs naturally with WP5, which owns streaks |
| **No deployment or hosting work exists in any work package** | Every package from WP0 onward lists infrastructure as out of scope, and no package was ever written to cover it. Nothing is deployed anywhere. The simulator can reach `localhost`; **a real device cannot**, so this becomes pressing around WP8 when the founder wants the app on their phone — and it is obviously required for launch | Architect, 2026-08-11 | **Open — needs its own work package.** Not yet scheduled |
| **Explore has no pagination affordance** — stops at twenty with no indication anything follows | Live rather than theoretical now that the corpus is 28 Tracks | Manager, WP11 | **Assigned to WP8** |
| **Three XXXL layout issues exposed by realistic titles** | Only visible once seeded content had real-length titles rather than fixture stubs | Manager, WP11 | **WP14** |
| **`apps/backend/.env` pointed at `zoomout_cms`, not `zoomout`** — so `daily_session` and `streak` never existed in the database holding real readers, `db:migrate` wrote 0004 and 0005 into Payload's database, and `apps/admin` now fails to boot at Payload's schema-pull | **The real cause of WP5a's failed device check**, and the third instance of verifying execution rather than effect — this time inside the very package whose acceptance commit recorded that rule. Recoverable: WP11's seed is idempotent, so CMS content is regenerable | Manager, WP5b | **Part A reopened.** Acceptance now phrased as observable state, not actions |
| **A timezone test proves nothing unless the two timezones disagree at the instant it picks** — WP9 replaced an `at time zone` conversion with a naive `::date` cast and all nine tests still passed, because the chosen instant was one where the server and Auckland agreed | Distinct from not writing the test, and identical to correct in a green run. Three local-date invariants in this project ride on exactly this kind of test | Manager, WP9 | Added to `agents/manager.md` |
| **Some assertions cannot be mutation-checked** — "wrapping up does not lock the reader out" asserts the *absence* of a mechanism, so there is no line to break | Guards a future regression rather than proving present behaviour. Knowing which tests are which is real WP14 information | Manager, WP9 | Added to `agents/manager.md`; Manager now reports them |
| **The achievement share screen was never walked end to end** — same `ShareCard` and capture path as the wrap-up screen, both verified, but its own route was not exercised | Reported honestly rather than counted | Manager, WP9 | **WP10's device session** |
| **Metro must be restarted after adding a native module** — a stale bundle silently serves the previous JS and is indistinguishable from a feature that does not work | Cost time twice, in WP5b and WP9 | Manager, WP9 | Recorded |
| **`GET /health` is not a schema probe** — it returned `{"status":"ok","checks":{"database":"up"}}` while every table it depends on had been dropped | Anyone verifying a deploy with it would see green against an empty database. **A WP12 requirement, not a note**: the health check must assert schema presence, not just connectivity | Manager, WP5b | **WP12** |
| **Migrations are not idempotent against a half-cleaned database** — `0005` begins with `CREATE TYPE reader_event_type` and fails where that type survives | One missing object aborts the whole transaction. Cleanup SQL should use `drop ... if exists` | Manager, WP5b | **WP12** |
| **Extra-large text clips app-wide** — absolute `lineHeight` in `design/typography.ts` while RN scales `fontSize` only | Founder ruled 2026-08-12 not to fix it and to drop it as a per-package check. Removed from `agents/manager.md` so it stops being re-reported. **Honest framing: "renders broken", not "unsupported"** — the app honours the OS setting and then clips, which is the worse state to ship | Manager, WP5b | **Moved to `launch-blockers.md`** |
| **Endpoint tests can pass while the storage guarantee beneath them is gone** — replacing `onConflictDoNothing` with an upsert left all nineteen endpoint tests green, because the service filters already-held achievements before reaching the insert | Only a repository-level race test reddens for that mutation. **The most useful WP14 input so far** | Manager, WP5b | Recorded — repository-level test added |
| **Verification that observes execution rather than effect is not verification** — WP5a's `printf >>` created a 235-byte stray `apps/backend/.env.example`; building `.env` from that stub produced a file with no `DATABASE_URL`, and the write silently no-opped. The check confirmed the command ran, not that it changed anything (`re.sub` succeeds when it matches nothing) | Same class as WP4's criterion that passed without the mechanism working. Cost WP5a its device check | Manager, WP5b | Recorded as a standing rule |
| **Backend has no `apps/backend/.env`** — it runs only from a shell with `DATABASE_URL` already exported | **Has now blocked two packages**: WP11's CMS operator account and WP5a's device check, where a migration landed in Payload's database rather than the backend's. Will block WP12. Ruled 2026-08-12: gitignored `.env` plus `--env-file` on dev and migrate scripts is standard practice, not a compromise — the password lives only in the untracked file | Manager, WP5a | **WP5b, first item** |
| **Session time is elapsed-time-per-Leaf, clamped to five minutes** | The only signal available without a client heartbeat. Under-counts a genuinely slow reader, which errs toward letting them continue — the right direction to be wrong for a wellbeing feature | Manager, WP5a | Open — **a real activity signal needs scheduling**, not deferring indefinitely |
| **Trap: `CASE … ELSE NULL` beside a bound parameter will not plan in Postgres** | Surfaced as a 500 on completion and reddened twelve integration tests with `expected undefined to be false`, no type error anywhere | Manager, WP5a | Recorded — it will recur |
| **Trap: a 24-hour test span outlives a 15-minute access token** | A date test then fails with a 401 that looks exactly like a date bug | Manager, WP5a | Recorded — it will recur |
| **Re-reading a finished Track from Library is not possible** | Needs a `firstLeafId` — a shared-type change plus backend rollup work the WP8 handoff did not ask for | Manager, WP8 | **WP14**, or its own small item if it turns out to matter sooner |
| **The on-device partial-rollup render is unverified** — needs the simulator signed out, and partial progress cannot be produced by tapping until the Leaf player exists | Verified at the API instead: 7 of 20, `status: active`, `nextLeafId: 9`, 700 XP — consistent with 80+20 across seven first-try Leaves | Manager, WP11 | **WP8** — becomes verifiable by tapping |
| **The draft filter is definitive at config but never empirically exercised** — both content collections set `read: publishedOrAuthenticated` and `PayloadClient` calls anonymously, so drafts cannot reach us | Correct, and no redundant filter was added. But the running corpus holds one Track and one Leaf, both published, so nothing proves it | Manager, WP7 | **WP14** — needs WP11's seed to create a draft that *could* leak |
| **`fakePayload` ignores `page` and `limit`**, always answering `page: 1, totalPages: 1` | The `listTracks` totals fix is verified for a single page only | Manager, WP7 | **WP14** |
| **Cover images are unvalidated URLs** — `trackSchema` requires a URL, not an *image*. The seeded Track's `coverUrl` points at an Amazon product page, so every card renders the fallback | Silent and total: the whole Explore surface looks broken while every test passes and the data is technically valid | Manager, WP7 | **Assigned to WP11** — add a CMS-side rule before real content is seeded. Cheapest possible moment to fix it |
| **The progress rollup is N+1 against the CMS** — one Track fetch and one Leaf-list fetch per library entry | Cached and concurrent, fine for a few books; thirty books is thirty Leaf-list requests per Library open | Manager, WP7 | Open — **deferred deliberately.** Ruled approach when it matters: batch Track reads with Payload's `where[id][in]`, and cache the ordered Leaf-id list on a longer TTL than content since it is structural. **Do not add a stored counter** — see the note below |
| **`Track.leafCount` exists in the frozen schema but must not be trusted as the rollup total unless a CMS hook computes it** | Manager correctly refused a counter on `user_tracks` because it would drift the moment a Leaf is added or withdrawn. `Track.leafCount` is the *same* stored-counter problem: safe only if a Payload hook maintains it, unsafe if an author types it | Architect, WP7 review | Open — resolve if and when the N+1 batching is done |
| **No pagination UI in Explore; `GET /library` is unbounded** | Both are the same shape of problem — behaviour with a lot of content, which does not exist yet | Manager, WP7 | Open — pair them, post-content-scale |
| **Changing OS text size while the app runs leaves Expo Go rendering the old line height** | Looks like a severe layout bug and is not one. Wasted time on WP7's accessibility verification | Manager, WP7 | Recorded — **verify accessibility from a cold start with the size already set** |
| **Two overlapping 401s can produce two refreshes** — `client.ts` clears `refreshInFlight` in `finally` and never compares the token a request *sent* against the current one, so a request dispatched during the refresh window carries a stale token and its late 401 starts refresh #2 | Confirmed by execution. **Not a security problem** — `adopt()` is awaited inside `performRefresh()` before the promise resolves, so refresh #2 reads the rotated token and no family revocation occurs. One extra round trip in a ~1-RTT window. Fix: record the token used per request, retry without refreshing when it has already changed | `code-reviewer`, WP6 | Open — follow-up, not blocking |
| **Server-side validation failures surface on the wrong screen** — `SignUpScreen` validates email as `includes('@')` and ignores the backend's 256-char limit; nothing submits until after the age gate, so `reader@example` fails with "Request body is invalid" on the *age gate* | Recoverable (draft survives `goBack()`), but badly signposted. Also `MINIMUM_PASSWORD_LENGTH = 12` duplicates a backend literal with only a comment tying them together — nothing in `packages/shared` holds it | `code-reviewer`, WP6 | Open — follow-up |
| **`responseFields` can clobber `code`** — `app.ts` spreads `{ code, message, ...responseFields }`, so an opted-in error could overwrite the client's branch key | Nothing does today | `code-reviewer`, WP6 | Open — `Omit<…, 'code' \| 'message'>` on the getter's return type |
| **Coverage gaps in WP6**: `SignUpScreen` has no test at all (the only screen holding validation rules); no test asserts `SignInScreen` → `ProviderEmailMissing`; `ProfileScreen`, `TabShell`, `RootNavigator` and the three shells have no render test in either theme | The provider path is unreachable under Jest because the buttons are hidden — worth making `SignInScreen` accept an injected provider | `code-reviewer`, WP6 | Open — WP7 |
| **`expo-web-browser` and `expo-crypto` are dependencies with no importer** | Dead weight in the bundle | `code-reviewer`, WP6 | Open — **narrowed 2026-09-22: `expo-auth-session` now has an importer and is no longer part of this row.** The other two still have none. Remove or justify |
| **`ProviderEmailMissingError` covers two distinct failures** — "the provider returned no email" and "a first-time social signup arrived without date of birth" | A client cannot branch on them, so WP6 will show the wrong screen for one. They need opposite recoveries | Manager, WP2 | Open — **must land before WP6 builds against it**. Candidate for WP2.1 |
| Timing equalisation covers the missing-account path only — a wrong password on a real account still differs from a correct one by argon2 time | A narrow account-enumeration oracle. Closing it fully needs a constant-time envelope around the whole handler | Manager, WP2 | Open — accepted for now, revisit pre-launch |
| No password reset / email verification flow | A locked-out user has no recovery path. Survivable at promotional-launch scale, not at public launch. `email_verified_at` reserved in WP2 so adding it is not a backfill | Architect, WP2 planning | Open — pre-launch |
| **Adding a graph node cannot reach threads that already finished** | WP17 hit this: the run reached `END` before the CMS node existed, so `resume` did nothing **and reported success** — the worst shape of failure. Solved with an explicit `write-drafts --run-id`. **WP18 and WP19 both hit it again by construction**, since each adds nodes behind runs that already completed. Named in both handoffs so it is designed for rather than rediscovered | Pipeline Manager, WP17 | Open — a standing property of the architecture, not a bug to close |
| **`acquisition` participates in Payload's draft/publish versioning — it is not a flat column** | Found by WP15.1's device gate, which proved more than it asked. Editing the field in the admin UI and saving landed it in `_tracks_v` as the latest **draft** version while the published row stayed `undocumented`, and anonymous REST kept serving `undocumented`. **Consequence for WP17, and it is a good one:** the pipeline setting `acquisition` on a draft cannot change what readers are served until a human publishes. The provenance record and the publish gate are independent by construction rather than by discipline. Also noted: Payload made the column nullable-with-default, so `mapTrack`'s `?? 'undocumented'` fallback is genuinely reachable at runtime even though the generated type says otherwise — the generated type describes documents written since the column existed, not rows predating it | Manager, WP15.1 | ℹ️ **Not debt — a property to rely on.** Recorded here because WP17 needs to know it before its first Track write |
| **Payload has no role-based permissions — `Admins.ts` defines no roles at all** | Anyone who can review a draft can also publish it. **Not currently a vulnerability**: one person is writer, reviewer and admin, so this describes the founder publishing their own work, which is the intended workflow. It becomes real the moment a second human touches the CMS. Ruled 2026-08-27 as **not blocking WP19** — "the pipeline never publishes" is structural (the pipeline writes drafts; Payload's publish validation is independent of it), not dependent on roles | Pipeline Manager, WP17 | Open — **explicit trigger: a second CMS user.** Not before |
| **Grounding proves anchoring, not entailment** | A claim can cite a genuine passage that does not actually support it and pass the gate. The mechanical check verifies the handle was retrieved and the quote is verbatim; it cannot verify that the passage *argues for* the claim. Closing it needs an LLM judge — **a different instrument, and deliberately not folded into the mechanical gate**, whose whole value is that it cannot be argued with | Pipeline Manager, WP17 | Open — a candidate node for WP19, sized separately |
| **The measured Wattles Track predates the option shuffle** — its 18 Leaves still carry the position bias (correct option in B for 15 of 18, never C) | The content is otherwise sound, but the gate on those specific Leaves is answerable by always picking B. **Do not use this Track for anything a reader sees.** Regeneration is a ~25-minute rerun | Pipeline Manager, WP17 | Open — regenerate before this Track is shown to anyone |
| **Every pipeline measurement so far is one book** | Structure-check thresholds, prompt quality, model comparison, cost per Track and the 3–4 sticky-note clustering are all *The Science of Getting Rich*. Wattles is short, aphoristic and 1910 — a second, structurally different book is the cheapest way to find out which findings are about the pipeline and which are about this text | Pipeline Manager, WP16.1 + WP17 | Open — worth one run before WP20 commits to the numbers |
| **Track 42's cover still hotlinks a retailer's CDN — and that is now the *only* one of the three left** | **Re-verified against the live CMS 2026-09-11, and two of three are closed.** The purchase URL reads `https://gutenberg.org/ebooks/59844` — **the scheme is present, so the Track is no longer being silently dropped from Explore** (`launch-blockers.md` recorded that fix on 2026-09-02; this row did not, and the two lists disagreed for nine days). The disclaimer was never broken. **What remains is `coverUrl` pointing at `harivubooks.com`'s CDN** — someone else's image on someone else's infrastructure, on a published Track, in a product whose whole posture is care with other people's material, and **Track 42 sorts first in Explore** so it is the first card anyone sees. **It is a founder item rather than a package because it needs an asset that does not exist yet** — Gutenberg's own scan or a generated cover in the anchor style — and the 2026-08-28 ruling refused to let the pipeline auto-fill it. It becomes a package the moment there is something to point at. **Third stale-list correction in one day**, all three found by checking rather than reading | Pipeline Manager, WP20; corrected by Architect 2026-09-02 and again 2026-09-11 | 📤 **COVER-1, handed off 2026-09-22.** **Correction: it is not the only one** — Ikigai's cover hotlinks `taleaway.com`, verified 2026-09-22. Both are in COVER-1. |
| **`scenario_patch` omits `scenario.audio`, and `verify_siblings` cannot see the omission** | The scenario group has **four** fields — `prompt`, `options`, `image`, `audio`. The patch carries the first three and the sibling check compares the first two. **Unreachable today**: `audio` is `admin.hidden`, marked *"Reserved for Phase 2 voiceover. Unused in Phase 1"*, and nothing in the pipeline writes it — so a dropped null is a no-op. **The generalisable part is not the missing field, it is why the check could not catch it: `verify_siblings` was written from the same list of fields as `scenario_patch`, so it shares the patch's blind spot by construction.** A verification derived from the same enumeration as the write can only confirm the write did what its author intended, never that the enumeration was complete. The check that would catch it compares the whole group, or diffs the document. Related: `revised_leaf_patch`'s own docstring records that Payload's group-merge semantics were *"really only confirmed for one case"* — WP18 patched `stickyNotes.diagram` and `notes` survived, which **contradicts** WP19's hand demonstration that omitted siblings get nulled. Two observations disagree; both packages correctly took the paranoid path anyway | Architect, WP20.1 review | Open — low. **Explicit trigger: Phase 2 voiceover**, which is the first thing that populates `audio`. Fix the patch and widen the check in the same package |
| **"Use this candidate" writes `url` and `alt` but leaves `scenario.image.width`/`.height` untouched** | The two fields describe the *previous* image after a swap. **Not reachable today and deliberately not fixed**: the pipeline writes candidates as `{url, alt}` only, so nothing populates dimensions, and `SlideImage.tsx` lays out at a fixed `IMAGE_HEIGHT` regardless. **Bounded even when reached** — `content.ts` declares them "optional and advisory… their absence must never stop it rendering", so the failure is a reserved-space mismatch, not a broken image. Recorded because it is this project's signature shape in miniature: a partial write leaving a sibling that describes something that no longer exists | Architect, WP15.7 review | Open — low. **Explicit trigger: the first path that populates `width`/`height`.** Fix then, in the same package |
| **The answer-length guarantee is warn-only until a publish-time block exists** | Ruled 2026-08-29. Warning is correct for WP20 because the founder is the only reader and blocking at generation is unbounded. **It is not correct once a Track is visible to anyone else** — a Track that is 83% answerable without reading makes active recall decorative, and `PRODUCT.md` names active recall as the product thesis. **The fix is a publish-time check in Payload** (`apps/admin`, Manager), evaluating the Track's Leaves together, so a failing Track can still be generated and reviewed but cannot go live. **Explicit trigger: before any pipeline-produced Track is published where a real reader can reach it** | Architect, WP20 | Open — **blocking, not deferrable, at the point content becomes public** |
| **Raw-text retention is now human-triggered, not automatic** | Ruled 2026-08-29: `purge_raw_text` stays a command run after publish, because a run does not truly end at `write_drafts_to_cms`. R6 requires the raw text to go when a Track completes, and a manual command makes that a thing someone remembers rather than something that happens. **Mitigation required: a command listing runs still holding raw text**, so the question "what have we not purged" is a query rather than an act of memory — the same reasoning that put `acquisition` on Track. Until that exists, retention depends on discipline | Architect, WP20 | Open — mitigation wanted in WP20 or immediately after |
| **`onError` does not reliably fire on an `<img>` inside a Payload custom admin component** | Found in WP15.6 and confirmed three independent ways live — a logging handler, a manually-attached native listener, and a bare non-React `<img>` for comparison which *did* fire. The browser genuinely fails the load; no error event reaches anything listening in that render path. **Root cause unknown, and deliberately not chased** — that was the right call on a one-field package, since it leads into Payload/Next rendering internals with no bound on the time. Worked around by polling `complete`/`naturalWidth`, verified against both a DNS failure and a same-server 404. **Recorded so the next custom Payload component needing to detect a failed async resource knows going in rather than rediscovering it** — the failure mode is silence, which is the kind that reads as "my handler is wrong" for an hour | Manager, WP15.6 | Open — a known framework behaviour with a verified workaround, not a bug to fix |
| **The narrator beat has no graceful failure** | A failed samples fetch shows `ErrorState` with **Retry only** — no Continue — so an existing account (`narratorOnly`) cannot reach Tabs and a new one cannot reach pick-book. **Not new in kind** (ONBOARD-1's beat also dead-ended on a *thrown* fetch error; it degraded only on content gaps), **but the new `/content/narrator-samples` endpoint adds version skew: an old backend answers 404, so backend-before-mobile is now a hard deploy order**, and the founder's own `:3000` strands until it is restarted. The gate fails open by design and this screen does not. Same screen: a missing greeting file is a **dead card** — `playbackFailed` is not surfaced here and Payload answers a missing file with 500 (the window is a re-take: the founder deletes the Media document by hand, then uploads) | `code-reviewer` + Manager, ONBOARD-3 sign-off, 2026-09-25 | Open, **medium** — fix before any install that is not the founder's → **ONBOARD-3.1** (proposed): Continue stays available when the fetch fails |
| **The narrator beat's choice and audio handling** | (a) `selected` is seeded from `DEFAULT_NARRATOR` and Continue writes it back, so a repeat pass — reachable through the accepted quit-mid-first-Leaf design — **silently overwrites a stored choice** (the same for any account that picked in Profile before meeting this beat). (b) In `full`, Continue *pushes* pick-book over the beat, so a clip still playing finishes over the next screen (`narratorOnly` resets and is unaffected). (c) The one-voice rule keys on the player's reported `playing`, so a tap on the second card during the first clip's buffering window does not stop it. (d) Replaying a *finished* greeting is probably a dead tap — `useNarration` only calls `play()` — **not observed; a device question**. No test can see (b)–(d): the fake sets `playing` synchronously | `code-reviewer` + Manager, ONBOARD-3 sign-off, 2026-09-25 | Open, low-medium → **ONBOARD-3.1** (proposed) for (a) and (b); (c) and (d) only if the founder hears them at the gate |
| **Pick-book: `choose` and `skip` race** | `busy` disables only the pressed card, so Skip and the other card stay live across the two round trips (`addToLibrary`, then `listLibrary`). Choose then Skip: the reader lands on Explore with the flag set, then a late `reset` puts them into `[Tabs, LeafPlayer]` anyway. Two Choose taps add both books and land on the second. **The shape is ONBOARD-1's; ONBOARD-3 lengthened the window** by moving the Library lookup here. Untested | `code-reviewer`, ONBOARD-3 sign-off, 2026-09-25 | Open, low-medium → **ONBOARD-3.1** (proposed) |
| **A fresh worktree's root `npm run build` fails at `apps/admin`** | `next build` needs `PAYLOAD_SECRET` and `PAYLOAD_DATABASE_URL`. CI sets both (`ci.yml:23-24`, verified 2026-09-25) and a new worktree has neither, so a Manager's "build exits 1" is real and **is not a code defect**. Either the gate documents the two variables or admin's build stops needing them | Manager, ONBOARD-3 completion report, 2026-09-25 | Open, low |
| **ONBOARD-3's tests: three soft spots** | `navigation.test.tsx:513` asserts that Explore is not mounted and cannot tell that from *still loading*; `testing/firstLeaf.ts` casts its fixtures through `unknown`, so a change in the Leaf's shape will not surface there; nothing tests audio stopping on Continue | `code-reviewer`, ONBOARD-3 sign-off, 2026-09-25 | Open, low → **ONBOARD-3.1** (proposed), with the narrator-beat row |
| **First-run copy: two "fifteen minutes" lines and two factual slips** | `ExploreScreen.tsx:178` and `:255` still say a book is "about fifteen minutes" — the framing the founder objected to in beat 1. **They are the only two such strings in `apps/` and `packages/`** (grepped 2026-09-25; `ShareCard`'s footer tagline "15 minutes a day" is a habit claim consistent with the session cap, not this framing) and no test pins either. Separately, the drafted promise says a Leaf "ends with a question" — **the question is slide 2 of 5; a Leaf ends on sticky notes and the takeaway** (`PRODUCT.md`) — and the narrator beat says "Every Leaf can be read aloud", when only Ikigai has narration and slide 4 has no voice button. The words are the founder's | Manager (Explore) + `code-reviewer` (drafts) + Architect (grep), 2026-09-25 | Open → **ONBOARD-3.1** (proposed) with the founder's words |
| **Three pre-existing gaps ONBOARD-3 found and deliberately left** | (a) `WrapUp`'s ordinary exit says "Back to Journey" whatever the reader came from (Library, say); ONBOARD-3 relabelled only the closing's. (b) **The onboarding flag is per install, not per account** — a second account on the same install skips onboarding entirely (`introSeen` and the coach-mark flag are per install too). Harmless with one reader per phone; wrong on a shared device. (c) **`AppStack`'s five non-onboarding routes** (`LeafPlayer`, `TrackDetail`, `WrapUp`, `AchievementShare`, `TrackComplete`) **take the platform's default transition and ignore Reduce Motion** — verified 2026-09-25: the navigator's `screenOptions` sets no `animation` and only the four onboarding routes take the fade swap (`AuthStack` does too) | Manager, ONBOARD-3 completion report ("pre-existing, not touched"), 2026-09-25 | Open, low — (a) is a one-line label; (b) needs a decision about what "seen" is keyed on before a shared-device reader exists; (c) is the per-route pattern the onboarding routes now have, and `appStackReduceMotion.test.tsx` already discovers routes from the navigator, so it extends |
| **ONBOARD-2.1's residuals — named, and not fixed** | (a) **Dynamic access to `SpeechClient._call`** (`getattr`, a string-built attribute) — invisible to every static pin, and `_call` will speak any string. (b) **The Leaf door has no closed list**, so nothing at runtime can refuse a `NarrationLine`: `replace(line, **fields)`, an alias (`NL = NarrationLine`, `import … as`) called afterwards, and `copy` plus `setattr` are unseen by the static pins, which see only the literal `text=` keyword. (c) **A transport failure between the two greeting uploads** leaves one document behind — resumable (the re-run accepts the identical clip), not atomic. (d) **The twin render is still two copies** (~250 lines), now pinned by effect in both; extract once if a third caller needs the money handling. (e) **`tests/_greeting_preflight_rehearsal.py` is committed** — read-only by construction (raising `upload_media`, ceiling 0, a copy of the cache, the key from the environment); **delete it if it ever gains a write path** | Pipeline Manager + Architect, ONBOARD-2.1 sign-off, 2026-09-25 | Open, low — (a) and (b) are adversarial rather than the "reasonable instruction" the boundary exists to stop; revisit a type-level fence (a sealed type only the two doors can build) if the boundary should be structural |
