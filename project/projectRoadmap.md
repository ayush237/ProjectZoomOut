# Project Roadmap

Owned by Architect.

## Phase 1 status board

**Single source of truth for "what's done, what's next."** Architect updates this at every sign-off and every handoff.

**✅ Reached 2026-08-13.** Every Phase 1 mechanic works end to end: sign up, browse, add to library, play a Leaf, answer, unlock the payoff, earn XP, keep a streak, hit a graceful cap, wrap up, share, unlock achievements, report an error, and see the legal surfaces. Next: sequence `launch-blockers.md`.

~~Current goal: an end-to-end working app~~ — every Phase 1 feature working against seeded content on a device. That is **WP5b → WP9 → WP10**, three packages.

**Launch blockers live in `launch-blockers.md`, not here** (split 2026-08-12). Deployment, password reset, test hardening, real content, and every legal item are parked there so they stop competing with the build. Nothing in that file blocks current work.

Legend: ✅ done · 🔵 in progress · ⬜ not started · 🔒 blocked

### Work packages

| # | Delivers | Owner | Status | Waiting on |
|---|---|---|---|---|
| WP0 | Monorepo, shared types, Postgres, CI | Manager | ✅ Signed off 2026-08-06 (PR #1) | — |
| WP1 | Payload 3.x CMS — Track + Leaf collections, publish-time validation | Manager | ✅ Signed off 2026-08-07 | — |
| **GATE** | **Schema freeze — one structurally complete Leaf authored** | **Founder** | ✅ **Closed 2026-08-08** — 4 findings, all ruled | — |
| WP2 | Backend foundation — auth (email/Apple/Google), age gate, profile | Manager | ✅ Signed off 2026-08-07 | — |
| WP2.1 | Schema-freeze alignment (4 gate rulings) + small backend gaps (logout, error split, token reaping) | Manager | ✅ Signed off 2026-08-08 (12/12) | — |
| WP3 | Content API — Explore, Library, Track/Leaf delivery | Manager | ✅ Signed off 2026-08-08 (11/11) | — |
| WP4 | Learning loop API — answer, unlock payoff, complete Leaf, award XP | Manager | ✅ **Signed off 2026-08-09 (12/12)** — reviewed by `code-reviewer`, no blocking defects | One ~10-line test to add before merge |
| WP5a | Session cap and streaks — server-authoritative | Manager | ✅ **Signed off 2026-08-12 (7/8)** | 8th is a device check, blocked on the env fix in WP5b |
| WP5b | Env fix, achievements, total XP | Manager | ✅ **Signed off 2026-08-12 (9/9)** — Part A resolved, verified by query | Founder to push the branch |
| ~~GATE~~ | ~~Device verification~~ | Manager | ✅ **Done in WP5b** — 7 items pass, first since WP5a | — |
| WP6 | Mobile shell — navigation, auth screens, age gate, design system | Manager | ✅ **Signed off 2026-08-11 (14/15)** after a rejected first pass and a clean second | 15th void — Apple/Google dormant |
| WP7 | Mobile surfaces — Explore, Library, Journey, Profile | Manager | ✅ **Signed off 2026-08-11 (11/11)** after a rejected first pass | — |
| WP11 | Seed fixture — full-length placeholder Track (~20 Leaves) | Manager | ✅ **Signed off 2026-08-12 (10/11)** | 11th deferred to WP8, where it becomes verifiable by tapping |
| WP8 | Mobile Leaf player — 5 slides, unlock gate, SFX | Manager | ✅ **Signed off 2026-08-12 (10/11)** — the loop works on a device | 11th is a **founder** device check: reduced-motion, and how the unlock feels |
| WP9 | Shareable session wrap-up + achievement screens | Manager | ✅ **Signed off 2026-08-13 (10/10)** | — |
| WP10 | Report-an-error, fix queue, legal surfaces | Manager | ✅ **Signed off 2026-08-13 (9/10)** | — |
| — | **🏁 PHASE 1 COMPLETE — the app works end to end** | — | ✅ **2026-08-13** | — |
| WP15 | Leaf v2 — scenario image, sticky-notes diagram, apply-in-life | Manager | ✅ **Signed off 2026-08-13 (10/10)** | — |
| WP16 | Pipeline skeleton — ingest, analyze, breakdown, human gate 1 | **Pipeline Manager** | ✅ **Signed off 2026-08-26 (12/12)** — reviewed against the diff, not the report | Merge to `main` |
| WP16.1 | Breakdown prompt + model measurement with n≥3 | **Pipeline Manager** | ✅ **Signed off 2026-08-27 (6/6)** — structure check 1-of-3 → 8-of-8; models measured indistinguishable | — |
| WP17 | Leaf generation, grounding, and the Payload draft boundary | **Pipeline Manager** | ✅ **Signed off 2026-08-28 (10/10)** — both halves; merged as PR #19 | — |
| WP15.1 | Track `acquisition` field + the red typecheck on `main` | Manager | ✅ **Signed off 2026-08-27 (7/7)** — root `typecheck` green for the first time since WP15 | — |
| WP15.2 | Publish-incapable API key for the pipeline + admin `allowedDevOrigins` | Manager | 📤 **Handed off 2026-08-28** | — |
| WP18 | Assets — scenario images, diagram specs, one visual identity | **Pipeline Manager** | ✅ **Signed off 2026-08-29 (11/11)** — Track 42 fully illustrated, $2.34 against credit | Merge |
| WP15.2 | Publish-incapable API key + admin `allowedDevOrigins` | Manager | ✅ **Signed off 2026-08-29 (8/8)** — merged as PR #20 | `delete` scoping + the pipeline-side switch, both handed off |
| WP19 | Gate 2, the editorial reviewer, and the answer-length check | **Pipeline Manager** | 🟡 **8/11 verified 2026-08-30** — the 3 open are blocked on WP15.4, correctly | **WP15.4** (Manager) |
| WP15.3 | Remove `delete` from the machine account | Manager | 📤 **Handed off 2026-08-29** — not started | — |
| WP15.4 | Gate 2's three fields on Leaves | Manager | 📤 **Handed off 2026-08-30** | **Blocks WP19's last 3 criteria** |
| WP20 | One book end to end — a published Track the pipeline produced | **Pipeline Manager** | ⬜ queued | WP19 |
| — | *WP12 deployment · WP13 password reset · WP14 test hardening* | Manager | ⏸ **parked** | see `launch-blockers.md` |

### Blocked on the founder

> **The live list moved to `launch-blockers.md` Part 3 on 2026-08-27.** Keep it there and nowhere else. Two lists drifted apart — this one still said "GCP account, do this now" after the credit was already being spent, and still carried a device gate closed in WP5b. A stale blocker list is worse than none, because it costs the reader trust in every row including the true ones.

<details><summary>Phase 1 archive — closed items, kept for the record</summary>

| Item | Cost | Blocks | Status |
|---|---|---|---|
| ~~Merge WP1 and WP2 to `main`~~ | — | — | ✅ PRs #2 and #3 merged |
| ~~Schema-freeze gate~~ | — | — | ✅ **closed 2026-08-08** |
| ~~Report gate timing~~ | — | — | ✅ 5 min mock / 15–25 min real. Estimate revised down |
| ~~Rule on save-vs-publish authoring UX~~ | — | — | ✅ Helped — stays on save |
| **Confirm how Phase 1 content gets drafted** — ad-hoc AI assistance vs pulling the Phase 2 pipeline forward | minutes | The Phase 1/Phase 2 boundary, and the Critic-in-the-Loop review standard | ⬜ |
| ~~Visual design direction~~ | — | — | ✅ **Approved 2026-08-08** — `proposals/design-direction.md` |
| **Fix the gate content in the admin UI** — add `publisher` + `coverUrl` to the Track, add a locator to the Leaf's source reference, re-save both | ~2 min | **WP3** — the mapper rejects both records as they stand | ⬜ **do this now** |
| ~~Achievement list~~ | — | — | ✅ **Delegated to Architect and written 2026-08-09** — `proposals/achievements.md`, 19 achievements |
| ~~Install Xcode~~ | — | — | ✅ **Installed 2026-08-08.** Simulator verification appended to WP2.1 |
| ~~Register OAuth clients~~ | — | — | ✅ **Not needed — social sign-in deferred 2026-08-11.** Email/password only for Phase 1 |
| **GATE: device verification session** — its own slot, not the tail of a package | ~10 min | Closes deferrals from **three** packages | ⬜ **after WP5b**. Slipped twice because it was always the tail of something larger; given its own slot for that reason |
| ↳ 1. Restart the backend | 1 cmd | Closes WP5b Part A's last criterion — the running process still points at the old database |  |
| ↳ 2. Play a Leaf to the cap | | Does "That is today done" read as an ending or a refusal? |  |
| ↳ 3. Unlock an achievement | | Does the moment land? |  |
| ↳ 4. iOS Reduce Motion on, replay the unlock | | Fade rather than spring — **WP8's open 11th criterion** |  |
| ↳ 5. Both themes at XXXL on the new screens | | Carries the three XXXL items from WP11 |  |
| **Narrow `Read(**/.env.*)` in `.claude/settings.json`** | ~1 min | Three incidents so far; see the debt register | ⬜ recommended |
| ~~Play a Leaf on a device~~ | ~30 sec | **Closes WP8's 11th criterion and settles the auto-advance ruling.** Does the unlock feel earned or like a page turn? Then repeat with iOS Reduce Motion on: does it fade rather than spring? | ✅ closed in WP5b/WP8's device gates |

</details>

### When the founder can try it

Four distinct moments. Sequence, not dates — there is no velocity data and no target launch date yet.

| Moment | After | What you can actually do | Still missing |
|---|---|---|---|
| **Author content** | WP1 | Log into the CMS and build a Leaf. Real software you use, and it *is* the schema-freeze gate | Everything user-facing |
| **Exercise the loop headless** | WP4 | Hit the API from the terminal: fetch a Leaf, submit a wrong answer, submit the right one, watch the payoff unlock and XP land. Validates the core mechanic before any UI exists | All UI |
| **Hold the app** | WP6 | Install on your own phone, sign up, pass the age gate, navigate the four surfaces | Content, the Leaf player — the surfaces are empty shells |
| **Test the product** | WP8 | The real thing: read a summary, answer a scenario, unlock the payoff, complete a Leaf, feel whether the thesis works | Session cap, streaks, share screens (WP5, WP9) |

**WP8 is the answer to "when can I judge whether this works."** Chain to reach it: WP1 → GATE → WP2 → WP3 → WP4 → WP6 → WP11 → WP8. WP6 is the pinch point, and it is blocked on founder items (design direction, Xcode), not on Manager.

### Where to look for what
- **This board** — current state, at a glance
- `projectplan.md` — the one work package in flight, in detail
- `collaboration-log.md` — every handoff and completion report, newest first
- `proposals/phase-1-implementation-plan.md` — the full milestone plan and its reasoning
- **`launch-blockers.md`** — everything between a working app and a shippable one. Parked, deliberately

## Vision
ZoomOut turns non-fiction books into gamified, interactive micro-lessons that build lasting retention — reclaiming screen time from algorithmic feeds with focused, ~15-minute sessions that require active engagement instead of passive consumption.

## Milestones
| Milestone | Target | Status |
|---|---|---|
| Phase 1 — core learning loop, gamification, all app surfaces, manual content admin tool | MVP / promotional launch | **In progress** — plan approved 2026-08-06, see `proposals/phase-1-implementation-plan.md` |
| Phase 2 — AI content pipeline (Gemini/Vertex AI, ElevenLabs) | Post-MVP | Not started |
| Phase 3 — social ("Reading Circles") | Post-MVP | Not started |
| Phase 4 — monetization | Last | Not started — deliberately deferred to the final phase |

## Backlog
| Feature | Priority | Status | Notes |
|---|---|---|---|
| WP0 — Monorepo scaffolding (npm workspaces, TS strict, shared types, Postgres + migrations, CI) | P0 | ✅ **Done** — signed off 2026-08-06, PR #1 | 9/10 criteria verified by execution, CI green. 10th carried forward, see below |
| Verify Expo app boots in the iOS simulator | P0 — gates WP6 | 🔵 Appended to WP2.1 (2026-08-08) | Xcode now installed. Was the last open WP0 criterion |
| WP1 — Content admin tool: Payload 3.x CMS setup | P0 | **Handed off** 2026-08-06 | Blocks all content — see `PRODUCT.md` Phased scope. Approval workflow deferred; Payload makes it a config change later |
| **GATE — schema freeze** | P0 — **blocks everything downstream of WP1** | Not started | ~45 min of **founder** time: author one structurally complete Leaf through the real Payload editor, placeholder prose, no research. Nothing downstream starts until the schema is signed off. See plan §5 |
| User auth & profile management | P0 | Not started | |
| Core learning loop (5-slide Leaf UI, unlock-on-correct-answer logic) | P0 | Not started | |
| Gamification (streaks, XP, badges, SFX) | P0 | Not started | |
| Session cap / positive friction (15 min or 500 XP) | P0 | Not started | Intentional constraint, not a bug |
| Explore / Library / Journey screens | P0 | Not started | |
| Shareable session-wrap-up & achievement screens | P1 | Not started | Growth-loop mechanic |
| "Report an error" flow + fix queue | P1 | Not started | Legal requirement — see `PRODUCT.md` Content integrity |
| Purchase-forward / affiliate links | P1 | Not started | |
| Age-gating / minor protection | P0, pre-launch blocker | Not started | Threshold owner: TBD (legal) |
| AI content pipeline (book → Leaves via Gemini/Vertex) | Phase 2 | Not started | Source of launch content, but built **after all app and admin work completes** (decided 2026-08-08). Needs its own brainstorming cycle. Separate Python service. **No Architect proposal until Phase 1 app work is done** |
| Social / Reading Circles | Phase 3 | Not started | |
| Monetization / subscription | Phase 4 | Not started | Deliberately last. Model still undecided — see Open items in `PRODUCT.md` |
| Launch library authoring — "The Mountain Is You" (~20 Leaves) | P0, pre-launch gate | Not started | Deferred off the build critical path 2026-08-06; app is built against placeholder content. ~10–15 hours of solo writing still required before launch |
| Replace all placeholder content | P0, pre-launch blocker | Not started | `isPlaceholder` records must never reach production — see proposal §3.4 |

## Decisions log
| Date | Decision | Why | Alternatives rejected |
|---|---|---|---|
| 2026-08-06 | Mobile: React Native + Expo, TypeScript | Cross-platform velocity for a small team; strong animation/gamification ecosystem (Reanimated, Lottie); shares TypeScript with backend | Flutter — legitimate alternative, smaller hiring pool; native Swift/Kotlin — 2x build/maintain cost for an early-stage team |
| 2026-08-06 | Backend: Node.js + TypeScript | Unified language with mobile; fast to build CRUD, gamification logic, and the Phase 1 admin tool | Python for everything — better AI/ML tooling, but weaker fit for general app backend work |
| 2026-08-06 | Phase 1 content: manual authoring via an internal admin tool | AI pipeline is explicitly Phase 2; the app needs content to launch with | Waiting for the AI pipeline (delays launch indefinitely); semi-automated generation (premature — no pipeline to assist yet) |
| 2026-08-06 | AI pipeline (Phase 2) will likely be a separate Python service, not part of the Node backend | Best tooling fit for Gemini/Vertex AI/ElevenLabs orchestration; isolates a batch/pipeline workload from the request-serving app | Building it in Node/TypeScript — workable but weaker ecosystem for this specific workload |
| 2026-08-06 | Wrong answer on slide 2 → unlimited retry, slide 3 stays locked until correct | Simplest state machine; with only 3 options no user is permanently blocked, so retention risk is low. Stakes move to XP, not access | Limited attempts then reveal (needs attempt-tracking + penalty rules); wrong-still-advances (guts the active-recall differentiator); Duolingo-hearts (churn risk) |
| 2026-08-06 | No Branch layer — Track → Leaf is the real structure | The brief's "Tree/Branch/Leaf" was legal framing, not a functional spec. Originality argument rests on Leaves not mapping 1:1 to chapters, which Track → Leaf already satisfies | Building Branch now (schema + admin + Journey UI cost for no functional gain); nullable `branch_id` reserved for later (half-built taxonomies stay half-built) |
| 2026-08-06 | Voiceover deferred to Phase 2 | ElevenLabs already lives in Phase 2; pulling it forward adds a vendor, per-minute cost, audio storage/CDN and a DPA to the MVP. Schema reserves a per-slide audio reference so it's a later migration, not a redesign | Ship ElevenLabs in Phase 1; on-device TTS stopgap (robotic voice undercuts a product selling polish) |
| 2026-08-08 | **Phase 1 launch content will be produced by the AI pipeline**, not hand-written | Founder's call. Changes the Phase 1/Phase 2 boundary: the pipeline is no longer strictly post-MVP, it becomes the source of launch content. **Consequence:** DPAs with Gemini/Vertex and `LEGAL.md`'s "IP counsel review of actual generated content" move from post-launch to pre-launch, and Critic-in-the-Loop stops being a process description and becomes the control standing between generated text and a fabricated-quote incident | Ad-hoc AI drafting with human fact-check (no pipeline built); fully manual authoring (cleanest legally, ~10–15 hrs/Track) |
| 2026-08-13 | **The device check becomes a named gate in every handoff**, above the acceptance criteria rather than the last line of them | It is the only step that crosses the whole path, and it is where **three** packages stopped being finished: WP10 shipped legal surfaces rendered nowhere, WP11 had a flagship Track invisible behind pagination, WP15 had a backend mapper dropping every new field with 932 tests green. Last in a list is what gets squeezed | Leaving it as a criterion among criteria |
| 2026-08-13 | **A maximal-fixture contract test lives in `apps/backend`, against real Payload — the deliberate exception to WP3's stand-in ruling** | **Optional fields are invisible when dropped**: a missing required field is a validation error on the first request, a missing optional one is indistinguishable from content that legitimately has none. Architect chose optional to avoid a backfill across 28 Tracks — right, and exactly what created the blind spot. The stand-in is correct for behaviour we control and **wrong for a contract test whose purpose is detecting drift between two real systems**, because the fake inherits the same blind spot as the code under test. One Leaf with every optional field populated, asserted end to end, catches every additive field from now on | A stand-in-based test (same blind spot); a separate e2e workspace (the backend is the seam and where the defect was) |
| 2026-08-13 | **An asset URL with no alt text is passed through, not repaired** | Dropping it would make the mapper agree with the CMS by staying silent — the exact failure two independent gates exist to prevent. A published asset lacking alt means a gate is not running, and that must be visible rather than smoothed over | Dropping the asset; substituting a default alt |
| 2026-08-13 | **`url` and `alt` are trimmed before the schema sees them** | `min(1)` counts a single space as content, and this mapper also reads rows the **Phase 2 pipeline writes directly**, which never pass through Payload's `beforeChange` trim hook. Untrimmed, a space-only alt satisfies both gates and reaches a screen reader as silence | Relying on the CMS hook (which the pipeline bypasses) |
| 2026-08-13 | **Apply-in-life is always visible, not collapsed like Dinner Table Knowledge** | DTK is a discovery and earns its tap; a call to action behind a tap is one most readers never see | Matching DTK's disclosure for consistency |
| 2026-08-13 | **The non-endorsement disclaimer and purchase links were rendered nowhere in the app** — WP10's §3 was a missing feature, not a verification | `trackSchema` required them, WP3 made a Track unservable without them, and the API returned them on every response. The only occurrence anywhere under `apps/mobile/src` was a **test fixture**. Four packages of enforcement, zero readers reached. **`LEGAL.md`'s fair-use position rests partly on those disclaimers being seen**, so this was a launch-blocking legal defect found in the last package. Same shape as the payoff shipping ungated: a guarantee enforced at one layer and assumed at the next | Leaving it — the obligation cannot be met without a surface |
| 2026-08-13 | **`TrackLegal` keeps the disclaimer and purchase links inseparable** | A purchase link standing alone is exactly the affiliate-looking surface the disclaimer exists to disown. Better than what the handoff specified | Rendering them independently; a disclaimer on every card in a scrolling list |
| 2026-08-13 | **`ErrorReport.trackId` is denormalised at filing time** | A takedown pulls a Track, so resolving Leaf → Track at triage would query the CMS about content that may be gone — and **the report about withdrawn content is the one most worth reading** | Resolving at triage (fails exactly when it matters) |
| 2026-08-13 | **`ErrorReport.user_id` is `on delete set null`, not cascade** | A deleted account must not erase evidence that a factual claim was disputed. It also answers a GDPR question nobody had asked: erasure removes the personal link while the report survives | Cascade delete (destroys the audit trail a deletion request happens to touch) |
| 2026-08-13 | **The moderation operator token fails closed** — unset config refuses everyone | The WP2 precedent. The opposite default would publish every reader-submitted report on any deployment that forgot the variable | Defaulting to open when unconfigured |
| 2026-08-13 | **`SessionSummaryService` sits above `ProgressService` and `ContentService`, not inside either** | `ProgressService` cannot depend on `ContentService`, because content already depends on progress through `PayoffAccessPolicy` — a method on `ProgressService` would have closed that cycle. Composition services sit above; domain services do not reach sideways. Same shape as `LibraryService` | A method on `ProgressService` (closes a dependency cycle) |
| 2026-08-13 | **The session summary reads content through `ContentService`, never the repository** | The summary names books in an image built to be posted in public, so the placeholder guard and the takedown cascade must reach it. A Leaf whose Track has been withdrawn drops out and the rest of the day survives. Nobody specified this and it is obviously correct | Reading the repository directly (faster, bypasses both guards) |
| 2026-08-12 | **Wrapping up is a ceremony, not a lock** | It shows the summary, records `session_wrap`, and returns the reader to Journey — it does **not** prevent further learning. The daily cap is the hard stop; "wrap up" is the ritual ending. Locking someone out because they tapped a celebratory button would be punitive and surprising, and a reader who wraps up then wants one more Leaf should get one | Ending the day on wrap-up (punitive, and duplicates the cap) |
| 2026-08-12 | **The cap screen leads into the wrap-up screen** | Two differently-styled endings to one day is worse than one good one. WP5b's cap screen already unlocks `daily-cap` above the notice, and that ordering is what makes it read as an ending rather than a refusal — keep it | A separate cap ending and a separate wrap-up ending |
| 2026-08-12 | **The session cap does not interrupt** — a reader under the cap when they open a Leaf is paid in full for it even if finishing crosses the line; the next Leaf earns nothing | Refusing the completion would discard work already done in order to enforce a *wellbeing* constraint, which inverts the feature's purpose. This satisfies "an in-progress Leaf finishes" and "XP past the cap is not awarded" simultaneously, and leaves `calculateLeafXp` ignorant of the cap. Consequence, and it is fine: a reader can end the day slightly over 500 XP. The cap exists to stop a session, not to balance a ledger | Refusing the completion (cruel); awarding partial XP (arbitrary and unexplainable to a reader) |
| 2026-08-12 | **Cap thresholds travel with the session state, not in the client** | The limit screen must say "500 XP", and a client holding that number independently goes stale the first time the cap moves — the same argument as the configurable age threshold | Hardcoding thresholds in the app |
| 2026-08-12 | **`GET /progress/today` takes no date parameter** | A client supplying its own date could reset its cap by changing the device clock | Accepting a client date for flexibility |
| 2026-08-12 | **`delivery.ts` rule sharpened: additive changes proceed with a note; changing or removing a field needs a ruling** | "Treat it like frozen" was too blunt and would have forced an unnecessary round trip on a purely additive change that both apps compiled against | Requiring a ruling for every change (slow, no safety gain) |
| 2026-08-12 | **A gitignored `apps/backend/.env` plus `--env-file` on dev and migrate scripts** | Standard practice, not a compromise: the password lives only in the untracked file and `.env.example` already documents every variable. Environment living in one shell has now blocked two packages and would have blocked WP12 | Continuing to require an exported `DATABASE_URL` (blocks every fresh clone and every device check) |
| 2026-08-12 | **`DeliveredLeaf`, `AnswerOutcome` and `CompletionOutcome` move to `packages/shared/src/delivery.ts`** | Required, not merely permitted: `CLAUDE.md` forbids redefining a shape across `apps/mobile` and `apps/backend`, and WP8 made mobile the second consumer. A **new module** rather than an edit to frozen `content.ts`, with the backend re-exporting so no call site changed. **Condition: `delivery.ts` is a cross-workspace contract — changes break two apps at once and deserve `content.ts`-level care even though it is not frozen** | Duplicating the shapes in mobile (violates the rule the shared package exists to enforce); widening `content.ts` (breaks the freeze) |
| 2026-08-12 | **Explore pagination is a WP8 item, not WP14** | The corpus now crosses the boundary — Explore shows twenty of twenty-eight Tracks and stops dead with no affordance. WP8 is the package where the founder judges the product on a device, and a silently truncating Explore is a defect they hit in the first thirty seconds | Deferring to WP14 (leaves a visible defect through the one session that matters most) |
| 2026-08-12 | **SFX ships as a swappable asset map with no assets** | Sourcing and licensing sound is an open founder item of unknown cost, and blocking the Leaf player on it would stall the critical path for polish. The sound layer, its trigger points and the mute/silent-switch behaviour are all built; adding real files later is a drop-in | Blocking WP8 on assets; skipping the sound layer entirely (retrofitting trigger points across a finished player is worse) |
| 2026-08-12 | **`create-admin` operator script approved** — outside the WP11 handoff | Payload's create-first-user screen works exactly once per database with no email-based recovery, and the package was locked out mid-flight. WP12 needs it on a fresh production database regardless. **Condition: it is a privilege-escalation path — it must not be invocable from the deployed application, and WP12 must treat it as an out-of-band operator command, never a route or a startup hook** | Manual SQL to recover (worse and undocumented); leaving the trap in place |
| 2026-08-12 | **Unpublish the schema-freeze gate Track, do not delete it** | It carries placeholder prose under a real author's name (the §3.4 hazard), fails the new cover rule, and is live only because it predates that rule — so it cannot be re-saved without being fixed. Its purpose ended when the gate closed, and the seed now supplies 28 Tracks. Keeping the record preserves the gate's history | Deleting it (loses the record); fixing it (work on content with no remaining purpose) |
| 2026-08-11 | **Takedown cascades through a single `resolveVisibleLeaf`**, shared by `ContentService` and `ProgressService` | Unpublishing a Track did not withdraw its Leaves: `GET /content/leaves/:id` still served the full Leaf and the progress endpoints still graded and paid XP for it. **One shared resolver rather than a check at each call site, because two copies of a takedown rule is how one gets missed — which is exactly what had happened.** The 404 names the Leaf, never the Track | A check duplicated at each call site (the state that produced the bug); a CMS cascade hook (leaves the backend depending on CMS state being right) |
| 2026-08-11 | **A test-layer split can hide a defect from both layers** — removing the parent-Track check fails six unit tests and **zero** integration tests | The fake CMS makes an unpublished Track vanish entirely, so integration exercised the deleted-Track branch while the unit tests exercised the draft/placeholder branch. Both are real paths, neither layer covers both — the likely reason this survived WP3 and WP4. **Lesson: when a fake simplifies a state the real system represents differently, the fake defines a coverage boundary, not just a convenience** | Treating a green suite across two layers as coverage of the union of their paths |
| 2026-08-11 | **A comment that contradicts the code is evidence the code is wrong, not just the comment** | The `EmptyState` comment claimed `Icon` cancels OS font scaling. Review called the behaviour correct and the explanation misleading; **Architect downgraded it to a cosmetic nit. Both were wrong** — `Icon` divided by `fontScale` to cancel a multiplication that never happens, so every icon shrank ~40% at XXXL. It was visible in earlier screenshots and misattributed to a stale render | Treating a stale comment as a documentation defect |
| 2026-08-11 | **`TrackProgressSummary` is derived per request, never stored** | A counter on `user_tracks` would be a second source of truth that drifts the moment a Leaf is added or taken down — and content changing underneath progress is normal here, not exceptional. Same reasoning as the ruling that total XP is derived on read | A maintained counter (fast, drifts, unauditable) |
| 2026-08-11 | **`LibraryService` composes the rollup, not `ProgressService`** | Progress fetching its own content would have gone through `ContentRepository` and skipped the `isProductionPublishable` guard — the exact bypass WP4 had to guard against. Composition belongs on the side that already holds the content boundary | `ProgressService` fetching content (bypasses the placeholder guard) |
| 2026-08-11 | **Tiered testing bar — velocity over depth until the app is functional end to end** | Founder's call: development is the priority, and the highest-value information available is whether the product thesis holds, which needs WP8. Cut by category rather than across the board, because the record shows what actually caught defects here: **running the app** (the `+05:30` timezone bug, the app pinned to light mode, emoji glyphs ignoring tint, the WP6 font-scaling clip) and **a handful of invariant tests**, not permutation matrices. Tier A (payoff gate, `isCorrect` containment, takedown, placeholder guard, award idempotency, age gate, local-date logic, migrations) is retained — roughly fifteen tests, so cutting them buys almost nothing and they are the ones that cannot be paid for later. Tier C defers to **WP14**, scheduled rather than assumed. Manual device verification becomes mandatory in exchange, and `code-reviewer` runs per package — it costs no wall-clock and has already caught a double-refresh window and a tautological test | A blanket reduction (drops the invariants that are expensive to retrofit); keeping the uniform bar (slower to the only information that matters right now) |
| 2026-08-11 | **A rejected package's corrections go in an addendum, not a silent edit** | WP6's first-pass report claimed a falsified criterion as verified and asserted a false premise about animation. Manager corrected both by appending to the log entry, leaving the wrong claims and their correction both on the record. A silently edited report is indistinguishable from one that was right the first time, which destroys the log's value as the durable record | Editing the original entry in place |
| 2026-08-11 | **New tests for a rejected finding must be mutation-checked** | WP6's second pass proved each new test fails when — and only when — the behaviour it covers is broken. The first pass shipped a tautological failed-refresh test that passed with the handler deleted entirely. This is now the standard for any test written to close a review finding | Trusting a green test as evidence it pins anything |
| 2026-08-11 | **`refreshProfile()` added to `AuthContext`** | Needed to exercise the session-expiry wiring through a real request rather than a synthetic one, and WP7's pull-to-refresh needs it regardless — a testability-driven addition with a real consumer, not test-only API surface | Test-only hooks into the provider |
| 2026-08-11 | **Phase 1 ships email/password sign-in only. Apple and Google deferred** | Founder's call. Removes the OAuth-registration blocker entirely, and — because **Apple only requires Sign in with Apple when you offer another third-party social login** — offering neither means the App Store requirement never applies. Costs signup conversion, which matters less at promotional-launch scale. **No code change needed**: the buttons are already hidden without a client id and unconfigured providers already fail closed. **Consequence: password reset becomes the only account-recovery path and its priority rises to pre-launch blocker** | Registering both clients now (an hour of console work plus an App Store constraint, for conversion that does not matter yet) |
| 2026-08-11 | **Keep the in-house Node/Fastify/Postgres backend. Not adopting Firebase** | It is already built, tested and reviewed across WP0/WP2/WP3/WP4 — auth, content delivery, the payoff gate, grading, XP. Firebase Auth would replace roughly one package's worth of that while moving user identity outside our Postgres, complicating the progress and XP joins and the age gate. The one genuine gap it would close — password reset and email verification — is really "outbound email", solvable with a transactional email provider at a fraction of a migration | Firebase Auth + Firestore (discards working verified code, splits the data model); Firebase Auth only (still moves identity out of Postgres) |
| 2026-08-11 | **`AppError.responseFields` — opt-in, empty by default** — lets `SIGNUP_DETAILS_REQUIRED` serialise its `missingFields` | The field existed and its own comment said it was "part of the contract, so WP6 can jump straight to the right input", but `app.ts` serialised only `{ code, message }` and dropped it — leaving a WP6 criterion unmeetable. Opt-in rather than a blanket rule because `ContentInvalidError.reasons` names CMS fields and must never leak. `missingFields` also moved from prose to machine-readable names: `['your date of birth']` would have forced the client to string-match against copy we are free to reword | Serialising all error properties (leaks internal detail); client string-matching on `message`; leaving the criterion unmet |
| 2026-08-11 | **`jest-expo` in `apps/mobile`; Vitest everywhere else** | Rendering RN under Vitest means owning the Flow transform and every native-module mock across SDK upgrades. `jest-expo` is versioned against the SDK and ships exactly that. Cost: two runners in one repo | Vitest with a hand-rolled RN transform (ongoing maintenance against every Expo SDK) |
| 2026-08-11 | **React Navigation, not Expo Router** | Expo Router wants a file-based `app/` tree and would have restructured the `src/navigation/` and `src/screens/` layout the handoff specified, with no functional gain here. Note for WP9: React Navigation supports deep-link config, which shared wrap-up screens will want | Expo Router (restructure for no benefit at this stage) |
| 2026-08-11 | **A network failure during token refresh does *not* sign the reader out** | The criterion said "a failed refresh clears the session"; Manager split it correctly. A server *rejecting* the token ends the session; a dropped connection throws `NetworkError` and keeps it. Clearing the keychain on a tunnel would log people out on the underground | Treating all refresh failures alike (the literal reading of the criterion, and wrong) |
| 2026-08-11 | **Light theme has its own measured values — no shared base palette** | `#3DDCC8` on white is 1.6:1. A shared base palette the two themes reference is precisely the mechanism by which a dark-tuned accent lands on a light surface | Deriving light from dark by lightness inversion |
| 2026-08-09 | **`ContentService.getLeaf` takes a reader id and gates the payoff** — WP4 was right to change WP3's endpoint | The WP4 acceptance criterion "the payoff is unobtainable by any route" could not be met inside WP4's stated scope, because the offending endpoint was WP3's and `toPublicLeaf` lives in frozen `content.ts`. **This was an Architect handoff error, not a Manager scope breach** — the contradiction was found, the minimal fix taken, and it was reported at the top of the report | Serving the payoff only from progress endpoints (two calls to render one screen, Leaf shape split across two modules for WP7 to reassemble) |
| 2026-08-09 | **`PayoffAccessPolicy` is declared in the content module and implemented by `ProgressService`** — content → progress via an interface | Dependency inversion in the correct direction: content is the module that must not leak the payoff, so content owns the question "is this unlocked?" without knowing what unlocking involves. Neither service imports the other's concrete class; the composition root is the only place both names appear. The reverse direction would make progress depend on content-delivery concerns and put the gate in the wrong module | Progress importing ContentService; gating inside the route handler; a shared mediator |
| 2026-08-09 | **`correctAt` added to `LeafProgress`** in `packages/shared/src/progress.ts` | The frozen shape could not express unlock state: `firstTryCorrect: false` is true both for a reader who never answered and for one correct on the third attempt, and those two must not get the same payoff access. Nullable timestamp rather than boolean because WP5 wants to know *when*. `content.ts` untouched — the freeze held | A boolean `hasAnsweredCorrectly` (loses the timestamp WP5 needs); deriving it from `attemptCount` (cannot distinguish) |
| 2026-08-09 | **`completedLocalDate` is a column but deliberately NOT in the shared type** | WP5 groups streaks and the cap by local day, so it is computed once at completion with `localDateIn(user.timezone)`. Projecting it would invite the mobile app to derive "today" from it and reintroduce exactly the drift plan §3.5 warns about | Including it in the domain shape "for completeness" |
| 2026-08-09 | **XP = 80 base + 20 first-try bonus** (founder-approved) | Five first-try Leaves hit WP5's 500 cap exactly; a reader needing a second attempt each time takes six to seven. **WP5 must constrain against these numbers, not restate them.** Note the interaction: at ~3 min/Leaf the 15-minute cap also binds near five Leaves, so both caps bite in the same place for a strong reader while a struggling reader gets slightly more content per session — that asymmetry is intended, not a bug to fix | Other splits; a flat award with no first-try bonus (removes the reason to think before answering) |
| 2026-08-09 | **Total XP is derived on read**, not maintained on `users.total_xp` | Correctness over premature optimisation. A denormalised counter that drifts from `leaf_progress` surfaces as "my XP is wrong" with no way to reconstruct truth, and the idempotent-completion path is exactly where an increment would be double-applied. Cheap at Phase 1 scale with an index on `user_id`; revisit only if profile reads become hot | `users.total_xp` maintained on write (fast reads, drift risk, harder to audit) |
| 2026-08-08 | A scenario option **without a Payload row id is rejected**, never given an index-derived id | An index changes meaning the moment an author reorders options, silently turning a correct answer wrong with no error anywhere. Being over-strict costs a withheld Leaf plus a logged error — visible and fixable. The alternative costs silent mis-grading. Verified the live CMS issues hex row ids, so this rejects only genuinely broken documents | Deriving an id from array position; accepting the option unidentified |
| 2026-08-08 | Integration tests run against a **controllable Payload stand-in**, not the real CMS | The behaviour under test is ours — cache TTL, placeholder filter, mapper, 503 path — and each needs content to mutate mid-test. Booting Payload also inherits WP1's two upstream defects. Payload's own half of takedown was proven against the real thing in WP1 | Booting real Payload per suite (slow, inherits upstream defects, uncontrollable) |
| 2026-08-08 | **`listTracks` pagination: filter in the Payload query, keep the service guard** (for WP7) | Returning Payload's unfiltered totals under-reports; recomputing per page is worse, because a page-local total looks correct while being wrong. A `where` clause excluding placeholder content in production makes totals accurate *at the source*. `ContentService`'s guard stays the authoritative control so nothing depends on the query being right — the filter is an optimisation, not the control | Payload's raw totals (WP3's state, acceptable at one book); recomputing per page |
| 2026-08-08 | Invalid content: **listing drops and logs it, direct fetch throws** | One malformed Track should degrade Explore, not empty it — but a reader who asked for *that* Track must not get a success response for something we refused to serve | Failing the whole list; serving the invalid document |
| 2026-08-08 | `ContentInvalidError` is a **502**, and hidden content is a **404** not a 403 | The backend is working correctly and refusing what an upstream system produced — that is a gateway failure, not an internal one. And whether an unpublished or placeholder Track exists is not something a reader is entitled to learn | 500 for invalid content; 403 for hidden content |
| 2026-08-08 | Logout revokes the **whole token family**, not the single presented token | A family is one device's login chain, so family revocation is what a sign-out button promises. Other devices are unaffected because each holds its own family | Revoking only the presented token (leaves the device still signed in via rotation) |
| 2026-08-08 | Refresh-token reaping deletes on **expiry, not revocation** | A revoked-but-unexpired row is exactly what lets a replayed token be recognised as *reuse* rather than as an unknown token. Reaping those would silently downgrade theft detection | A retention window after revocation (extra config, no added safety) |
| 2026-08-08 | The CMS locator rule is implemented **independently** of `hasSourceLocator` in shared, not imported | A shared predicate means one bug defeats both gates — the exact failure the two-gate design exists to prevent. The mapper may reuse it; the CMS may not | Importing the shared predicate into the CMS (DRY, but collapses the two gates into one) |
| 2026-08-08 | **Design direction approved** — playful/gamified, dark default (light supported), teal primary + warm amber reward accent, no mascot in Phase 1** | Founder's calls. Playful-on-dark loses its usual toolkit — shadows are invisible and saturated colour glares — so depth comes from surface lightness and energy comes from motion. The second warm accent exists because teal-on-dark is the least distinctive option available and reward moments must not compete with the interface. Full brief: `proposals/design-direction.md` | Bold editorial (Architect's recommendation); calm premium; dark-first focus. Light-default was recommended for share screenshots — mitigated instead by making share screens a light-surface exception |
| 2026-08-08 | **Refined: the AI pipeline comes after *all* app and admin work**, not just after WP8 | Founder's call — the pipeline needs its own brainstorming and design cycle, and interleaving it with app work would fragment both. The entire Phase 1 build (WP3–WP11) runs on placeholder content, which WP11 already provides at realistic volume. No Architect proposal for the pipeline until app work completes | Pipeline after WP8 (earlier ruling, superseded); pipeline first |
| 2026-08-08 | **Build order: app through WP8 first, pipeline after** — *superseded same day by the row above, which extends it to all of Phase 1* | One Manager session works serially, so this is a resource decision, not a technical one — the pipeline is orthogonal to WP3–WP9 and blocks nothing architecturally. Decided this way because **a generated Leaf cannot be judged without the player**: whether a scenario lands or an unlock feels earned is invisible in a CMS record and only observable at WP8. Building the generator first means tuning prompts against JSON nobody can evaluate. Architect writes the pipeline architecture proposal in parallel — planning time, not Manager time | Pipeline first (app stalls, generated output unjudgeable); a thin generator slice first (half-built pipelines stay half-built) |
| 2026-08-08 | **Exactly-one-correct-option stays enforced on save**, not moved behind publish | Settled by the founder authoring a real Leaf — the refusal read as helpful, not obstructive. This is why the question was deferred from WP1 rather than guessed at: Architect's prior was to move it behind publish, and the evidence went the other way | Publish-gating it (Architect's prior, not supported by the evidence) |
| 2026-08-08 | **Authoring estimate revised down**: ~5 min mechanical per Leaf, 15–25 min with real content and review | Measured at the gate. The editor is not the bottleneck — thinking, writing and sourcing are. A 20-Leaf Track is ~5–8 hours rather than the 10–15 originally planned | The original 30–45 min/Leaf estimate, which was ~9x pessimistic on the mechanical portion |
| 2026-08-08 | **Schema frozen** after the gate, with four corrections | One authored Leaf surfaced exactly the class of gap the gate existed to find. Content types in `packages/shared/src/content.ts` lose their PROVISIONAL status once WP2.1 lands | Freezing unchanged (would have shipped four known defects); another authoring round before freezing (diminishing returns from a second data point) |
| 2026-08-08 | All CMS text fields trimmed on save — leading/trailing only, **never** collapsing internal whitespace | Trailing whitespace reached rendered output and made otherwise-identical values distinct, which poisons any later dedup or comparison. Internal whitespace is intentional in multi-line payoff bodies | Trimming at render (too late, data is already dirty); collapsing all whitespace (destroys authored formatting) |
| 2026-08-08 | `SourceReference` requires `note` **plus at least one of** `chapter` / `page` / `quote`, publish-gated | `note` alone is a description of where a claim came from, not a citation anyone can check — weaker than `LEGAL.md` intends for an audit trail. Requiring `chapter` outright produces junk for a Leaf synthesising across a book, and `page` is edition-dependent false precision. Letting the author pick the honest locator gets specificity without junk | Requiring `chapter` always; leaving all three optional (status quo, weak audit trail); dropping `page` entirely |
| 2026-08-08 | Sticky notes bounded at **min 2, max 6** | Chosen by failure direction, not by the single data point: too tight blocks an author immediately (cheap, visible), too loose ships content that breaks the board layout at WP8 (expensive, late). Loosening later is safe; tightening invalidates existing content | A wider bound informed by n=1; no bound (the WP0 debt item, unresolved) |
| 2026-08-08 | `publisher` and `coverUrl` required to publish a Track | `coverUrl` is load-bearing for Explore in WP7. `publisher` is a compliance field — `LEGAL.md`'s curation policy excludes publishers in active AI litigation. **Note:** `trackSchema` already declared both non-optional, so the CMS was weaker than the domain model and WP3 would have thrown at serve time. The two-gate design caught it, just expensively | Leaving them optional and defaulting at render (hides missing compliance data) |
| 2026-08-07 | Social signup carries `dateOfBirth` and `timezone` in the request body alongside the provider ID token | Apple and Google supply neither, and the age gate cannot be skipped. Collecting them *after* account creation would mean creating an account for a possible minor. **WP6 consequence:** only date of birth needs a screen — timezone is read silently from the device via `Intl.DateTimeFormat().resolvedOptions().timeZone` and never asked for | Post-signup collection (defeats the gate); deriving age from the provider (neither supplies it) |
| 2026-08-07 | Password hash stored on `user_auth_providers`, not `users` | A social-only reader has no password, so the hash belongs to the *identity* rather than the person | A nullable column on `users` |
| 2026-08-07 | Minimum password length 12, no composition rules | NIST 800-63B advises against forced composition; length is what adds entropy | Complexity requirements |
| 2026-08-07 | Unconfigured social providers fail closed | An unregistered provider gets an audience no token can match, so it fails the audience check rather than skipping verification. Neither app is registered yet and requiring the IDs would block local development | Requiring client IDs at boot; skipping verification when unconfigured |
| 2026-08-07 | Cross-user profile access returns 403, not 404 | Ids are unguessable UUIDs, so the enumeration risk a 404 would hide is not live — and a 404 makes a genuine bug look like a missing row | 404 to avoid confirming existence |
| 2026-08-07 | Payload `typescript.declare: false` — generated types emitted into `packages/shared` **without** the `declare module 'payload'` augmentation | `packages/shared` must never acquire a `payload` dependency, because **mobile consumes that package** — pulling a CMS framework's module augmentation into the mobile dependency graph is the same class of problem as the Next.js leak WP1 guarded against. Cost is loose slug typing inside `apps/admin` alone, bounded at three collections | Emitting twice (local copy with the augmentation, shared copy without) — a second artifact and a drift risk for a local, minor typing gain |
| 2026-08-07 | Payload's auth collection is `admins`, not `users` | `User` in `packages/shared` means an app *reader*. Two things named `User` would collide the moment codegen emits into that package | Naming it `users` and disambiguating later |
| 2026-08-07 | Generated CMS types exposed on a `./cms` subpath, not from the shared index | Nothing picks up CMS shapes by accident; WP3 imports `@zoomout/shared/cms` deliberately | Re-exporting from the index |
| 2026-08-06 | Backend framework = **Fastify 5**, not Express | First-class pino integration (structured logging was a WP0 requirement) and materially stronger typing under strict mode. A "how" call inside Manager's remit; flagged and ruled on rather than left implicit | Express 5 — weaker type story, logging bolted on |
| 2026-08-06 | `toPublicLeaf` / `publicScenarioSlideSchema` live in `packages/shared`, not the backend | `PublicLeaf` is the type **mobile** renders, so keeping the projection in shared means the client's type system cannot represent a Leaf carrying an answer key. In the backend's content layer, mobile would need a parallel type — violating the "never redefine the same shape" rule in `CLAUDE.md` and putting the product's core invariant back on discipline | Hand-rolled stripping in WP3/WP4; a backend-only mapper |
| 2026-08-06 | Dinner Table Knowledge requires a takeaway `SourceReference`, enforced in `leafSchema` **as well as** the CMS; `sourceReferences` co-located on `Leaf` | The CMS is third-party software whose validation is configuration; `leafSchema` is ours, version-controlled and tested. Two independent gates on the highest-severity risk in `LEGAL.md`, failing in the correct direction — if they disagree, content doesn't ship. Consequence: Payload must model source references as a nested array on the Leaf document, since a `beforeChange` hook cannot validate the invariant across documents | CMS-only enforcement (single point of misconfiguration); separate `SourceReference` collection with a relationship (breaks atomic validation) |
| 2026-08-06 | "Verified locally" on this repo means **`dist` deleted, not just `npm ci`** | `npm ci` clears `node_modules` but leaves `packages/shared/dist`, which masked a real lint-ordering bug through a supposedly clean run. Caught by Manager; adopted as the standard for all future completion reports | Trusting `npm ci` as a clean room |
| 2026-08-06 | CMS = **Payload 3.x**, read by the backend via its REST API (not its Postgres tables) | Only Payload models the fixed 5-slide Leaf as real nested typed groups — Directus field groups are presentational and don't nest data. Validation lives in testable TypeScript hooks rather than GUI Flows. MIT with RBAC in core, vs Directus v12's MSCL relicensing that puts RBAC at $499/mo and blocks the API without a registration key — unacceptable coupling for an hours-to-takedown obligation. Settled by `researcher` | Directus (loses criterion 1 structurally, licensing hazard); custom admin build; files in git |
| 2026-08-06 | Monetization deferred to a final Phase 4 | Founder's call. Removes billing, IAP, and FTC negative-option compliance from every earlier phase | Monetizing at MVP (compliance burden before product-market fit) |
| 2026-08-06 | Voiceover = AI-generated audio in a natural human voice (ElevenLabs), not human narration | Confirms the reading in `PRODUCT.md`; human recording doesn't scale past a handful of books and blocks the Phase 2 pipeline entirely | Literal human voice actors |
| 2026-08-06 | Phase 1 launch library narrowed to 1 book — "The Mountain Is You" | Only title clear on both curation criteria (indie publisher, no official companion app), and it cuts authoring from ~45 hours to ~12. Adding books later needs zero code, which is the point of the CMS | 3 books (2 flagged on curation, triple the writing load) |
| 2026-08-06 | Build Phase 1 against placeholder content; real authoring deferred to a pre-launch gate | Decouples the app from the content entirely so the full structure is testable end to end without any writing. Schema risk is retained via a 45-min structural gate | Authoring in parallel with the build (writing becomes the critical path) |
| 2026-08-06 | A Leaf is ~3 minutes to consume — ~5 per capped session | Keeps "micro-learning" honest and keeps the 500 XP cap a meaningful second constraint; at 10–15 min/Leaf the XP cap never fires and a 20-Leaf Track becomes a 20-day commitment | ~7 min (2/session); 10–15 min (1/session) |
| 2026-08-06 | ~~Phase 1 launch library = 3 books~~ **superseded same day, see row above** | No AI pipeline means every Leaf is hand-written by a solo founder | More books at launch (unachievable solo) |
| 2026-08-06 | Phase 1 defaults: streak = ≥1 Leaf/day local tz, no freezes; session cap server-authoritative, resets local midnight, finishes in-progress Leaf; XP flat + first-try bonus; auth email/Apple/Google, no guest; online-only; iOS first | Each is the lowest-complexity option that doesn't compromise the product thesis. Offline sync and guest mode were the two big architecture costs removed | Offline-first sync; anonymous guest start (better activation, worse data model) |
| 2026-08-06 | Phase 1 content authoring = self-hosted headless CMS, not a custom `apps/admin` build | ~500 structured slides for a 5-book launch needs a real editing UI. A CMS gives schema modelling, media handling, draft/publish, versioning and roles in days rather than weeks — and makes the still-undecided approval workflow a config change instead of a migration | Custom web app (weeks spent on a tool no customer sees); YAML/JSON in git + seed script (free, but unworkable past a few books and hostile to non-technical authors) |
| 2026-08-25 | **Human gate 1 is file-based in WP16, not a Payload admin view** — overrides §5 of `proposals/content-pipeline.md` | Two reasons. A Payload custom view lives in `apps/admin`, which Pipeline Manager does not own — building it would force a cross-session scope breach on the pipeline's very first package. And building CMS review UI before the pipeline has produced anything worth reviewing is backwards: the plan file is 20 titles, which a text file reviews perfectly well. The Payload surface arrives with gate 2 in WP19, where it is built once for both gates | Gate 1 in Payload now (blocks WP16 on `apps/admin` work by a different session); a standalone review web app (a second tool and a second auth system for one person) |
| 2026-08-30 | **Track 42 is not bulk-revised — confirms Pipeline Manager** | Its text is already slated for regeneration once the reviewer exists, which is what this package built, and its images were generated before the answer-length shuffle. Spending review budget polishing 18 Leaves that are due for replacement pays twice for one result — the same reasoning that declined to regenerate its images for the sixth style anchor. **Verifying the mechanism on a sample was the right call and is the stronger evidence anyway**: a bulk run proves throughput, a traced sample proved the revise prompt was dropping citations it had never been shown. Regenerate the Track once, properly, in WP20 | Bulk-revising now (~$1–2, and every Leaf is replaced afterwards); leaving the mechanism unverified until WP20 (would have shipped the citation bug) |
| 2026-08-30 | **`revise` is capped at 2, not WP16.1's 5 — and the divergence is correct** | Pipeline Manager reasoned this rather than inheriting either number, which is what the handoff asked for. WP16.1 raised the *breakdown* cap because its cost assumption had changed: generation moved to a free Flash call, so a cap guarding money we no longer spend was costing plans for nothing. **Editorial review may run cross-family, and Claude has no free tier anywhere**, so R7's original cost reasoning is the one that actually applies to this loop. Two caps, two numbers, each matching its own economics — recorded so neither is later "corrected" to match the other | One cap for both loops (tidier, and wrong in one of the two places) |
| 2026-08-29 | **The machine account loses `delete` as well as `publish`** | Answers Manager's WP15.2 question. The pipeline has no use for deletion — its idempotency is find-then-update, and WP17's junk-Track cleanup was manual housekeeping done by a human with a login, not something the pipeline does in production. **The argument is WP15.2's own finding.** Publishing was believed scoped, thirteen unit tests agreed, and the key could still unpublish a live Track. The same class of mistake on `delete` does not expose content, it destroys it — and takedown is a legal obligation, so a machine account able to delete a Track is able to break a compliance mechanism. Least privilege, and Manager reports it as two lines | Leaving `delete` open (no use case, worst blast radius); a soft-delete convention (a convention is not a permission) |
| 2026-08-29 | **`_status` is not a safe access-control constraint when drafts are enabled** | WP15.2's finding, recorded because it will recur wherever access control meets versioning. A `_status not_equals published` constraint reads against the **latest version**, so a published document carrying a pending draft evaluates as a draft and passes the check. The hole was open on real content and closed on a clean fixture — which is exactly why thirteen passing unit tests missed it and only the behavioural probe against a live document found it. **The fix is `draft=true`, which changes where a write lands rather than what it claims**: the published row is not the thing being written, whatever state the document is in | Constraining on `_status` (the bug); a publish-time hook (guards the wrong end — the write has already chosen its target) |
| 2026-08-29 | **A sixth anchor is approved; Track 42's images are not regenerated for it** | The no-glow rule holds except where a subject implies a light source, so the fix is a sixth anchor showing a lit lamp *without* glow — teaching the exception rather than only forbidding it, which is why the existing prohibitions work and this one does not. ~$0.04, evidence-based, approved. **But do not regenerate Track 42's images for it.** Track 42's *text* is already scheduled for regeneration after WP19's editorial reviewer exists, and images follow text — regenerating now buys a marginal style fix on 18 illustrations that are about to be replaced. Add the anchor so the next generation inherits it | Regenerating now (pays twice for one result); skipping the anchor (leaves a known drift for every future book) |
| 2026-08-29 | **Pipeline cost is ~$4 per Track — and money is not the constraint. Human review is** | Measured, not estimated: $2.03 in images and ~$2 in text for a fully illustrated 18-Leaf Track, diagrams free because R4 made them a text call. **A 50-book library is therefore about $200 in generation** — which is not a number that decides anything. The number that does is **Critic-in-the-Loop review at 15–25 minutes per Leaf: 4.5 to 7.5 hours per book, 90–150 hours across twenty.** That is the real ceiling on library growth, it lands entirely on one person, and it is the thing WP19's editorial reviewer exists to reduce. **Recorded to stop the roadmap treating image spend as the cost of content** — it is roughly half of a number that was never the bottleneck | Treating per-image cost as the growth constraint (off by the wrong dimension entirely) |
| 2026-08-29 | **Requiring stylised, non-identifiable figures satisfies the legal guardrail and visual consistency with one rule — and that alignment is luck** | Pipeline Manager's observation, worth keeping. Faces turned away make a figure off-style before it is off-policy, so one requirement enforces both, and the cheap enforcement is not evidence that both are cheap. **Future asset types will not always align this way** — a diagram carrying a real framework name, or audio in a synthesised voice, put the legal constraint and the quality constraint in tension rather than in agreement. When they diverge, the legal one is not the one that bends | — |
| 2026-08-28 | **The two-gate design is validated by evidence, not argument** | Since WP1 the shared schemas and Payload's publish hooks have enforced the same invariants independently, deliberately duplicated, on the reasoning that a second implementation written from a different understanding catches what the first misses. **WP17 turned that from reasoning into evidence.** Payload rejected a Leaf whose Dinner Table fact had no takeaway source reference — content the pipeline's own `ground_check` had already passed. Both were correct; the corruption happened between them, in storage. **Had the pipeline owned both gates, it would have shipped.** Recorded because the duplication has a standing cost in maintenance and every future session will be tempted to collapse it | Collapsing the duplication (the temptation this entry exists to refuse) |
| 2026-08-28 | **A positional handle must be stored with its resolution, never recomputed** | WP17's most serious defect. Passage handles `P1…P12` are positional over the retrieved list, but only `cited_chunk_ids` — a **sorted set** — was persisted, so rebuilding renumbered them. A Leaf citing P1–P4, P7, P9 came back as P1–P6: two references vanished, **and P5/P6 silently pointed at different chunks than the model cited.** The dropped references were caught by Payload. **The wrong-chapter references were caught by nothing** — and a citation naming the wrong chapter is worse than no citation, because it looks checkable. That is precisely the mis-attribution `LEGAL.md` exists to prevent. Same shape as this project's signature failure: handles are *established* at retrieval and *assumed* stable at storage | Reconstructing handles deterministically (the sorted set destroys the ordering that defines them); validating handles downstream (detects the symptom, not the silent wrong-chapter case) |
| 2026-08-28 | **Track 42 is a demonstration of the boundary, not launch content — regenerate after WP19** | Confirms Pipeline Manager. It predates the shuffle fix and carries **both** answerability tells, measured: the correct option is in position 2 in 15 of 18 Leaves and never position 3, and is the longest option in 15 of 18. **A reader scores 83% without reading**, which makes the product thesis decorative. Regenerating now would only need doing again once the editorial reviewer and the length check exist, so it waits — but **its draft status is the only thing standing between it and a reader**, and `isPlaceholder: false` means the placeholder guard will not catch it. Do not publish it | Regenerating immediately (wasted — WP19 changes the inputs); publishing it as a demo (83% answerable without reading) |
| 2026-08-28 | **The pipeline's draft Track deliberately omits `publisher`, `coverUrl`, `purchaseLinks` and `disclaimer`** | Confirms Pipeline Manager's design call. The pipeline cannot know a retailer link or a cover image, and inventing them is fabrication of a different kind than the one grounding prevents — but fabrication all the same. Payload relaxes required fields on drafts, so the omission is legal there and **fails loudly at publish**, which is the correct place: purchase-forward and non-endorsement are the legally load-bearing surfaces and belong to the human gate. **A useful second-order property falls out of this:** a pipeline draft can never be published without human input, which is an independent structural reason the pipeline cannot accidentally ship — separate from, and stronger than, the pipeline's own promise not to | Auto-filling from a search (invents retailer links); marking them optional on publish (removes the two surfaces `LEGAL.md` requires) |
| 2026-08-28 | **Human RBAC still not needed; a scoped machine credential is — and it is the half that matters** | Re-confirms the 2026-08-27 ruling and sharpens it. Pipeline Manager is right that *"the pipeline never publishes"* is today enforced entirely on the pipeline's side: `Admins` has `auth: true` and no `useAPIKey`, so the pipeline holds a **login** with a human's full rights. Roles for humans still model an org chart with one box in it. **But a publish-incapable API key converts a promise the pipeline's code makes about itself into a permission it cannot exceed regardless of its code** — which is what the guarantee should have rested on all along. Handed to Manager as WP15.2 and **raised from low priority to before WP20** | Human RBAC now (models a team of one); leaving the login in place (the never-publish guarantee has no enforcement outside the pipeline's own source) |
| 2026-08-28 | **Age gate is 13+** — founder ruling, open since the original brief | The standard answer and the defensible one. A floor of 13 puts the product **outside COPPA** entirely rather than inside it with obligations: under-13 users would require verifiable parental consent, an age-neutral collection posture, and restrictions on the behavioural mechanics — streaks, XP and push notifications are exactly what COPPA scrutinises. Excluding under-13s removes that whole surface for the cost of a birthdate check the product already has | 16+ (satisfies every EU member state without analysis, but excludes a real part of the audience for a market not launching first); no gate (COPPA exposure, and the App Store requires an age declaration regardless) |
| 2026-08-27 | **The answer-length tell gets its own mechanical Track-level check — not `ground_check`, and not the advisory reviewer either** | Half-confirms Pipeline Manager's ruling. **Keeping it out of `ground_check` is right** and for the right reason: the legal gate's value is that it cannot be argued with, and style findings are exactly what would make it arguable. **But routing it to WP19's editorial reviewer is too weak a home.** Editorial review is *advisory* by design, and this is not a style preference — the correct option being longest in 15 of 18 (chance ≈ 6) makes the gate answerable without reading, and `PRODUCT.md` names active recall as the product thesis. An advisory finding on a thesis-critical defect will sometimes be ignored, which is the wrong failure mode. **Precedent already exists for the third option:** the 1:1 structure check is mechanical, deterministic, and not the legal gate. This is the same shape. Two design notes: it must measure **per Track, not per Leaf** — one Leaf whose correct answer is longest is chance, a Track where it always is is a tell — and the generation-side fix is to require *substantive distractors* rather than a shortened correct answer, which also addresses the strawmanned-wrong-option problem the read-through found | Folding it into `ground_check` (blurs the one gate that must stay mechanical); leaving it to the advisory reviewer (a thesis-critical defect guarded by a suggestion); ignoring it (the gate is decorative, which is what the position bias already proved) |
| 2026-08-27 | **WP19 is not blocked on role-based permissions. Proceed without them** | RBAC encodes a separation of duties that does not exist: one person is writer, reviewer and admin today, so roles would model an org chart with one box in it. The real trigger is **a second human touching the CMS**, and that is a hiring event, not an engineering one. What WP19 genuinely needs is that publishing is a deliberate human action rather than something the pipeline can do — and that is already structural, because the pipeline writes drafts and Payload's publish validation is independent of anything the pipeline believes. **The observation that a reviewer can also publish is correct but is not currently a vulnerability** — it describes the founder being able to publish their own work, which is the intended workflow at this size. Logged as debt with an explicit trigger so it is not rediscovered | Building RBAC before WP19 (blocks the review loop on modelling a team of one); treating it as a security defect now (it is a scaling gap, and conflating the two would have delayed WP19 for no safety gain) |
| 2026-08-27 | **Contested author claims get attributive framing, and apply-in-life must be actionable in the physical world** | Answers Pipeline Manager's editorial question about teaching Wattles' 1910 metaphysics sincerely. **Quietly modernising an author is fabrication — that judgement was right and stands.** But the choice is not "assert it or soften it", it is *assert* versus *attribute*, and attribution is not softening, it is accuracy. ZoomOut's entire position — legal and product — is "we teach what the book says", never "we endorse what the book says". The non-endorsement disclaimer asserts exactly that at the Track level while the slide prose currently contradicts it. **That is this project's recurring failure mode: a guarantee enforced at one layer and assumed at the next** — the same shape as the payoff shipping ungated, the app pinned to light mode, and the legal surfaces rendered nowhere until WP10. So: a claim about *how the world works* is framed as the author's ("Wattles argues that…"), while ordinary practical advice needs no such hedge — otherwise every sentence acquires a stammer. **The sharpest case is apply-in-life**, which instructs a reader to *do* something: where the book's mechanism is metaphysical, apply-in-life takes the behavioural residue ("write a specific, vivid description of what you want and read it daily" — which is what Wattles operationally prescribes and is defensible goal-setting) rather than the metaphysical claim. **This is general, not a Wattles patch** — it applies to pop psychology built on unreplicated findings, contested economics, and most of this genre, and it feeds the still-open Content Curation Policy, which now has a concrete case to reason from | Modernising the author (fabrication, correctly rejected); sincere assertion plus a disclaimer (the disclaimer is at the Track level and the assertion is at the slide level — the layer that reaches the reader wins); excluding the genre (removes most of self-help, and the framing fix is a prompt change) |
| 2026-08-27 | **The WP16 model finding is withdrawn, not merely superseded** | WP16.1 measured four first-attempt samples per model against one held-constant analysis: Flash 0.04 mean (sd 0.053), Pro 0.06 (sd 0.079), both 100% pass. **Indistinguishable.** Flash is recommended because it is equal, cheaper and faster — not because it is better. The 2026-08-26 hold was correct and is now closed: "a stronger model follows the source structure more faithfully" was a mechanism fitted to three samples from a distribution wide enough to produce any ordering. **Recorded as withdrawn rather than quietly dropped**, because a plausible story attached to real numbers is the kind of thing that gets carried forward by a future session reading only the summary | Keeping the directional claim as a footnote (it would be re-cited); deleting the row (loses the lesson about n=1) |
| 2026-08-26 | **The structure check cannot be overridden by a human at gate 1** — confirms Pipeline Manager's implementation | The check enforces a `LEGAL.md` requirement, and R3's governing principle is that a legally load-bearing gate must not be arguable. An override would be used by one person, under time pressure, at the exact moment shipping matters more than the requirement — which is precisely when it must hold. **The escape hatch is editing the plan, not waiving the check**: rewriting Leaves so they stop mirroring is the work the gate exists to provoke. And the distinction that keeps this from being dogma — **the thresholds are negotiable, the verdict is not.** A genuine false positive comes to Architect with its measurements and the constants get tuned, with a record. If a book cannot pass without contortion, that is a signal the book is a poor fit for ZoomOut, not that the check is wrong | A recorded-reason override (an escape hatch built before hitting the wall gets used); a soft warning (makes the requirement advisory, which is what R3 exists to prevent) |
| 2026-08-26 | **`MAX_BREAKDOWN_ATTEMPTS` 3 → 5** | R7 set the cap on cost grounds assuming a Pro-tier call per round. On Flash against credit a round is effectively free, so the cap now guards money we do not spend while costing us plans — one real run burned all three attempts without converging. Bounded-ness was the actual requirement and it survives. **Paired with the prompt fix, not instead of it**: raising the cap alone buys more attempts at the same under-constrained prompt. If 5 routinely fails *after* WP16.1, that is evidence the thresholds are wrong — escalate, do not raise it again | Leaving it at 3 (loses plans to a constraint that no longer binds); removing the cap (unbounded cycles are exactly what R7 exists to prevent) |
| 2026-08-26 | **Default to Flash on cost and speed. Do *not* record "capability isn't the lever" as policy yet** — WP16.1 settles it | The operational answer needs no evidence: Flash is cheaper and faster and the check passed on it. The *causal claim* is a different thing and this data cannot support it. Two runs of the same model scored 24% and 78% on the single-chapter ratio; Flash and Pro differ by 11. **The within-model spread is roughly five times the between-model gap, and every cell is n=1.** The hypothesis is genuinely interesting — a model tuned for instruction-following may treat "break this book into lessons" as summarization, where the book's own structure is the highest-likelihood output, and Pro reusing 10 of 18 chapter titles verbatim is a striking observation — but writing it down now would shape WP17–WP20 model choices on noise. Measure n≥3 per config, then rule | Recording the Flash-over-Pro capability claim now (a policy built on n=1 with unmeasured variance); staying model-agnostic (gives no operational answer, and one is needed today) |
| 2026-08-26 | **The Payload boundary opens in WP17, not WP19 — the pipeline writes drafts as it generates** | The package that produces a field should be the package that proves the field survives the boundary. WP15 shipped a mapper that silently dropped three new fields with 932 tests green, because a dropped optional field is indistinguishable from an absent one — and WP17's source references are optional fields. Splitting production from round-trip is how that defect happened. It also makes gate 2 a review of drafts already sitting in Payload, which is where §3.5 always wanted the human to work, instead of a custom admin view built from nothing | Boundary in WP19 with `publish_to_cms` (repeats WP15's failure mode and leaves WP18's media with nowhere to go); pipeline-side storage until publish (a second asset store for one release) |
| 2026-08-26 | **WP18: scenario images ship — founder override of the recommendation to defer** | Founder's call: the launch experience needs to be as real as possible, and illustrated scenarios are a visible part of that. Accepted. Guardrails ruled with it — no author likeness, no real people, no cover art or publisher branding, no rendered text in images — and a **single visual identity across the app**, via reference-image conditioning against a committed anchor set with a style contract derived from `design-direction.md`. Amber `#FFB020` is excluded from illustrations because §3 reserves it for reward moments. **Cost consequence:** images have no free tier and no free fallback, so this is the one workstream the credit clock genuinely binds | Deferring illustrations to post-launch (cheaper, and the original recommendation — overruled on experience grounds) |
| 2026-08-25 | **WP16 builds against public-domain books only, on the Gemini free tier** *(reason revised 2026-08-26 — see §4a: the training-corpus leg is gone, R6's ingestion leg stands)* | Not a cost preference — a legal one. §4a verified that the free tier uses submitted content to improve Google's products while the paid tier does not. A real book through the free tier feeds a copyrighted work into a training corpus: a worse version of the ingestion exposure R6 already names, and unfixable after the fact. Public-domain input makes the engineering phase carry zero exposure while the output is being thrown away anyway. Recommended first title: *The Science of Getting Rich* (Wattles, 1910) — right genre, 17 real chapters for the 1:1 check to bite on, short enough that a full run is cheap | A real book on the paid tier from day one (spends money to test a pipeline whose output is discarded); a real book on the free tier (the exposure above) |

## Technical debt register
| Item | Impact | Flagged by | Status |
|---|---|---|---|
| **Payload `destroy()` does not close its database pool** — it only resets in-memory schema state, and `pool.end()` hangs because Payload keeps a client checked out | Affects anything booting Payload **outside a request lifecycle**: WP3's integration tests if they seed via the Local API, and the WP11 seed script. **Not** the `ContentRepository` runtime path, which calls REST over HTTP and never boots Payload | Manager, WP1 | Open — mitigate once in a shared test/seed harness, not per caller |
| **Payload attaches no `error` listener to its pool** — an idle-client error becomes an uncaught exception | Same blast radius as above: test harnesses and the seed script | Manager, WP1 | Open — same shared harness |
| Payload's stock template tracks their unreleased `main` and disagreed with 3.87.0 in three places (`generatePayloadViewport`, `importMap`, `turbopack.root`) | All fixed and commented at the site. A future Payload upgrade must re-run `payload generate:importmap` and re-check `(payload)/layout.tsx` | Manager, WP1 | Fixed — recorded as an upgrade checklist item |
| **No input trimming in the CMS** — four authored fields carried trailing whitespace, including `" ; \n"` on `dinnerTableKnowledge` | Reaches rendered output; makes identical values distinct | Manager, gate 2026-08-08 | Assigned to WP2.1 |
| **An acceptance criterion was satisfiable without exercising the code it was about** — "first-try correct earns more XP" passed against tests that skipped `start`, hitting the upsert's INSERT branch, while every real client hits `ON CONFLICT` where a different expression decides the bonus | Caught by the founder on review; closed by a new test, proven by mutation (breaking the `ON CONFLICT` expression fails only the new test). **Directly relevant to WP5**: `DailySession` and `Streak` are both upsert-shaped with the same INSERT-vs-`ON CONFLICT` split | Founder, WP4 review | ✅ Closed. Rule added to `agents/architect.md`: where state is upsert-shaped, conditional or cached, criteria must **name the path**, not just the outcome |
| **WP3 signed off 11/11 while shipping payoff prose ungated** — "the answer key never leaves the server" had a criterion, "the payoff never leaves early" did not, though they are halves of one guarantee | Every WP3 demo of `/content/leaves/:leafId` showed content the product intends to withhold. Not a Manager error — no criterion covered it, because the gate was WP4's. **Architect process failure**: a package shipped a surface whose guarantee was only half-built and nothing recorded the gap | Architect, WP4 review | **Fixed in WP4.** Rule added to `agents/architect.md`: when an invariant has two halves, both get criteria in the same package, or the unenforced half is logged as debt at sign-off |
| **`apps/backend/src/index.ts` carried two NUL bytes from WP2** (`?? '\0unconfigured'`, where a space was intended) — git treated the composition root as binary, so every diff of it since WP2 was unreviewable | Functionally harmless, but it survived two code reviews because the file *renders* normally; the byte only shows as `Bin` in `git diff --stat` | Manager, WP4 | Fixed in WP4. **Recommend a CI guard**: fail the build if any tracked text file contains a NUL byte — cheap, and this class of defect is invisible to review by construction |
| **No rate limit on answer submission** | Brute-forcing a scenario is trivial *by design* (3 options, unlimited retries), but it is an unbounded write path — each attempt is a row update plus a cached CMS read | Manager, WP4 | Open — pre-launch, on write-volume grounds not answer-secrecy grounds |
| **Nothing computes a reader's total XP** | `leaf_progress.xp_awarded` sums to it, but no endpoint exposes it and there is no `users.total_xp`. WP7's profile screen needs it | Manager, WP4 | **Assigned to WP5.** Ruled 2026-08-09: derived on read, indexed on `user_id` |
| The answer response identifies the correct option by elimination — two wrong answers leave one | Inherent to 3 options plus unlimited retries as specified, not a defect | Manager, WP4 | Recorded so WP8 does not rediscover it as a bug |
| ~~`.env.example` missing the XP variables~~ | — | Manager, WP4 (stale claim) | ✅ **Not a gap.** Both lines are present, committed in `e220510` and verified. The completion report's claim was stale; WP4 is 12/12. The `Read(**/.env.*)` deny rule causes read friction but did not block the write — **no settings change recommended** |
| **Streaks are structurally capped by library size** — a reader needs ≥1 Leaf per day, so a 20-Leaf library supports at most a 20-day streak, and a reader working at the intended 5/day exhausts it in four | Surfaced while designing the achievement list. **The streak is a core retention mechanic and one book cannot sustain it.** Three options: allow re-completing Leaves to count toward a streak, ship more books before launch, or accept that streaks only become meaningful as the library grows. `streak-30` is unreachable at launch either way | Architect, 2026-08-09 | **Open — needs a founder decision before launch, not before WP5** |
| **Payload's own REST API serves `payoff` and `isCorrect` anonymously** — by design, since the backend calls anonymously to get published-only content | **The highest-consequence item in the WP4 review.** WP4 made the payoff gate the product's central guarantee, and that guarantee stops at the ZoomOut API. If Payload is reachable from the public internet in production, both the payoff gate and the answer key are bypassable by calling Payload directly. Private networking is currently only a comment in `payloadClient.ts` — nothing in code, config, or `.env.example` enforces or records it | `code-reviewer`, WP4 | Open — **must become an explicit, verified deployment requirement before any deploy.** Not a code change; a deployment constraint that currently exists only as prose |
| **`leaf_progress.attempt_count` is a PG `integer` with no ceiling** | Extends the rate-limit item above. Sustained wrong answers reach 2^31 in under two weeks at a few thousand/sec, after which every further answer on that Leaf raises `integer out of range` — a permanent 500 on a Leaf the reader can never complete, contradicting "unlimited retries". Self-inflicted, but a terminal state | `code-reviewer`, WP4 | Open — rate limiting on `POST .../answer` closes both this and the write-amplification concern |
| **`toDomainProgress` validates on the *response* path, but the error handler maps any `ZodError` to `400 INVALID_REQUEST`** | A server-side data-contract violation would be reported to the client as a malformed request, with internal field paths in `details` — misattributing our bug to the caller. Latent: every column is constrained tighter in Postgres than in Zod | `code-reviewer`, WP4 | Open — handler design predates WP4; fix when the error handler is next touched |
| **A non-numeric `:leafId` may 503 rather than 404** | `leafIdParams` accepts any non-empty string, reaching Payload as `where[id][equals]=abc` against serial integer ids. `FakePayload` returns empty docs so tests see a clean 404; real Payload may error → `ContentUnavailableError` → 503. Not an injection risk — `URL.searchParams.set` encodes properly | `code-reviewer`, WP4 | Open — unverified without the live CMS. Pre-existing from WP3 |
| **API-shape notes for WP7**: `CompletionOutcome.xpAwarded` (paid by *this call*) and `progress.xpAwarded` (the total) share a name in one response body; `start`/`GET` wrap in `{ progress }` while `answer`/`complete` return flat objects | A client author reading JSON rather than TypeScript will misread the first. The second is inconsistent within one module | `code-reviewer`, WP4 | Open — state in the WP7 handoff |
| `ProgressRepository.findReaderTimezone` selects from `users`, which auth/users owns | Bounded-context bleed. A small injected port would be cleaner | `code-reviewer`, WP4 | Open — low priority |
| **`listTracks` returns Payload's unfiltered totals** — in production a page of placeholder Tracks yields fewer rows than `totalTracks` claims | UI pagination will under-report. Ruled approach: `where` filter in the Payload query, service guard retained | Manager, WP3 | Assigned to **WP7** — must be stated in that handoff, not discovered in the UI |
| **`listLeavesForTrack` caps at 100 Leaves with no paging** | A Track is specified at 15–30 so this is comfortable, but it is a silent ceiling rather than an error | Manager, WP3 | Open — revisit if Track length ever changes |
| **Content cache is per-process and unbounded in entry count** | With multiple instances the TTL becomes the *worst-case* takedown latency across them — a legal-obligation concern, not just performance | Manager, WP3 | Open — must be revisited before horizontal scaling |
| **No ETag / `If-None-Match` on content responses** | Mobile refetches full Track lists on every Explore visit | Manager, WP3 | Open — consider in WP7 once payload size is real |
| `user_tracks.status` exists and is always `active` | Nothing sets it yet | Manager, WP3 | Open — WP4/WP5 own the transitions |
| **Integration tests use a Payload stand-in — a real response shape could go unseen** | Mitigated by a manual end-to-end run against the live CMS at WP3 | Architect, WP3 review | Open — **re-run the manual end-to-end check after any Payload upgrade**; added to the upgrade checklist |
| **Testcontainers intermittently skips whole integration suites** when they run back to back (`inspectContainerUntilPortsExposed`); an immediate re-run passes | An occasional red CI build that is green on re-run is this, not a regression | Manager, WP2.1 | Open — add a retry step to the workflow if it recurs |
| **Payload emits `id: number`, domain uses `cmsIdSchema` string** — serial integer keys | The WP3 mapper must stringify ids and handle relationships arriving populated or bare depending on `depth` | Manager, WP2.1 | Assigned to WP3 |
| CMS↔domain divergences documented in `payload.config.ts`: `trackId` is `string \| Track`; `stickyNotes.notes` is `{ note }[]` not `string[]`; `scenario.options` is a plain array not a 3-tuple; Payload adds `_status`, timestamps, row ids | `ContentRepository` must map across these. Left unreconciled, WP3 hand-rolls the mapping and WP4 inherits it | Manager, WP1 | Open — reconcile at the schema-freeze gate |
| `apps/admin/CLAUDE.md` + `AGENTS.md` are generated by `next dev` and committed | Third-party tooling injecting agent instructions into a repo where `CLAUDE.md` is a controlled artifact. Content is benign; the mechanism is unreviewed | Architect, WP1 review | Open — recommend a header noting it's tool-generated and that root `CLAUDE.md` wins |
| `typescript.declare: false` costs loose slug typing inside `apps/admin` | Bounded to one workspace with three collections; integration tests use bracket access for `_status` / `isPlaceholder` | Manager, WP1 | **Accepted** — ruled 2026-08-07, revisit only if `apps/admin` grows |
| `publicLeafSchema` derives from the Leaf shape *before* the Dinner Table Knowledge refinement | Safe today — `toPublicLeaf` only accepts an already-validated `Leaf`. Becomes a hole the moment anything parses a public Leaf from untrusted input | Manager, WP0 | Open — WP3 handoff must forbid parsing untrusted input with `publicLeafSchema` |
| 14 moderate dev-only npm advisories (drizzle-kit's bundled esbuild-kit and vite transitives) | Dev-only, no runtime exposure | Manager, WP0 | Accepted — revisit when upstream updates. Not worth forcing breaking upgrades |
| ~~Mobile component-testing stack undecided~~ | — | Manager, WP0 | ✅ **Closed 2026-08-11 — `jest-expo`.** Rendering RN under Vitest means owning the Flow transform and native-module mocks across every SDK upgrade; `jest-expo` is versioned against the SDK. Mitigation: one runner per workspace — Jest only in `apps/mobile`, Vitest untouched elsewhere |
| **The app was pinned to light mode from WP0 to WP6** — `app.json` carried `userInterfaceStyle: "light"`, telling iOS the app has no dark support, so `useColorScheme()` returned light on a dark device and the entire dark theme was dead code | Every unit test passed throughout. Caught only by the criterion "verified by switching theme, not by reading the token file" — **that phrasing is the template for WP7 and WP8's visual criteria** | Manager, WP6 | Fixed (`"automatic"`). Requirement now recorded in `design-direction.md` §3 |
| **Two tab glyphs (`↗`, `☺`) took emoji presentation on iOS and ignored the tint colour** | The active-tab affordance was dead on half the bar while looking correct in any screenshot-free review | Manager, WP6 | Fixed with U+FE0E. Symptomatic of having no icon set — see below |
| **No icon set** — tab and status glyphs are text characters | Deliberate deferral, but needs deciding against real surfaces | Manager, WP6 | Open — **WP7 decision** |
| **React Native Testing Library v14 made `render`, `renderHook`, `fireEvent` and `unmount` async** | Un-awaited they fail as `undefined.current` or as assertions against a tree that never committed — errors pointing nowhere near the cause. `jest-expo` also does not set `IS_REACT_ACT_ENVIRONMENT` | Manager, WP6 | Recorded so WP7 does not rediscover it; setup fixed |
| ~~No OAuth client registered for Apple or Google~~ | — | Manager, WP6 | ✅ **Void — social sign-in deferred 2026-08-11.** The code needs no change: `isAvailable()` is already false without a client id and the buttons are already hidden. Unconfigured providers fail closed (ruled 2026-08-07) |
| **Password reset is now the only account-recovery path** | Was survivable while social sign-in offered an alternative way in. With email/password as the sole method, a reader who forgets their password has **no route back into their account at all** — and their streak, library and XP are behind it | Architect, 2026-08-11 | **Open — priority raised. Pre-launch blocker.** Needs a transactional email provider, not an auth migration |
| **Push notifications are not scoped anywhere** | The streak is a core retention mechanic and a streak without a reminder is much weaker — a reader who forgets loses it with no prompt. FCM or Expo Push, usable without adopting Firebase as a backend | Architect, 2026-08-11 | **Open — needs scheduling.** Pairs naturally with WP5, which owns streaks |
| **No deployment or hosting work exists in any work package** | Every package from WP0 onward lists infrastructure as out of scope, and no package was ever written to cover it. Nothing is deployed anywhere. The simulator can reach `localhost`; **a real device cannot**, so this becomes pressing around WP8 when the founder wants the app on their phone — and it is obviously required for launch | Architect, 2026-08-11 | **Open — needs its own work package.** Not yet scheduled |
| **Manual verification found three defects invisible to 792 passing tests** — the flagship 20-Leaf Track sorted 26th of 27 onto a page Explore cannot reach; a placeholder cover background matching the card colour, so a loaded and a failed image looked identical; and `bookTitle` as the upsert key, so renaming a Track orphans it | Nothing was wrong with the data, which is why no assertion could see it. **This is the strongest evidence yet for the manual-verification half of the tiered testing bar.** All three fixed; sort order pinned by a mutation-checked test | Manager, WP11 | Closed — recorded as precedent |
| ~~The draft filter is definitive at config but never empirically exercised~~ | — | Manager, WP7 | ✅ **Closed empirically 2026-08-12** — 28 Tracks exist, 27 reach an anonymous caller. The thing WP7 could only argue from config is now measured |
| **Explore has no pagination affordance** — stops at twenty with no indication anything follows | Live rather than theoretical now that the corpus is 28 Tracks | Manager, WP11 | **Assigned to WP8** |
| **Three XXXL layout issues exposed by realistic titles** | Only visible once seeded content had real-length titles rather than fixture stubs | Manager, WP11 | **WP14** |
| ~~The unlock auto-advances to the payoff slide~~ | — | Manager, WP8 | ✅ **Closed 2026-08-12** — founder played it on a device and confirmed it reads fine. Stays as built |
| **The unlock auto-advances to the payoff slide**, so the reward animates during a screen transition and the spring competes with the navigation | **Open ruling.** Architect's lean: unlock on the *scenario* slide in response to the tap, then advance once it resolves — the reward should be causally attached to the answer, not to the navigation, or it reads as "here is the next screen" rather than "you opened this". Decided by implication in WP8 and correctly flagged rather than left silent | Manager, WP8 | **Blocked on the founder's device check** — a feel question nobody should rule on from a description |
| **Reduced motion is correct by inspection but unobserved** — a static screenshot cannot distinguish a spring from a fade | WP8's one unclosed criterion. Correctly refused rather than claimed, which is the WP6 failure not repeating | Manager, WP8 | **Founder device check** |
| ~~`apps/backend/.env` points at `zoomout_cms`~~ | — | Manager, WP5b | ✅ **Closed 2026-08-12.** `zoomout` holds all nine tables, ledger records 0000–0005, six real readers intact. `zoomout_cms` cleared of backend tables, drizzle schema and enum types; Payload's 20 tables and all 28 Tracks / 22 Leaves untouched. Payload's boot failure was ours, not upstream |
| **`apps/backend/.env` pointed at `zoomout_cms`, not `zoomout`** — so `daily_session` and `streak` never existed in the database holding real readers, `db:migrate` wrote 0004 and 0005 into Payload's database, and `apps/admin` now fails to boot at Payload's schema-pull | **The real cause of WP5a's failed device check**, and the third instance of verifying execution rather than effect — this time inside the very package whose acceptance commit recorded that rule. Recoverable: WP11's seed is idempotent, so CMS content is regenerable | Manager, WP5b | **Part A reopened.** Acceptance now phrased as observable state, not actions |
| **A timezone test proves nothing unless the two timezones disagree at the instant it picks** — WP9 replaced an `at time zone` conversion with a naive `::date` cast and all nine tests still passed, because the chosen instant was one where the server and Auckland agreed | Distinct from not writing the test, and identical to correct in a green run. Three local-date invariants in this project ride on exactly this kind of test | Manager, WP9 | Added to `agents/manager.md` |
| **Some assertions cannot be mutation-checked** — "wrapping up does not lock the reader out" asserts the *absence* of a mechanism, so there is no line to break | Guards a future regression rather than proving present behaviour. Knowing which tests are which is real WP14 information | Manager, WP9 | Added to `agents/manager.md`; Manager now reports them |
| **The thumbnail check failed first time** — at 130px the streak read and the book did not: `body` weight, muted grey, wrapped over two lines | Fixed to `h3`, full contrast, one line. A truncated title a stranger can read beats a complete one they cannot. **The manual criterion earning its place again** | Manager, WP9 | Closed |
| **The achievement share screen was never walked end to end** — same `ShareCard` and capture path as the wrap-up screen, both verified, but its own route was not exercised | Reported honestly rather than counted | Manager, WP9 | **WP10's device session** |
| **Android is entirely unverified**, including the `collapsable={false}` guard that exists because Android flattens container views and would leave nothing to capture | Platform decision was iOS-first, Android close behind | Manager, WP9 | **`launch-blockers.md`** |
| **Metro must be restarted after adding a native module** — a stale bundle silently serves the previous JS and is indistinguishable from a feature that does not work | Cost time twice, in WP5b and WP9 | Manager, WP9 | Recorded |
| **`GET /health` is not a schema probe** — it returned `{"status":"ok","checks":{"database":"up"}}` while every table it depends on had been dropped | Anyone verifying a deploy with it would see green against an empty database. **A WP12 requirement, not a note**: the health check must assert schema presence, not just connectivity | Manager, WP5b | **WP12** |
| **Migrations are not idempotent against a half-cleaned database** — `0005` begins with `CREATE TYPE reader_event_type` and fails where that type survives | One missing object aborts the whole transaction. Cleanup SQL should use `drop ... if exists` | Manager, WP5b | **WP12** |
| **Profile showed stale everything** — both cards fetched once at mount and never refreshed, so a reader who finished a Leaf saw "No streak yet", "0 XP", "1 of 19" against a database holding 100 XP and four badges | Library, Journey and Explore already refresh on focus; Profile did not. **Unit tests could not have caught it — they mount once and assert on state that was correct at mount** | Manager, WP5b | Fixed in `13c1084` |
| **Extra-large text clips app-wide** — absolute `lineHeight` in `design/typography.ts` while RN scales `fontSize` only | Founder ruled 2026-08-12 not to fix it and to drop it as a per-package check. Removed from `agents/manager.md` so it stops being re-reported. **Honest framing: "renders broken", not "unsupported"** — the app honours the OS setting and then clips, which is the worse state to ship | Manager, WP5b | **Moved to `launch-blockers.md`** |
| **Migration 0004 shipped without a drizzle snapshot** — hand-written SQL and journal entry only, so `db:generate` diffed against 0003 and emitted a 0005 re-creating both tables | Would fail on an empty database and break every integration suite. A latent CI bomb found by accident. Repaired; snapshot reconstructed byte-identical and 0004's journal timestamp restored | Manager, WP5b | Closed — **standing rule added: a hand-written migration must ship its snapshot** |
| **Endpoint tests can pass while the storage guarantee beneath them is gone** — replacing `onConflictDoNothing` with an upsert left all nineteen endpoint tests green, because the service filters already-held achievements before reaching the insert | Only a repository-level race test reddens for that mutation. **The most useful WP14 input so far** | Manager, WP5b | Recorded — repository-level test added |
| ~~Backend has no `apps/backend/.env`~~ | — | Manager, WP5a | ✅ **Closed 2026-08-12** — `--env-file-if-exists` on dev and migrate, so a fresh clone gets "See .env.example" rather than a crash |
| **Verification that observes execution rather than effect is not verification** — WP5a's `printf >>` created a 235-byte stray `apps/backend/.env.example`; building `.env` from that stub produced a file with no `DATABASE_URL`, and the write silently no-opped. The check confirmed the command ran, not that it changed anything (`re.sub` succeeds when it matches nothing) | Same class as WP4's criterion that passed without the mechanism working. Cost WP5a its device check | Manager, WP5b | Recorded as a standing rule |
| **`Read(**/.env.*)` has now caused three incidents** — a false "not done" claim in WP4, a stray stub file in WP5a, and WP5a's failed device check | The rule forces blind writes to a committed template that contains no secrets. **Architect recommendation, reversing an earlier retraction: narrow it to `.env` and `.env.local` and let `.env.example` be readable.** Founder's security config, founder's call | Architect, 2026-08-12 | ⬜ **founder decision** |
| **`2e99714`'s message and diff disagree** — it describes only the env fix but also carries `launch-blockers.md` and roadmap changes swept from the shared tree | Flagged rather than amended, correctly: the commit is on `origin/main` and rewriting pushed history is worse than an inaccurate message. `f743b31` explains it | Manager, WP5b | Closed — recorded, not fixed |
| **Backend has no `apps/backend/.env`** — it runs only from a shell with `DATABASE_URL` already exported | **Has now blocked two packages**: WP11's CMS operator account and WP5a's device check, where a migration landed in Payload's database rather than the backend's. Will block WP12. Ruled 2026-08-12: gitignored `.env` plus `--env-file` on dev and migrate scripts is standard practice, not a compromise — the password lives only in the untracked file | Manager, WP5a | **WP5b, first item** |
| **Session time is elapsed-time-per-Leaf, clamped to five minutes** | The only signal available without a client heartbeat. Under-counts a genuinely slow reader, which errs toward letting them continue — the right direction to be wrong for a wellbeing feature | Manager, WP5a | Open — **a real activity signal needs scheduling**, not deferring indefinitely |
| **Trap: `CASE … ELSE NULL` beside a bound parameter will not plan in Postgres** | Surfaced as a 500 on completion and reddened twelve integration tests with `expected undefined to be false`, no type error anywhere | Manager, WP5a | Recorded — it will recur |
| **Trap: a 24-hour test span outlives a 15-minute access token** | A date test then fails with a 401 that looks exactly like a date bug | Manager, WP5a | Recorded — it will recur |
| **Re-reading a finished Track from Library is not possible** | Needs a `firstLeafId` — a shared-type change plus backend rollup work the WP8 handoff did not ask for | Manager, WP8 | **WP14**, or its own small item if it turns out to matter sooner |
| ~~The on-device partial-rollup render is unverified~~ | — | Manager, WP11 | ✅ **Closed as a WP8 side effect** — Journey showed "7 OF 20 COMPLETE" with the bar a third filled, then 8 of 20 after finishing a Leaf |
| **The on-device partial-rollup render is unverified** — needs the simulator signed out, and partial progress cannot be produced by tapping until the Leaf player exists | Verified at the API instead: 7 of 20, `status: active`, `nextLeafId: 9`, 700 XP — consistent with 80+20 across seven first-try Leaves | Manager, WP11 | **WP8** — becomes verifiable by tapping |
| **The draft filter is definitive at config but never empirically exercised** — both content collections set `read: publishedOrAuthenticated` and `PayloadClient` calls anonymously, so drafts cannot reach us | Correct, and no redundant filter was added. But the running corpus holds one Track and one Leaf, both published, so nothing proves it | Manager, WP7 | **WP14** — needs WP11's seed to create a draft that *could* leak |
| **`fakePayload` ignores `page` and `limit`**, always answering `page: 1, totalPages: 1` | The `listTracks` totals fix is verified for a single page only | Manager, WP7 | **WP14** |
| **Cover images are unvalidated URLs** — `trackSchema` requires a URL, not an *image*. The seeded Track's `coverUrl` points at an Amazon product page, so every card renders the fallback | Silent and total: the whole Explore surface looks broken while every test passes and the data is technically valid | Manager, WP7 | **Assigned to WP11** — add a CMS-side rule before real content is seeded. Cheapest possible moment to fix it |
| **The progress rollup is N+1 against the CMS** — one Track fetch and one Leaf-list fetch per library entry | Cached and concurrent, fine for a few books; thirty books is thirty Leaf-list requests per Library open | Manager, WP7 | Open — **deferred deliberately.** Ruled approach when it matters: batch Track reads with Payload's `where[id][in]`, and cache the ordered Leaf-id list on a longer TTL than content since it is structural. **Do not add a stored counter** — see the note below |
| **`Track.leafCount` exists in the frozen schema but must not be trusted as the rollup total unless a CMS hook computes it** | Manager correctly refused a counter on `user_tracks` because it would drift the moment a Leaf is added or withdrawn. `Track.leafCount` is the *same* stored-counter problem: safe only if a Payload hook maintains it, unsafe if an author types it | Architect, WP7 review | Open — resolve if and when the N+1 batching is done |
| **No pagination UI in Explore; `GET /library` is unbounded** | Both are the same shape of problem — behaviour with a lot of content, which does not exist yet | Manager, WP7 | Open — pair them, post-content-scale |
| **Changing OS text size while the app runs leaves Expo Go rendering the old line height** | Looks like a severe layout bug and is not one. Wasted time on WP7's accessibility verification | Manager, WP7 | Recorded — **verify accessibility from a cold start with the size already set** |
| **Two overlapping 401s can produce two refreshes** — `client.ts` clears `refreshInFlight` in `finally` and never compares the token a request *sent* against the current one, so a request dispatched during the refresh window carries a stale token and its late 401 starts refresh #2 | Confirmed by execution. **Not a security problem** — `adopt()` is awaited inside `performRefresh()` before the promise resolves, so refresh #2 reads the rotated token and no family revocation occurs. One extra round trip in a ~1-RTT window. Fix: record the token used per request, retry without refreshing when it has already changed | `code-reviewer`, WP6 | Open — follow-up, not blocking |
| **Plaintext password in React Navigation route params** (`navigation/types.ts` `EmailSignUpDraft`) | Inert today — no state persistence or crash reporter is wired. Becomes a credential leak the day Sentry breadcrumbs or `onStateChange` logging is added, which we will add | `code-reviewer`, WP6 | **Required fix** — hold the draft in context or a ref, not route params |
| **Server-side validation failures surface on the wrong screen** — `SignUpScreen` validates email as `includes('@')` and ignores the backend's 256-char limit; nothing submits until after the age gate, so `reader@example` fails with "Request body is invalid" on the *age gate* | Recoverable (draft survives `goBack()`), but badly signposted. Also `MINIMUM_PASSWORD_LENGTH = 12` duplicates a backend literal with only a comment tying them together — nothing in `packages/shared` holds it | `code-reviewer`, WP6 | Open — follow-up |
| **`responseFields` can clobber `code`** — `app.ts` spreads `{ code, message, ...responseFields }`, so an opted-in error could overwrite the client's branch key | Nothing does today | `code-reviewer`, WP6 | Open — `Omit<…, 'code' \| 'message'>` on the getter's return type |
| **Coverage gaps in WP6**: `SignUpScreen` has no test at all (the only screen holding validation rules); no test asserts `SignInScreen` → `ProviderEmailMissing`; `ProfileScreen`, `TabShell`, `RootNavigator` and the three shells have no render test in either theme | The provider path is unreachable under Jest because the buttons are hidden — worth making `SignInScreen` accept an injected provider | `code-reviewer`, WP6 | Open — WP7 |
| `expo-auth-session`, `expo-web-browser`, `expo-crypto` are dependencies with no importer | Dead weight in the bundle | `code-reviewer`, WP6 | Open — remove or justify |
| ~~Reduced-motion behaviour unverified — WP6 ships no animation to swap~~ | **The premise was wrong**: `AuthStack.tsx` sets `animation: 'slide_from_right'`, and `useReducedMotion()` is exported but never called. There *is* an animation and reduced motion is not respected for it | `code-reviewer`, WP6 | **Required fix** — wire it or remove the animation | `motionPlan` is unit-tested to prove the rule is swap-not-remove and that no 'none' outcome exists by construction | Manager, WP6 | Open — **first real exercise is WP8's payoff unlock** |
| `isProductionPublishable` exists but nothing calls it yet | The placeholder-to-production block is written but not wired | Architect, WP0 review | Open — WP3 must enforce it on the content read path |
| ~~`stickyNotesSlideSchema` has no upper bound~~ | — | Manager, WP0 | ✅ **Closed** — min 2, max 6, ruled at the gate 2026-08-08 |
| ~~"Exactly one correct option" fires on every save~~ | — | Manager, WP1 | ✅ **Closed 2026-08-08** — founder authored a Leaf and reported it helped. Stays on save |
| **`ProviderEmailMissingError` covers two distinct failures** — "the provider returned no email" and "a first-time social signup arrived without date of birth" | A client cannot branch on them, so WP6 will show the wrong screen for one. They need opposite recoveries | Manager, WP2 | Open — **must land before WP6 builds against it**. Candidate for WP2.1 |
| **No logout endpoint** | Refresh tokens are revocable and the machinery exists, but nothing exposes it. WP6 needs it the moment there is a sign-out button | Manager, WP2 | Open — unblocked now. Candidate for WP2.1 |
| Expired refresh tokens are never reaped — the table grows unbounded | Slow-burn storage growth, no correctness impact | Manager, WP2 | Open — pre-launch. Candidate for WP2.1 |
| Timing equalisation covers the missing-account path only — a wrong password on a real account still differs from a correct one by argon2 time | A narrow account-enumeration oracle. Closing it fully needs a constant-time envelope around the whole handler | Manager, WP2 | Open — accepted for now, revisit pre-launch |
| `eslint --fix` strips type assertions on Fastify `inject().json()` (typed `any`), silently un-typechecking tests | Replaced with a `bodyOf<T>` helper routing through `unknown`. Do not trust `--fix` on backend test files | Manager, WP2 | Fixed — recorded as a working note |
| No password reset / email verification flow | A locked-out user has no recovery path. Survivable at promotional-launch scale, not at public launch. `email_verified_at` reserved in WP2 so adding it is not a backfill | Architect, WP2 planning | Open — pre-launch |
| **Adding a graph node cannot reach threads that already finished** | WP17 hit this: the run reached `END` before the CMS node existed, so `resume` did nothing **and reported success** — the worst shape of failure. Solved with an explicit `write-drafts --run-id`. **WP18 and WP19 both hit it again by construction**, since each adds nodes behind runs that already completed. Named in both handoffs so it is designed for rather than rediscovered | Pipeline Manager, WP17 | Open — a standing property of the architecture, not a bug to close |
| **`page` locators are never populated on source references** | EPUBs have no page numbers, so references carry `chapter` and `quote` instead. Honest rather than missing, and sufficient to locate a passage — but a print-edition citation cannot be produced from it if counsel or a publisher asks | Pipeline Manager, WP17 | Open — accepted; revisit only if a licensing conversation needs page-level citation |
| **Unit tests wrote to the real CMS** | The moment the CMS node joined the graph, a resume test ran to completion and created a 22-Leaf "A Test Book" Track in Payload — **twice**, because the durability test builds its dependencies in a subprocess the conftest fixture never reaches. Fixed by injecting the client, verified by counting CMS rows before and after a full suite run. **The subprocess is the part worth remembering**: a fixture that guards the parent process does not guard a child it spawns | Pipeline Manager, WP17 | ✅ Fixed — recorded because the subprocess blind spot generalises |
| **`acquisition` participates in Payload's draft/publish versioning — it is not a flat column** | Found by WP15.1's device gate, which proved more than it asked. Editing the field in the admin UI and saving landed it in `_tracks_v` as the latest **draft** version while the published row stayed `undocumented`, and anonymous REST kept serving `undocumented`. **Consequence for WP17, and it is a good one:** the pipeline setting `acquisition` on a draft cannot change what readers are served until a human publishes. The provenance record and the publish gate are independent by construction rather than by discipline. Also noted: Payload made the column nullable-with-default, so `mapTrack`'s `?? 'undocumented'` fallback is genuinely reachable at runtime even though the generated type says otherwise — the generated type describes documents written since the column existed, not rows predating it | Manager, WP15.1 | ℹ️ **Not debt — a property to rely on.** Recorded here because WP17 needs to know it before its first Track write |
| **`apps/pipeline/.venv` is not gitignored** | Two live consequences, one of which already cost a false bug report. **It pollutes `npm run lint`** on any branch whose `eslint.config.js` predates WP16's ignore — a `.js` file inside the virtualenv produced 26 errors that were reported as an eslint regression, when the actual cause was a branch forked before the ignore existed. **And it is one `git add .` away from committing a virtualenv**, which is forbidden but the files are sitting untracked in a shared working directory. Fix at the root `.gitignore` (`.venv/`, `__pycache__/`, plus `apps/pipeline/.data/` and `runs/`) so it holds on every branch regardless of eslint config | Architect, WP17 review | **Open — Manager, fold into WP15.1.** One line, and it removes a recurring source of phantom failures |
| **Payload has no role-based permissions — `Admins.ts` defines no roles at all** | Anyone who can review a draft can also publish it. **Not currently a vulnerability**: one person is writer, reviewer and admin, so this describes the founder publishing their own work, which is the intended workflow. It becomes real the moment a second human touches the CMS. Ruled 2026-08-27 as **not blocking WP19** — "the pipeline never publishes" is structural (the pipeline writes drafts; Payload's publish validation is independent of it), not dependent on roles | Pipeline Manager, WP17 | Open — **explicit trigger: a second CMS user.** Not before |
| **Grounding proves anchoring, not entailment** | A claim can cite a genuine passage that does not actually support it and pass the gate. The mechanical check verifies the handle was retrieved and the quote is verbatim; it cannot verify that the passage *argues for* the claim. Closing it needs an LLM judge — **a different instrument, and deliberately not folded into the mechanical gate**, whose whole value is that it cannot be argued with | Pipeline Manager, WP17 | Open — a candidate node for WP19, sized separately |
| **The measured Wattles Track predates the option shuffle** — its 18 Leaves still carry the position bias (correct option in B for 15 of 18, never C) | The content is otherwise sound, but the gate on those specific Leaves is answerable by always picking B. **Do not use this Track for anything a reader sees.** Regeneration is a ~25-minute rerun | Pipeline Manager, WP17 | Open — regenerate before this Track is shown to anyone |
| **Every pipeline measurement so far is one book** | Structure-check thresholds, prompt quality, model comparison, cost per Track and the 3–4 sticky-note clustering are all *The Science of Getting Rich*. Wattles is short, aphoristic and 1910 — a second, structurally different book is the cheapest way to find out which findings are about the pipeline and which are about this text | Pipeline Manager, WP16.1 + WP17 | Open — worth one run before WP20 commits to the numbers |
| **`npm run typecheck` is red on `main`** — `src/content/content.mapper.test.ts:285`, `Type '"dot"' is not assignable to type '"json" \| "mermaid" \| null \| undefined'`, from WP15's `cf3e286` | **Reported to Architect as "one character". It is not, and applying it as described would delete a real test.** Line 279 is a deliberate negative test — *"rejects a diagram whose spec format is not one the renderer knows"* — so `'dot'` is invalid **on purpose**; that is the assertion. Changing it to `'mermaid'` makes the test typecheck and assert nothing. The fix is to let the fixture hold a deliberately-invalid value — a cast through `unknown` at the fixture, not a change to the literal. Verified by reading the test, not the error | Pipeline Manager, WP16; diagnosis corrected by Architect | **Open — Manager.** Small, but do not hand it over as "one character" |
| **Payload's Track collection has no `acquisition` field, and WP16 records provenance only in the pipeline's own database** | Logged at handoff rather than discovered at sign-off, per the both-halves rule. WP16 legitimately never touches Payload, so the pipeline-side half is all it can build. But the moment **WP17** creates a Track in the CMS, provenance stops at the boundary — and the entire point of recording `acquisition` is that "which Tracks must be regenerated when the source question resolves" stays a query rather than an act of memory. A Track in Payload with no acquisition status is exactly the record we said we would never create. **This is `apps/admin` work, so it belongs to Manager, not Pipeline Manager** — a small package, or folded into WP17's start | Architect, WP16 handoff | Open — **must land before WP17 writes its first Track.** Not before WP16, which never opens the Payload boundary |
