# Technical debt — resolved rows, archived 2026-09-15

Moved out of `project/projectRoadmap.md`'s active register because they are closed. **Nothing was deleted** — these are kept so a later question about how something was fixed still has an answer, they are simply no longer loaded by every session at startup.

| Item | Impact | Flagged by | Status |
|---|---|---|---|
| **New state-bearing types need allowlisting and nothing was checking — the failure mode is that it works** | `TransportRecord` round-trips through the checkpointer correctly even while missing from `_CHECKPOINTED_TYPES`: the serializer falls back to a dict and Pydantic rebuilds it, **so every value is right and the only symptom is two `Blocked deserialization` lines per checkpoint read.** That is the shape of defect that survives a year because nothing is wrong with the output. **WP30's `ScenePlan` and WP17's `GeneratedLeafRecord` are in that tuple because somebody remembered.** WP32 shipped a test that walks `PipelineState`'s annotations and fails if any reachable model or enum is off the allowlist — **turning three lucky recollections into a check** | Pipeline Manager, WP32 | ✅ **Closed by the test** — recorded for the pattern |
| **One founder check, ~60 seconds, closes two things that have been open for four weeks** | **Reduce Motion on, replay the payoff unlock.** It is simultaneously WP22.1's 6th criterion and **WP8's founder criterion, open since 2026-08-12**. Both ask the same question — does the reduced-motion path *swap* the feedback or silently remove it — and neither can be answered by a test. The fix and the reasoning are in; **what is missing is one person looking at one transition.** A human distinguishes "faded in" from "just appeared" at 280ms trivially; sequential tool calls cannot bracket it at all | Architect, WP22.1, 2026-09-09 | ✅ **Closed 2026-09-10.** Founder observed the payoff unlock with Reduce Motion on and confirmed the swap. **WP8's criterion, open since 2026-08-12, is closed with it** — four weeks, shut by one person looking at one transition for a minute |
| ~~`AuthStack` sets `slide_from_right` and reduced motion is not respected for it — **required fix**~~ | **Verified false today and closed.** `AuthStack.tsx:52` reads `animation: reducedMotion ? 'fade' : 'slide_from_right'` with `animationDuration` swapped alongside, and carries a comment explicitly rejecting `animation: 'none'` on the grounds that removing feedback *"is worse than the animation it was meant to accommodate"*. **Fixed at some point after WP6 and never recorded**, so the register carried a required-fix entry against working code — the second stale entry this package surfaced. WP22.1 confirms it is also structurally immune to the Reanimated defect, its transition being react-native-screens' native animation rather than Reanimated's | `code-reviewer`, WP6; closed by Architect, 2026-09-09 | ✅ Closed — **stale, not open** |
| ~~**Reanimated reports reduce-motion as ON while the OS reports OFF**~~ | ✅ **Resolved by WP28 and it was not a defect.** Reanimated samples the OS value once at native init and freezes it; RN subscribes and stays live. They diverge only when the setting changes mid-session. **Two things survive the resolution:** the full-motion branches carry no override, so inside that staleness window an animation is suppressed while the fade branch was never taken (→ WP28.1), and **`NextNodeRing` is the surface with the most to lose** since it is the only marker for which node to tap next on a long scrolling graph | Manager, WP27; resolved WP28 | ✅ Closed — see WP28.1 |
| **The roadmap's visual drift was caused by geometry, not typography — and I named the wrong cause** | WP22.2's handoff called the long-label problem "the judgement this package is buying". **It was a symptom.** `graph.jsx` keeps its nodes in a band under a quarter of the frame, leaving ~125pt of label gutter; ours spanned two thirds, leaving **~37pt**. **Every label was a stub because 37pt is a stub** — no typography decision could have rescued it. Fixing the band (2/3 → 0.25) and the vertical rhythm (104–152pt → 24–40pt) also collapsed a ~2000pt scroll into roughly one screen, which is the sparseness the founder's screenshot showed. **The general lesson: a visual symptom traced to the nearest plausible cause is how a package gets pointed at the wrong problem**, and the handoff's own framing was the thing most likely to have led it there | Manager, WP22.2; framing error Architect's | ℹ️ Closed — recorded because the wrong diagnosis was mine and it was confident |
| ~~`motionPlan` is unit-tested… first real exercise is WP8's payoff unlock~~ — **the register was wrong and Manager caught it** | The entry implied WP8 exercised `motionPlan`. It did not: **`motionPlan` had zero callers until WP22.** WP8 and the other three sites call `useReducedMotion` directly and build their own animations, which is exactly how the flag got missed in four places. **Architect repeated the error in WP22's handoff**, writing that both were "already used in four places" — true of the hook, false of the helper. Corrected here so the next reader does not treat the branch as centralised when it never was | Manager, WP22; error originally Architect's | Closed as a correction — the substance moves to WP22.1 |
| **The app cannot render any of Track 42 — relative media URLs fail the domain schema** | Payload stores media as `/api/media/file/...`; `imageAssetSchema` requires `z.url()`; `mapImageParts` passes the value through unresolved. Every Leaf 502s as `CONTENT_INVALID`, whose reasons are logged and never returned — so the app says *"This content is unavailable"* and the cause is visible only in the backend log. **Verified by running the real `mapLeaf` over a live document, not by reading.** Blast radius is exactly Track 42: the 21 placeholder Leaves carry no media, which is why nothing caught it. **Broken since WP20 published the diagrams on 2026-09-01** — the scenario images doubled the count of bad fields rather than causing it. **The same shape as WP15, sign flipped**: that mapper silently dropped new fields with 932 tests green, this one rejects them; both times producer and renderer shipped weeks apart and nothing exercised them together | Architect, 2026-09-02 | ✅ **Fixed in WP15.8.** `resolveMediaUrl` resolves a leading-slash reference against `CONTENT_API_URL`; anything without a slash or a scheme is deliberately left alone so `z.url()` still catches genuine content defects rather than the mapper inventing a fix. Proved by playing a Leaf, not by the tests |
| **`Track.coverUrl` is the same bug, not yet firing** | `mapTrack` passes `coverUrl` through unresolved too, and `trackSchema` requires `z.url()`. It works today only because every cover is an external absolute URL. **It stops working the moment founder item 1 is done**, since replacing the hotlinked cover with an asset we host puts a relative URL in that field. Confirmed: `mapTrack` on Track 42 with `coverUrl: '/api/media/file/cover.png'` fails. **Named in WP15.8's scope for exactly the reason the 2026-08-29 sibling rule exists** — a fix that lands on two of three parallel call sites is a defect scheduled for later | Architect, 2026-09-02 | ✅ **Fixed in WP15.8, before it ever fired** — the sibling rule working as intended rather than being applied after the second failure |
| Payload's stock template tracks their unreleased `main` and disagreed with 3.87.0 in three places (`generatePayloadViewport`, `importMap`, `turbopack.root`) | All fixed and commented at the site. A future Payload upgrade must re-run `payload generate:importmap` and re-check `(payload)/layout.tsx` | Manager, WP1 | Fixed — recorded as an upgrade checklist item |
| **WP3 signed off 11/11 while shipping payoff prose ungated** — "the answer key never leaves the server" had a criterion, "the payoff never leaves early" did not, though they are halves of one guarantee | Every WP3 demo of `/content/leaves/:leafId` showed content the product intends to withhold. Not a Manager error — no criterion covered it, because the gate was WP4's. **Architect process failure**: a package shipped a surface whose guarantee was only half-built and nothing recorded the gap | Architect, WP4 review | **Fixed in WP4.** Rule added to `agents/architect.md`: when an invariant has two halves, both get criteria in the same package, or the unenforced half is logged as debt at sign-off |
| **`apps/backend/src/index.ts` carried two NUL bytes from WP2** (`?? '\0unconfigured'`, where a space was intended) — git treated the composition root as binary, so every diff of it since WP2 was unreviewable | Functionally harmless, but it survived two code reviews because the file *renders* normally; the byte only shows as `Bin` in `git diff --stat` | Manager, WP4 | Fixed in WP4. **Recommend a CI guard**: fail the build if any tracked text file contains a NUL byte — cheap, and this class of defect is invisible to review by construction |
| **The app was pinned to light mode from WP0 to WP6** — `app.json` carried `userInterfaceStyle: "light"`, telling iOS the app has no dark support, so `useColorScheme()` returned light on a dark device and the entire dark theme was dead code | Every unit test passed throughout. Caught only by the criterion "verified by switching theme, not by reading the token file" — **that phrasing is the template for WP7 and WP8's visual criteria** | Manager, WP6 | Fixed (`"automatic"`). Requirement now recorded in `design-direction.md` §3 |
| **Two tab glyphs (`↗`, `☺`) took emoji presentation on iOS and ignored the tint colour** | The active-tab affordance was dead on half the bar while looking correct in any screenshot-free review | Manager, WP6 | Fixed with U+FE0E. Symptomatic of having no icon set — see below |
| **Manual verification found three defects invisible to 792 passing tests** — the flagship 20-Leaf Track sorted 26th of 27 onto a page Explore cannot reach; a placeholder cover background matching the card colour, so a loaded and a failed image looked identical; and `bookTitle` as the upsert key, so renaming a Track orphans it | Nothing was wrong with the data, which is why no assertion could see it. **This is the strongest evidence yet for the manual-verification half of the tiered testing bar.** All three fixed; sort order pinned by a mutation-checked test | Manager, WP11 | Closed — recorded as precedent |
| **The thumbnail check failed first time** — at 130px the streak read and the book did not: `body` weight, muted grey, wrapped over two lines | Fixed to `h3`, full contrast, one line. A truncated title a stranger can read beats a complete one they cannot. **The manual criterion earning its place again** | Manager, WP9 | Closed |
| **Profile showed stale everything** — both cards fetched once at mount and never refreshed, so a reader who finished a Leaf saw "No streak yet", "0 XP", "1 of 19" against a database holding 100 XP and four badges | Library, Journey and Explore already refresh on focus; Profile did not. **Unit tests could not have caught it — they mount once and assert on state that was correct at mount** | Manager, WP5b | Fixed in `13c1084` |
| **Migration 0004 shipped without a drizzle snapshot** — hand-written SQL and journal entry only, so `db:generate` diffed against 0003 and emitted a 0005 re-creating both tables | Would fail on an empty database and break every integration suite. A latent CI bomb found by accident. Repaired; snapshot reconstructed byte-identical and 0004's journal timestamp restored | Manager, WP5b | Closed — **standing rule added: a hand-written migration must ship its snapshot** |
| **`2e99714`'s message and diff disagree** — it describes only the env fix but also carries `launch-blockers.md` and roadmap changes swept from the shared tree | Flagged rather than amended, correctly: the commit is on `origin/main` and rewriting pushed history is worse than an inaccurate message. `f743b31` explains it | Manager, WP5b | Closed — recorded, not fixed |
| ~~**Plaintext password in React Navigation route params** (`EmailSignUpDraft`)~~ | **Verified false and struck 2026-09-09.** The type does not exist; `auth/SignUpDraft.tsx` holds the draft in a ref-backed context, and its docstring gives exactly the reasoning this entry demanded — *"it exists so the password never becomes a navigation param... that state is serialisable by design."* Fixed in WP6, **the register carried a "Required fix" against solved work for five weeks, and my WP24 handoff then cited it as live** — sending Manager to check something already done | `code-reviewer`, WP6; struck by Manager, WP24 | ✅ Closed — stale |
| ~~Reduced-motion behaviour unverified — WP6 ships no animation to swap~~ | **The premise was wrong**: `AuthStack.tsx` sets `animation: 'slide_from_right'`, and `useReducedMotion()` is exported but never called. There *is* an animation and reduced motion is not respected for it | `code-reviewer`, WP6 | **Required fix** — wire it or remove the animation | `motionPlan` is unit-tested to prove the rule is swap-not-remove and that no 'none' outcome exists by construction | Manager, WP6 | Open — **first real exercise is WP8's payoff unlock** |
| `eslint --fix` strips type assertions on Fastify `inject().json()` (typed `any`), silently un-typechecking tests | Replaced with a `bodyOf<T>` helper routing through `unknown`. Do not trust `--fix` on backend test files | Manager, WP2 | Fixed — recorded as a working note |
| ~~**`page` locators are never populated on source references**~~ | ✅ **Closed 2026-09-15 — ruled not achievable and not worth wanting.** WP30 established it on Ikigai, the first PDF: `parse_pdf` joins pages into chapter text before anything downstream could use them, and the numbers would be **PDF page indices, not the printed book's** — which `content.ts` itself calls edition-dependent false precision. **So the honest locator set stays chapter and quote.** Recorded because the hope that PDFs would unlock pages was reasonable and is now answered | Pipeline Manager, WP15; answered by WP30, closed by Architect | ✅ **Closed** |
| ~~**`npm run typecheck` is red on `main`**~~ | ✅ **Closed 2026-09-11 — it was fixed two weeks earlier and this row never caught up.** WP15.1 (`24dd465`, 2026-08-27) applied exactly the fix this row prescribes: the fixture now reads `'dot' as unknown as CmsSpecFormat`, so the deliberate negative test survives and the literal still typechecks. **Confirmed by running it**, not by reading the diff — `typecheck` exits 0 against a backend tree byte-identical to `origin/main`. **The roadmap's own WP15.1 row has said *“root `typecheck` green for the first time since WP15”* since that day**, so two lists in this file disagreed with each other for two weeks. That is the stale-blocker cost already written into the *Blocked on the founder* section, occurring inside the debt register instead | Pipeline Manager, WP16; diagnosis corrected by Architect; closed by Architect 2026-09-11 | ✅ **Closed** |
| **~~53~~ 74 orphaned media files** — corrected 2026-09-02 | The library holds **146 media, 72 referenced, 74 unreferenced.** WP20's "53" was counted mid-run, before asset regeneration finished, and should not be cited. **This is the third wrong number this project has recorded with confidence** — after the $0.00 text cost and the 3.4×-under image rate, both from WP20 — and all three share a shape: a measurement taken at a moment when the thing being measured was still moving. The attaching half is closed (WP15.7 landed, WP20.1 attached 18/18 as drafts). **The sweep stays deferred and that is deliberate: a candidate the founder might switch to only becomes an orphan after they switch.** Also still true: the 73-minute gate 2 figure excludes image selection entirely and must not be compared against a later measurement that includes it | Pipeline Manager, WP20; corrected WP20.1 | Open — **sweep once the founder has overridden the 8 glow breaches and published.** Then it is a query, not a judgement |
| **~~⚠️ The paid-tier constraint is enforced by nothing, and a copyrighted book has already run~~** | `config.py:26` states that `require_paid_tier` *"turns that from a memory into a check"*. **`require_paid_tier` does not exist** — grepped across the whole repo, the only hit is that comment. `paid_tier: bool = False` is read by no code; `client.py` mentions it in a second comment. `GeminiClient` takes `use_vertex` and `api_key` independently and **cross-checks neither against the book**. **Google's free tier trains on submitted content**, so this is the control that confines development to public-domain books — and it is prose. **Ikigai went through on discipline alone**: Pipeline Manager ran every command with `USE_VERTEX=true` and the key unset and verified the transport before the first paid call, **and nothing in the repo records that it did** — the run log has no transport line, so the claim is unverifiable after the fact and always will be. **Ruled: this is the next pipeline package, ahead of the image detector.** And it should key off the Track's `acquisition` status rather than a hand-set bool — `public-domain` may use the free tier, everything else must be Vertex — because a flag someone has to remember to set is the failure this entry already is. **The run must also record its transport**, so provenance is a query rather than a memory, exactly as `acquisition` already is | Pipeline Manager, WP30.1; verified by Architect 2026-09-15 | ✅ **Closed 2026-09-15 by WP32, merged as PR #47** — `require_paid_tier` exists, is called from two sites, keys off `acquisition`, and records the transport; `ZOOMOUT_PIPELINE_PAID_TIER` is gone from the code. **Archived 2026-09-16**, a day late: the sign-off added the WP32 row but left this one open and left the 'Where things stand' warning in place, so the file asserted both that the control was a comment and that it was merged. **The sweep is part of sign-off, not a follow-up.** |
| **~~A settings validation failure prints part of the Gemini API key~~** | Any command run without `ZOOMOUT_PIPELINE_DATABASE_URL` raises a pydantic `ValidationError` whose message echoes the whole input dict, **including `gemini_api_key`**. Pipeline Manager hit it by accident and a truncated key landed in a terminal; it would land in any log or CI output the same way. **Verified in the code 2026-09-16: `config.py:101` declares `gemini_api_key: str` and `:172` `payload_api_key: str` — plain `str`, and `SecretStr` is not imported.** Fix is `SecretStr` on both, one line each plus a test; pydantic then renders them as `**********`. **This is a credential in a log, so it does not wait for a convenient package** | Pipeline Manager, WP33 | ✅ **Closed 2026-09-16 by WP33.1, merged as PR #49** — and **not by the fix this row named.** `SecretStr` alone does not close it; `hide_input_in_errors=True` does. Both shipped, mutation-checked as independent reversions. See the decisions log. |
| ~~**The design system has gained tokens since WP21 measured the diff at zero**~~ | **Resolved 2026-09-09 by reading the exported token files, and it was never a drift.** All three are semantic aliases over tokens the app already has: `--graph-edge: var(--border)`, `--graph-edge-reached: var(--primary)`, `--graph-node-unreached: var(--surface-3)`. **The zero diff still holds.** Recorded because the wrong conclusion was reachable from the evidence I had — a token name absent from `palette.ts` looks identical to a divergence until you read the definition, which is the same "verify at the point of citation" rule that produced this entry in the first place | Architect, WP23.1 handoff; resolved by Architect, 2026-09-09 | ✅ Closed — not a divergence |
| **Superseded — see the entry above** | `design/leaf_player/Leaf player.html` uses `var(--graph-edge)` and `var(--graph-node-unreached)`; **neither exists in `apps/mobile/src/design/palette.ts`** — checked, not assumed. WP21's zero token diff was true when taken and **is no longer a standing guarantee**: the system is live and picked up at least the Caveat font and these graph tokens since. **Consequence for every remaining package**: "no new colour values" and "the tokens already match" are now two different claims, and a mockup referencing a token the app lacks will look like a missing value rather than a divergence | Architect, WP23.1 handoff, 2026-09-09 | Open — **re-measure the token diff once the redesign packages are done**, and treat any mockup token that has no app counterpart as a question rather than a gap to fill |

> **The two rows above were appended 2026-09-22.** They had been sitting in `projectRoadmap.md`'s
> **decisions log** — a four-column table of dated rulings — carrying the debt register's shape and no
> date at all. Being foreign to that table, they broke it in two, which is why a second `| Date |
> Decision |` header had been added below them to restart it. Both are resolved, so they belong here.
> Moved verbatim; the second one's "see the entry above" still refers to the first, which is why they
> moved together.
